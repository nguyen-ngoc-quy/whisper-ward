# Review Log — Game Concept

Revision history for `design/gdd/game-concept.md`.

---

## Review — 2026-08-12 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: ai-programmer, level-designer, game-designer, creative-director
Blocking items: 4 | Recommended: 4+
Summary: The concept's centerpiece AI is well-scoped, but the review found the MVP could not test its own hypothesis (noise-maker deferred), a contradiction between "committed" position-prediction chase and its stretch-gated risk, unmeasurable "fair/engaging" acceptance criteria, and ambiguous FSM-vs-BT framing. Two disputes surfaced: AI architecture (FSM-first + minimal BT vs BT-primary) and suspicion-meter behavior (early-warning compounding meter vs classic meter). The user adjudicated: **FSM core + minimal BT showcase** and **early-warning compounding meter**.
Prior verdict resolved: First review

### Blocking Items (all 4 resolved in this session)
1. **MVP can't validate its own hypothesis** — noise-maker moved INTO required MVP list (throwable → arbitrary `NoiseEmitter` point); core hypothesis rewritten as 3 falsifiable claims (state-any-detection / clean-run-after-one-observation / noise-maker used ≥1×).
2. **Position-prediction contradiction** — chase demoted to last-known-position + re-sight retargeting (committed); position prediction is stretch-only, gated behind playtest. Applied consistently across Unique Hook, Technical Challenges, Risk, MVP, Scope Tiers.
3. **Unmeasurable "fair/engaging" criteria** — replaced with testable player-outcome acceptance criteria.
4. **FSM vs BT architecture ambiguity** — resolved as FSM core (hard 3-state cap) + minimal BT portfolio showcase built after the FSM is proven.

### Recommended Revisions (applied)
- Early-warning compounding suspicion meter (user-adjudicated)
- Suspicion Attribution added to Pillar 4 design test
- S/A/B room grading replaces abstract mastery loop
- Heightened-alert-on-capture tension budget
- Alert propagation hardened: imprecise info (area, not position), only detector chases, guards ignore self-alerts
- Open Questions updated to match new systems

### Status
Revisions applied. **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) to confirm the blockers are resolved.

---

## Review — 2026-08-12 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: systems-designer, game-designer, unity-specialist, ai-programmer, level-designer, qa-lead, creative-director
Blocking items: 4 | Recommended: 8+
Summary: Re-review confirmed all 4 prior blockers substantively resolved, but four definitional holes remained in the concept's own claims and loops: "detected"/"clean run" in MVP claim 2 undefined, the S/A/B grade punishing the noise-maker and near-miss loop, the suspicion meter described by two opposite behaviors, and a guard-count contradiction plus "room-as-puzzle" framing that clashed with the anti-pillar. User adjudicated B2 as "consequences, not verbs" (suspicion accrual + suspicion-escalating noise). All 4 blockers resolved via edits (operational definitions, grade-input redefinition, committed two-timeframe meter model, segment reframing + movement scope-cap anti-pillar).
Prior verdict resolved: Partially (all 4 from prior round confirmed resolved; 4 new blockers found and resolved this round)

### Blocking Items (all 4 resolved this session)
1. **B1 — "detected" undefined in MVP claim 2** — core hypothesis rewritten with operational definitions: *visually detected* = guard escalates above Patrol via LOS suspicion; noise-triggered Investigate without LOS suspicion is NOT a detection; *capture* defined; claim 1 anchored to capture-trigger log via dev-only Suspicion Attribution gizmo (added to MVP).
2. **B2 — S/A/B grade punished the noise-maker + near-miss loop** — grade redefined (user-adjudicated "consequences, not verbs") to *suspicion accrual* (time-integrated meter; tolerates isolated partial fills, penalizes compounded exposure) + *noise that escalated suspicion* (clean tool use not penalized) + *time* (wide band/soft bonus).
3. **B3 — "near-immediate fill" vs "early-warning" contradiction** — committed semantic model: two-timeframe ladder (brief confirm window → Investigate; sustained LOS → Chase; slow-decaying residual = persistent partial memory, NOT a permanent ratchet). Exact rates/curves deferred to Perception GDD tuning knobs. Also fixed "Patrol → suspicion → Investigate → Chase" 4-state wording.
4. **B4 — guard-count contradiction + "room-as-puzzle" framing** — "room" replaced with *segment* (zone bounded by thresholds/sightlines in one continuous facility; guards patrol across boundaries); guard count reconciled to 2-3 total; "routes change" pinned to cheap reading (patrol deviation + scan-rate tuning + ≤1 authored contingency per segment); difficulty scaling reframed as parametric tuning under the 3-state cap.

### Recommended Revisions (deferred to their proper tiers, not concept edits)
- AI GDD (R1): two-timeframe ladder rates, 3-state transition matrix, Investigate sub-behaviors, alert via radius+occlusion, noise-event schema, physical projectile + throw-seen decision, BT as decision-only wrapper over shared leaf pool (~100-150 lines), capture definition, cross-guard detection transfer, GlobalAlertSettings object.
- QA/playtest plan (R2): measurement protocols for the 3 claims (N=5, separate cohorts, pass thresholds), AC bounds for MVP items 1-4, claim 3 strengthened to voluntary use.
- /prototype + sprint-planning (R3): movement/camera/animation triangle as milestone 0 with "controller feel acceptable" pass/fail gate; producer revalidates "2-3 weeks for one room" estimate.
- Architecture docs (R4): pure-logic cores (Tick(float dt), interfaces for Time/Physics/NavMesh), hysteresis, fixed perception tick, injectable seeded RNG.
- Level-GDD (R5): continuous-facility anti-pillar first validated at Vertical Slice (+20% geometry); footprint/encounter count/minutes-between-encounters to justify 45-60 min.
- Noise GDD (R6): noise-maker physicality (thrown object lands, event on landing); post-Chase resolution; "can noise pull a chasing guard?" priority.
- Tier the capture→heightened-alert modifier explicitly (R7, currently in no scope tier); distance-based LOS charge falloff (R8).

### Status
Revisions applied. **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) to confirm B1-B4 are resolved.

---

## Review — 2026-08-13 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 5 | Recommended: 1 (user decision)
Summary: Re-review confirmed B1-B4 from round 2 substantively resolved but surfaced 5 remaining definitional holes plus a user-adjudicated Pillar 3 commitment. All blockers resolved via a user-approved 12-edit changeset. Pillar 3 committed two manipulation verbs (Burst = throwable noise-maker MVP; Lure = placed noise source, Target tier; both reuse `NoiseEmitter`) with an explicit anti-puppeting rule.
Prior verdict resolved: Partially (round-2 blockers confirmed resolved; 5 new blockers + 1 user decision found and resolved this round)

### Blocking Items (all 5 resolved this session)
1. **B1 — suspicion meter semantic model still ambiguous** ("compounds" + "near-immediate" vs "early-warning") — committed two-component model: (a) sustained-LOS accumulator, charge scales inversely with distance (prototype-validated), FULLY RESETS on LOS break, reaches Chase only after sustained continuous LOS; (b) separate residual that persists/decays slowly, only lowers Investigate re-entry threshold, NEVER accumulates toward Chase. Chase entry vs maintenance (via LKP) distinguished; state-independent LOS accumulation; ~4s investigate give-up self-resolution. Unique Hook rewritten to "fills fast and lingers after breaking line of sight".
2. **B2 — segment definition still clashed with anti-pillar** ("room-as-puzzle") — segment committed as designer-authored spatial-functional zone delimited by geometry and sightline choke points; boundary crossings scheduled and learnable, not free drift.
3. **B3 — "only the detector chases" contradicted personal sight** — alert propagation hardened: alert-derived info never chases on rumor alone, but any guard with personal sustained LOS always promotes to chaser.
4. **B4 — MVP claims not falsifiable** — core hypothesis reframed as directional (validates loop, not proof of engagement); "true cause" operationalized (any contributing event the Suspicion Attribution gizmo log confirms); measurement protocol (N, thresholds, screening, attempt bounds) committed to prototype-2 playtest plan.
5. **B5 — "guard density" difficulty scaling contradicted 2-3 guard cap** — reframed as coverage (a larger share of the same guards' route time) + parametric tuning.

### Recommended Revisions (applied)
- Pillar 3 user decision (Option A): **Burst (MVP) + Lure (Target tier)** — two committed manipulation verbs, both reuse `NoiseEmitter`; anti-puppeting = limited tool resource + grade penalizes compounded exposure. Scope Tiers Target row updated; MVP item 3 named "Burst".
- S/A/B grade: noise NOT graded as separate input (meter already contains any spike); attribution to the segment where the player lands; clean tool use never penalized.
- Content Volume: "first full run" phrasing (mastered clean runs meaningfully shorter — targets in Level GDD).
- Noise system: events do not add directly to the suspicion meter — they trigger Investigate/orientation only.

### Status
Revisions applied. **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) to confirm B1-B5 + Pillar 3 are resolved.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 6 | Recommended: 3+ (incl. 4 user design decisions)
Summary: Re-review confirmed round-3 blockers (B1-B5 + Pillar 3) substantively resolved, but 6 definitional/contract holes remained at concept tier: residual source undefined + grade double-count; grade operator unable to penalize compounding + chase-across-boundary attribution; Chase contract undefined + "physically catches" unimplementable in Unity NavMesh; MVP hypothesis unfalsifiable (claim 1 tautological, prototype-2 dangling); forgiveness stack cheapening the fantasy (guards forget too easily); phantom camera system + permanent alert ratchet + 45-60min vs 2-3 guards volume infeasible. Two specialist disputes surfaced (prototype-validated claim; difficulty=coverage) — resolved by fact and parametric-promotion respectively. User adjudicated 4 design forks. All 6 resolved via a user-approved 14-edit changeset.
Prior verdict resolved: Partially (round-3 blockers confirmed resolved; 6 new blockers found and resolved this round)

### Blocking Items (all 6 resolved this session)
1. **B1 — Residual source undefined + grade double-count** [ai] [systems] — residual committed to charge **in parallel with the accumulator during any LOS exposure at a lower rate**; strengthened (lowers Investigate re-entry threshold, re-detects faster, investigates more thoroughly); **never accumulates toward Chase**; fruitless noise investigations accrue a bounded share of residual wariness. Residual **deliberately excluded** from the graded quantity.
2. **B2 — Grade operator self-contradictory** [systems] — committed operator: **time-integral of the LOS accumulator above a forgiveness floor** + **compounding penalty per Investigate/Chase escalation event**, **aggregated as the worst-guard (max) integral**, with **exposure-source attribution** (chase begun in A, resolved in B → still penalizes A; carried-in residual never penalizes the new segment).
3. **B3 — Chase contract undefined + "physically catches" unimplementable** [ai] [unity] — committed Chase escape contract (LOS break → LKP → re-sight resets → give-up window → brief Investigate sweep → Patrol); **capture = distance/trigger check** (guard within catch range + short timer), not a body-catch.
4. **B4 — MVP hypothesis unfalsifiable** [qa] — claim 1 reframed to a **specific** true cause (more specific than "the guard saw me"); hypothesis narrowed to **fairness and learnability** (engagement verdict deferred to playtest sign-off); gizmo = **transparent reporter of the FSM's actual decision inputs** with the capture cause **hidden until after** the player states it; measurement protocol committed to `design/qa/prototype-playtest-plan.md` (the HTML prototype's N=1 internal result is explicitly NOT that protocol); Suspicion Attribution telemetry added as MVP item 5.
5. **B5 — Forgiveness stack cheapens fantasy** [game-designer] — strengthened residual (user-chosen tension lever) + floor+penalty grade + alert state **de-escalates on checkpoint passage** (retries practice the same observed schedule; stakes persist via the tainted grade).
6. **B6 — Camera phantom + permanent alert ratchet + volume infeasible** [level] — static LOS-cone cameras **committed** (low-scope, Target tier, disable interaction); alert ratchet **de-escalates on checkpoint**; content **budget band committed**: 8±2 segments at 3-6 min each, ≥3 distinct encounter types, 2-4 min between pressure events.

### User Design Decisions (2026-08-14, applied)
- **Residual model**: Strengthened residual (charges in parallel during LOS + bounded fruitless-noise wariness; exposed guards investigate more thoroughly + re-detect faster; grade stays accumulator-only).
- **Grade operator**: Floor + event penalty (integral above forgiveness floor + compounding penalty per escalation; worst-guard max aggregation).
- **Cameras**: Commit static cameras (low-scope system: static LOS cone + disable interaction).
- **Content volume**: Commit budget band (8±2 segments, 3-6 min each, ≥3 encounter types, 2-4 min between pressure events; 45-60 min target kept).

### Recommended Revisions (applied)
- Difficulty scaling: **parametric AI tuning** (patrol speed, scan rate, suspicion-fill rate, investigate duration) promoted to primary lever; coverage demoted to a bounded spatial-redistribution variable (envelope committed in Level GDD).
- Segment rule: every segment offers **≥1 route reachable only via a manipulation verb** (route-required, not optional) — keeps the "consequences, not verbs" principle while fixing the over-correction.
- Content Volume row: budget band committed; Open Question 1 answered (Prototype-1 **PROCEED**, engine milestone-0 to validate strengthened-residual behaviors).
- Scope Tiers: Target row updated with **+ static cameras**.

### Status
Revisions applied (14-edit changeset, user-approved, 2026-08-14). **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) to confirm B1-B6 + the 4 user decisions are resolved.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 5 | Recommended: 3
Summary: Re-review confirmed round-4 blockers (B1-B6 + 4 user decisions) substantively resolved, but 5 definitional holes remained at concept tier: grade escalation events undefined; Chase contract undefined + the "Unity NavMesh cannot implement a body-catch" claim factually false; alert de-escalation referenced checkpoints the MVP doesn't have; content-band arithmetic couldn't reach 45-60 min; Suspicion Attribution visibility contradicted itself across Pillar 4 / operational definitions / MVP item 5. User adjudicated 3 design forks (respawn de-escalation, base-threshold events, keep-45-60-and-fix-the-math). All 5 resolved via a user-approved 16-edit changeset.
Prior verdict resolved: Yes (round-4 blockers B1-B6 + 4 user decisions confirmed resolved)

### Blocking Items (all 5 resolved this session)
1. **B1 — Grade escalation events undefined** [systems] — escalation event defined as **fresh LOS exposure crossing a committed BASE Investigate/Chase threshold**; noise-triggered Investigates, alert-derived Investigates, and residual-lowered re-entries never count. Aggregation rewritten to **episode-based** (simultaneous same-exposure counted once via max-within-episode; sequential exposure to different guards summed across disjoint episodes). Compounding growth law + event-counting rules deferred to Systems/Perception GDDs.
2. **B2 — Chase contract undefined + NavMesh "cannot implement" false** [ai] [unity] — accumulator **resets at Chase entry** (the LKP LOS-break is a state transition, not a suppressed reset; post-Chase meter starts at zero); re-sight requires **sustained LOS ≥ min duration** (single-tick corner occlusion doesn't reset the hunt); **chase-duration cap + re-sight-count cap** bound the hunt (no free corner-stalemates). Capture reworded to **design-scope rationale** — NavMesh agents DO chase via pathfinding; catch checks NavMesh path distance or a clear-LOS gate (never raw Euclidean); a scripted grab is animation/IK scope. Also committed: bounded charge form `rate = min(k/d, rate_max)` + confirm-window LOS-break-cancels rule.
3. **B3 — Alert de-escalation referenced nonexistent MVP checkpoints** [game-designer] — de-escalation now **on respawn at a segment-start checkpoint**; heightened-alert explicitly **Target-tier** (the MVP room's fast restart always returns to baseline); the permanently tainted grade is the persisting stake.
4. **B4 — Content-band arithmetic can't reach 45-60 min** [level] — "3-6 min" defined as **first-run traversal excluding failed attempts**; pressure event operationalized (a designed tension beat, not ambient noise); model added: *segment sum × ~1.3 observation/retry overhead + modeled escape (~3-5 min)* → band's upper half (8-10 segments @ 4.5-6 min) lands 45-60; quicker players ~30-40; mastered clean runs ~20-30. Session Length + Time availability rows reconciled.
5. **B5 — Attribution visibility contradiction** [qa] — all attribution **hidden in the MVP tester build** (no live display during the run; capture cause revealed only after the player states it); player-facing Suspicion Attribution is an explicit **Target-tier HUD** feature. Wording unified across Pillar 4 / operational definitions / MVP item 5.

### User Design Decisions (2026-08-14, applied)
- **Alert de-escalation**: **Respawn de-escalates** — respawn at segment-start checkpoint returns to the baseline patrol schedule; heightened alert = Target-tier system.
- **Grade events**: **Base-threshold events** — escalation = fresh LOS crossing committed BASE Investigate/Chase thresholds only.
- **Content band**: **Keep 45-60, fix the math** — first-run traversal + ~1.3 overhead + modeled escape.

### Recommended Revisions (applied)
- Camera fairness posture: detection follows early-warning philosophy, **never instant capture**, visually telegraphed (line 78).
- 3-state cap applies to **top-level FSM states only**; sub-behaviors within a state are the sophistication lever (line 102).
- MVP room gains two geometry ACs: objective reachable by pure timing alone; Burst-only route never opened by patrol schedule alone (line 234).

### Status
Revisions applied (16-edit changeset, user-approved, 2026-08-14). **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) to confirm B1-B5 + the 3 user decisions are resolved.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 12 | Recommended: 3
Summary: Creative-director ruled **final revision pass** — 12 blockers, all pinning fixes. Review-#5's arithmetic claim (content band reaching 45-60 min) was textually corrected but not logically verified, so this round forced the band to actually close. Convergence was strong: 5 of 6 lenses independently flagged the content-band arithmetic as false; the Chase contract had a genuine AND-inversion; the "whichever is stricter" capture gate was unimplementable as written; "exposure episode" was undefined, making the grade operator vacuous; WebGL runtime telemetry logging was unsupported by the committed platform. User adjudicated 3 design forks (keep-45-60-and-fix-the-math, proximity-only capture, respawn de-escalation + 2nd alert trigger). All 12 resolved via a user-approved 16-edit changeset.
Prior verdict resolved: Yes (round-5 blockers B1-B5 + 3 user decisions confirmed resolved)

### Blocking Items (all 12 resolved this session)
1. **B1 — Content arithmetic false + timeline contradiction** [level] [producer] — "3-6 min each × 8±2" can't reach 45-60 min even in the upper band. Fixed: band corrected to **8±1 segments at 4-5.5 min each (first-run traversal, excluding failed attempts), ≥3 encounter types, 2-4 min between pressure events**; first full run ≈ segment sum S × ~1.3 + modeled escape (~10-15% of S via a designed shortcut, not a full retrace); **the band closes to 45-60 min when S ∈ [32, 42] min**; quicker players ~30-40; mastered clean runs ~20-30 (~0.55×S + escape). Timeline reconciled: **Target tier ~10-12 weeks (2-3 months)**, Full Vision ~14-16 weeks (3-4 months).
2. **B2 — Chase termination AND-inversion** [ai] — "no re-sight AND no bound reached" made the chase unstoppable for a corner-staler. Fixed: termination is **OR** (give-up-window-expired ∨ duration-cap ∨ re-sight-count-cap), all caps **per-Chase-episode**; re-sight resets **only** the give-up window, never the duration cap; sub-threshold LOS does not reset the give-up window; ordering committed: `re-sight min-duration < give-up window < chase-duration cap`.
3. **B3 — Capture "whichever is stricter" unimplementable** [ai] [unity] — a gate that is "whichever is stricter" between distance and LOS would never trigger. User adjudicated: **proximity-only** — catch range measured along the NavMesh path (`agent.remainingDistance`, gated on `!agent.pathPending`) for a short timer, **no LOS component**; catch range must exceed the agent radius; off-mesh (path-partial) falls back to the chase give-up window. **Capture is a terminal outcome of Chase, not a fourth FSM state.**
4. **B4 — Peek→break→repeat exploit** [systems] — peeking (LOS gain, break) resets the accumulator each time, so repeated brief peaks never escalate and never punished. Fixed: the **residual lowers the Investigate *entry* threshold for any subsequent exposure episode by the same guard** (first entry uses the base threshold; every later entry uses the residual-lowered value); the drop is **player-visible** (a threshold marker that sinks on the meter — Pillar 4).
5. **B5 — Residual-during-Chase contradiction** [ai] — text had residual charging during Chase, contradicting "meter inert during Chase". Fixed: **during Chase both the accumulator and the residual are inert**.
6. **B6 — "Exposure episode" undefined → grade vacuous** [systems] — the episode-based aggregation referenced a term that was never defined. Fixed: **episode = maximal continuous LOS interval to a single guard, closed by LOS break ≥ X s or segment transition**; overlapping episodes merge before per-episode max; same-guard consecutive and different-guard disjoint episodes **sum**; **one escalation event per episode at its highest reached threshold**; **confirm-window-cancelled episodes are excluded** from the integral and count no event; **the segment grade accumulates all exposure across attempts, including failed/captured attempts** — a respawn retry cannot wash the grade clean.
7. **B7 — WebGL runtime telemetry unsupported** [unity] — the committed WebGL target has no runtime file system, but the trace was spec'd to flush to a file. Fixed: full FSM decision trace held in an **in-memory ring buffer, flushed only after the player states the cause** (build-enforced), and **the tester run plays on the PC build**; the WebGL demo flushes via clipboard transcript.
8. **B8 — Camera consequence undefined** [level] — a full camera meter had no defined consequence. Fixed: full camera meter **routes to the segment's guards as an alert-derived Investigate at the camera's location** (imprecise area, not position) — never a separate camera capture, never instant.
9. **B9 — Alert perverse incentive** [game-designer] — de-escalation-on-respawn made dying the fastest way to clear a high alert. Fixed (user decision): **MVP keeps respawn de-escalation** (fast restart returns to baseline) **+ a second Target-tier trigger** — the alert also decays after [N] s without incident, or de-escalates on segment clear. Renamed "alert state" → **facility alert level** (a global parameter modifier, not a guard FSM state).
10. **B10 — Geometry ACs not testable + verb-route tension** [level] [qa] — ACs lacked sampling/verification protocol; "route-required" read as verbs-always-mandatory. Fixed: three geometry ACs — **(a) existence** (a spawn-to-objective route whose full transit at walk speed accrues zero LOS suspicion past the confirm window across one full patrol period including scan sweeps), **(b) universal** (the Burst-only route's sightline is never clear for a full transit without a noise event, sampled across the same cycle), **(c)** the Burst investigation hold-position + look-around arc must not re-cover the Burst-only route's sightline; verified via the dev Suspicion Attribution overlay (≥10 Hz sampling, ≥3 consecutive cycles). Verbs are **structurally load-bearing, never the only route in any segment**; goal examples made non-gating (disable a camera, reroute coolant).
11. **B11 — Terminology collision** [game-designer] — "alert state" collided with the guard FSM's 3-state cap. Fixed: "facility alert level" (global modifier) throughout; capture explicitly **not a fourth FSM state**.
12. **B12 — HideSpot LOS contradiction** [systems] — the text said hiding blocked charge but was inconsistent about whether it broke LOS. Fixed: a hidden player is **not perceptible to LOS — a genuine LOS break**; hiding composes with the confirm-window and re-sight rules.

### User Design Decisions (2026-08-14, applied)
- **Content volume**: **Keep 45-60, fix the math** — closing corridor S ∈ [32, 42] min; escape modeled as a fraction of S via a designed shortcut; timeline reconciled to 2-3 months.
- **Capture gate**: **Proximity-only** — catch via NavMesh path distance (`remainingDistance`, gated `!pathPending`) for a short timer; no LOS component; off-mesh → give-up fallback.
- **Alert reset**: **MVP respawn + 2nd trigger** — respawn de-escalation kept for MVP; a second Target-tier trigger added (alert decays after N s without incident / de-escalates on segment clear).

### Recommended Revisions (applied)
- Moment-to-moment wording no longer resurrects the rejected "brief sight escalates" model; "closer fills faster **up to the cap**" (perception).
- MVP claim 2 scored as two sub-bars (comprehension + execution) with an attempt bound; claim 3 gains **unprompted** use + discoverability thresholds (only after the build presents the tool).
- Honest line added: the S/A/B grade-loop validation is scheduled for the Vertical Slice playtest, not the MVP.
- Core-hypothesis claim framing narrowed to first **post-observation** attempt.

### Status
Revisions applied (16-edit changeset, user-approved, 2026-08-14). Review #6 = **final revision pass** — creative-director found the concept converged: no new structural holes after this changeset. **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) to confirm all 12 blockers + the 3 user decisions are resolved, then APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 6 | Recommended: 3
Summary: Creative-director's #1 finding was a **genuine regression introduced by review #6**: the proximity-only capture used `agent.remainingDistance`, which measures path distance to the guard's *destination*, not the player — during the LKP leg of a Chase the guard arrives at the stale last-known-position, `remainingDistance → 0`, and the catch timer fires, "capturing thin air." Independently confirmed by unity-specialist and ai-programmer; resolvable from the doc (fix the metric, not the design). The content-band arithmetic was restated rather than verified for the **third consecutive review** (exemplar 8@4-5.5 = [32,44] exceeds the committed S ≤ 42). Second-order convergences across lenses: off-mesh capture immunity (unity/ai/qa), residual threshold with no floor (systems), grade black-box + no cross-run record (game/qa), MVP hypothesis + geometry ACs not independently testable (qa/level). Verdict **NEEDS REVISION** with a focused 10-edit changeset, NOT MAJOR REVISION; convergence "real but partial." User adjudicated 4 design forks (relax verb-route rule, commit grade card + cross-run record, segment-count dial, conditional approval). All 6 resolved via a user-approved 10-edit changeset.
Prior verdict resolved: Partial — review #6's "final revision pass" applied its changeset but introduced the capture-metric regression and left the content-band arithmetic restated rather than verified.

### Blocking Items (all 6 resolved this session)
1. **B1 — Capture metric regression** [unity] [ai] — `agent.remainingDistance` measures path distance to the guard's *destination*, not the player; during the LKP leg the guard arrives at the stale LKP and the catch fires on thin air. Fixed: catch metric is **guard-to-player distance** — chase target is the player's **NavMesh-sampled feet position** (`NavMesh.SamplePosition`, throttled ~2-5 Hz, never every frame), catch check runs **only while the guard holds a live player fix** (set on valid re-sight or "saw the player enter a hide spot"; cleared on redirect to LKP); chasers use `stoppingDistance = 0`; catch timer **pauses** during `pathPending` and tolerates intermittent range exits (hysteresis); a **Euclidean sanity backstop** prevents stale/invalid paths from triggering a catch. Capture op-def in MVP updated to match.
2. **B2 — Off-mesh capture immunity** [unity] [ai] [qa] — path-partial positions evaded capture via the give-up-window fallback. Fixed: off-mesh fallback is now the **Euclidean backstop**, not the give-up window; hide-spot clause gains a **live fix on the spot's front under the capture contract** — a hide spot, on- or off-mesh, is never capture immunity; new geometry AC **(d) no-capture-immunity** (no reachable off-mesh position evades a catch longer than the give-up window; NavMesh coverage verified at the milestone-0 bake).
3. **B3 — Residual threshold has no floor** [systems] — with no clamp, the residual-lowered entry threshold could reach zero/negative, breaking the crossing semantics and reading as random detection (Pillar 2 violation). Fixed: residual **clamped** (`R ∈ [0, R_max]`), effective entry threshold **floored** (`T_entry = max(T_base − k·R, 0.2 × T_base)`), decay rate pinned as a first-order tuning knob with target scale (~45-60 s: persists within a segment, resets between encounters).
4. **B4 — Content-band arithmetic false (3rd consecutive occurrence)** [systems] [level] — the "e.g. 8 segments at 4-5.5 min" exemplar sums to [32, 44], exceeding the committed S ∈ [32, 42] upper bound. Fixed: band restated as an **invariant** — **S ∈ [32, 42] min is the binding constraint** (7 → 4.6-6.0, 8 → 4.0-5.25, 9 → 3.6-4.7 min); violating exemplar removed; **segment count is the first scope dial** (if content velocity < ~1 polished segment/week, shrink toward 7 and stretch per-segment minutes — cut segments before AI depth, Pillar 1).
5. **B5 — Grade black-box + no cross-run record** [game-designer] [qa] — the grade operator had no legibility principle and no persistent improvement surface. Fixed: **grade legibility committed (Pillar 4)** — grade inputs are the same visible meter + visible escalation events; a post-segment **grade card** (Target-tier HUD) shows the meter trace, escalation-event list, and penalty breakdown; a **cross-run best record** (best segment grade / best full run) is a Target-tier HUD element; **MVP emits no S/A/B output** — it collects grade-input telemetry only, with S/A/B labels placeholder until the forgiveness-floor value, compounding law, and band thresholds are defined and validated at the Vertical Slice playtest.
6. **B6 — MVP hypothesis + geometry ACs not independently testable** [qa] [level] — claim 2 scored on a single ambiguous attempt with no bound (residual carryover between attempts unlogged); AC(a) qualified only at walk speed with "zero suspicion" undefined; AC(c) mixed MVP-requirement with forward-design commitment; no noise-re-cover guard, no observation vantage, no off-mesh immunity check, verification without residual reset. Fixed: **claim-2 explicit scoring rule** (execution sub-bar on first post-observation attempt; committed-LOS-escalation attempt = design signal/fail; no-escalation-but-incomplete = execution miss that does not consume the attempt, K=3 bound; trace logs residual + threshold state at each attempt start/end so carryover is recorded, not misattributed); **claim-1 ≥80% of capture events yield a specific true cause** (≤5 captures scored per tester, minimum viable N=5, INCONCLUSIVE below N); **claim-3 ≥60% unprompted use**, separate cohort (trace reveal is a contamination vector); geometry ACs → **four criteria** — (a) existence at **crouch + walk**, **timing-gated** (start-phase window), zero *committed* escalation with explicit tolerance, noise state verified to produce no re-covering Investigate; (b) universal; (c) split into **(c-1)** MVP trace-verify vs whatever look-around ships + **(c-2)** forward-design commitment re-verified at milestone-0/Vertical Slice; (d) no capture-immunity; **safe observation vantage** required; **guard residual reset to zero between verification cycles** (measures geometry, not accumulated wariness).

### User Design Decisions (2026-08-14, applied)
- **Verb-route rule**: **Relax to satisfiable form** — ≥1 verb-only route **per major area** (not per segment); verbs structurally load-bearing, never the only route in any crossing; pure timing remains possible **at base residual** (residual-wary guards tighten timing windows — readable, never a silent lockout); verb routes often open optional rewards; static cameras enforce "never clear by timing" chokes in guard-poor segments.
- **Grade legibility**: **Commit both** — grade card (meter trace + escalation events + penalty breakdown) **and** cross-run best record, both Target-tier HUD; MVP emits no S/A/B.
- **Content contingency**: **Segment-count dial** — segment count is the first scope dial; shrink toward 7 and stretch per-segment minutes (S-band floor protects the run target); cut segments before cutting AI depth.
- **Approval gate**: **Yes — conditional** — concept approval conditional on a **qa-lead-approved `design/qa/prototype-playtest-plan.md` before the engine milestone-0 build**; claim-1 rubric authored against the FSM contract (the spec), not reverse-engineered from the implemented trace.

### Recommended Revisions (applied)
- Residual decay rate pinned as a first-order tuning knob with a target scale (~45-60 s) rather than an unnamed constant.
- Pure-timing-at-base-residual qualification folded into the verb-route clause (timing remains possible at base residual; residual-wary guards tighten windows).
- Claim 2 execution-miss scoring rule with a K=3 attempt bound + residual carryover logging.

### Deferred to GDD Tiers (not concept edits — review #7 handoffs)
give-up-window suspend semantics → Chase/Give-up spec (ai); grade merge-to-max inversion → Systems GDD (game-designer E1); rate-formula `k/d` pinning + chase-speed ratio → Perception GDD; guard-personality framing → AI GDD; "alert" terminology (guard-alert vs facility-alert) → Perception GDD; escape-as-design → Level GDD; segment-boundary legibility → Level GDD; pure-timing-at-base-residual phrasing → Perception GDD.

### Status
Revisions applied (10-edit changeset, user-approved, 2026-08-14). Review #7 corrected the capture-metric regression introduced in review #6 and finally *verified* (not restated) the content-band arithmetic. **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) — user chose re-review. **BLOCKING prerequisite before milestone-0**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 6 | Recommended: 15+
Summary: Re-review confirmed **all 6 review-#7 blockers + 4 user decisions substantively resolved by all 6 specialist lenses** — capture metric, off-mesh immunity, residual floor, Chase OR-termination, content-band arithmetic (finally verified, not restated), grade legibility, and claim scoring. But the review-#7 changeset **introduced two new self-contradictions** (AC(a) "at every phase" vs the new start-phase window; claim-2 "committed LOS escalation" colliding with the grade's base-threshold "escalation event") and **one prior blocker survived in mutated form** (hide-spot live-fix lifecycle re-opened the off-mesh capture-immunity question B2 was built to close). Creative-director ruled **NEEDS REVISION (focused), not MAJOR** — the concept is structurally done; remaining defects are precision-level contract collisions, not design gaps — and **committed to APPROVED on the 5-edit changeset without another full 6-agent round**. User approved the changeset; all 5 edits applied this session. Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition, not a new blocker).
Prior verdict resolved: Partial — review #7's blockers confirmed resolved, but its changeset introduced AC(a) + claim-2 collisions and left the hide-spot fix lifecycle ambiguous.

### Blocking Items (all 5 resolved via approved 5-edit changeset this session)
1. **B1 — Hide-spot "live fix" lifecycle contradiction** [unity] [ai] — line 74 "fix cleared when the guard redirects to an LKP" collides with line 77 "live fix on the spot's front" when the spot IS the LKP: reading (a) re-breaks B2 (no catch at spot front), reading (b) catches an emptied spot. Fixed: fix persists through approach + spot-front verification; clears only on spot-verified-empty / give-up expiry / redirect to a *new* LKP; "never capture immunity" scoped to the AC(d) reach-and-verify envelope.
2. **B2 — AC(a) self-contradiction** [systems] [level] — "zero committed escalation AT EVERY PHASE" vs "timing-gated… start-phase window… outside which it accrues suspicion" cannot both hold; AC vacuous. Fixed: zero escalation *when transited within a start-phase window of the patrol cycle*; same transit outside the window accrues suspicion.
3. **B3 — Claim-2 fail bar vs grade "escalation event"** [game] [qa] — if the grade's base-threshold-only meaning leaks into claim-2, a residual-lowered Investigate (a real visual detection per the op-def) doesn't fail the sub-bar → claim 2 can pass while the player was detected. Fixed: claim-2 fail bar explicitly decoupled — any actual LOS-suspicion escalation to Investigate/Chase including residual-lowered entries, excluding cancelled peeks and noise/alert-only Investigates; independent of the grade's base-threshold accounting. (CD overruled the cancelled-peek sub-point: per the op-def a cancelled peek is not a visual detection, so both bars already agree on it.)
4. **B4 — Claim-1 statistics + reveal contamination** [qa] — "≥80% of capture events" with N counting testers can collapse to a 2-event verdict; captures 2–5 are training-contaminated by the reveal protocol. Fixed: scored captures = **first capture per tester only**; minimum viable sample = 5 first-captures; later captures recorded as secondary evidence.
5. **B5 — MVP timeline unsupported** [level] — line 249 "~2-3 weeks" exceeded by the enumerated MVP deliverables (4-6+ weeks for a solo first-project dev). Fixed: timeline flagged **~4-6 weeks + producer-revalidation note** (production estimate, not a creative defect).
6. **B6 — Conditional-approval gate (carried, NOT a new blocker)** [qa] — `design/qa/prototype-playtest-plan.md` still does not exist; it is the already-declared review-#7 approval condition and stands as the milestone-0 gate regardless of this round.

### Recommended Revisions (applied to GDD tiers — review #8 handoffs)
- **Perception GDD required content**: commit `Chase_threshold ≤ rate_max × max-achievable-continuous-LOS` (sole failure state reachable, not instant) [systems]; duration-cap must not terminate while the player is in continuous LOS + `guard-chase-speed > player-run-speed` (give-up-while-staring + re-Chase loop) [systems] [game R6].
- Grade: soften Pillar 3 "the grade penalizes compounded exposure" (event penalty is base-only; the exposure integral still catches residual-lowered detections) [game R1]; residual-lowered entry sustaining to Chase — event count or "never count" pin [systems]; camera exposure not a grade input [game R3]; multi-guard max-merge misprices simultaneous-crossing risk [game R4]; "major area" undefined [game R5].
- Unity pins: bounded `NavMesh.SamplePosition maxDistance`; catch distance in the XZ plane (3D fires through floors); backstop = hard AND-gate on the player's actual transform; hide detection gated by hide state not ray physics; perception tick ~10 Hz, ≤ ≥10 Hz AC sampling and < re-sight min-duration; `localAvoidanceRadius` vs catch range [unity].
- AC(d): bake alone doesn't verify no-stand-off — editor-time grid-sample check required [qa] [unity]; AC(b) restated as outcome not spatial proxy; ≥10 Hz anchored to confirm window [qa].
- Level: pressure-event density (dense end needs cameras to carry beats — dilutes Pillar 1); facility authoring size (~3.8-6.3 km path; "one level" anti-pillar caps count not size) [level].

### Status
Revisions applied (5-edit changeset, user-approved, 2026-08-14). **Pending re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) — creative-director committed to APPROVED on this changeset without another full 6-agent round. **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 13 | Recommended: 3
Summary: Full 6-agent panel confirmation re-review of review #8's 5-edit changeset. All 6 review-#8 blockers + 4 user decisions confirmed substantively resolved — but the creative-director's meta-finding was that review #8's **"focused confirmation shortcut"** (committed to APPROVED without a full panel) missed precision-level contract defects only the full panel surfaced: 13 blockers spanning non-deterministic Chase/termination contracts, self-contradictory grade/attribution invariants, and unfalsifiable geometry ACs. The panel also flagged a recurring **"restated-not-verified" arithmetic pattern** (three consecutive prior rounds) — fixed for the current instance. User adjudicated 4 design forks (immediate Chase entry on sight-of-entry into a hide spot; accumulator resets at segment transition; 7 segments as Target planning datum; full 6-agent panel for the confirmation round). All 13 resolved via a user-approved **5-edit / ~19-clause changeset**. Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition).
Prior verdict resolved: Partial — review #8's 5 blockers + 4 user decisions confirmed resolved, but its APPROVED commitment without a full panel missed 13 blockers; this round's full panel surfaced them.

### Blocking Items (all 13 resolved via approved 5-edit / ~19-clause changeset this session)
1. **Edit 1 — Chase & hide-spot contract** [ai] [unity] — (1a) **immediate Chase entry on personal sight of the player entering a hide spot** (LKP = the spot, live fix on its front) — the mechanism that makes a hide spot never capture immunity; (1b) **termination carve-out** — a valid re-sight is re-acquisition of sustained LOS after a break (maintained sight is a continuation); the give-up window suspends while a valid re-sight is maintained, and the duration/re-sight caps never terminate a chase during sustained continuous LOS (no give-up-while-staring / instant re-Chase loop); (1c) **spot-front resolution** — "spot empty / no LOS" applies only to non-hide-spot LKPs; "confirm empty" = the spot-front reach check completes with the player not present, never inferred from "no LOS"; (1d) **catch metric pinned** — live transform in the XZ plane + vertical-delta tolerance, never the sampled fix, never 3D straight-line; `NavMesh.SamplePosition` gains a bounded `maxDistance`.
2. **Edit 2 — Grade & attribution invariants** [systems] [game] [qa] — (2a) `forgiveness_floor ≤ 0.2 × T_base` (no committed escalation, base or residual-lowered, contributes zero to both grade inputs); (2b) **accumulator resets at segment transition** (carried-in exposure does not penalize the new segment); (2c) safe observation vantage = **zero LOS exposure and zero residual accrual**, and AC(a)/(b) verified at the residual level the first claim-2 crossing faces; (2d) **residual resets to zero at the segment-start respawn** (retry practices the same schedule with no carried wariness).
3. **Edit 3 — Falsifiability & verification** [qa] [level] [systems] — (3a) AC(d) verified by an **editor-time grid-sample sweep** within a committed "certified-route vicinity" radius (a bake alone proves walkability, not the absence of off-mesh stand-off pockets); (3b) **"major area" defined** (contiguous group of 1-2 segments sharing a functional identity, delimited by a major sightline choke — enumerated in the Level GDD); (3c) **≥3 encounter types enumerated** (guard-timing crossing, camera-disable choke, burst-distract investigation arc, hide-from-investigation); (3d) content-band 9-row capped at **3.6-4.6** (4.7 × 9 = 42.3 exceeded the S ≤ 42 invariant); (3e) AC(b) restated **outcome-based** (the transit fails) + AC(c-2) gains a stated failure consequence (re-author the route / re-tune the arc; the AC gates that tier).
4. **Edit 4 — MVP-hypothesis policy honesty** [qa] [game] — (4a) **"or HUD hint" removed** — a placed pick-up alone guarantees discoverability (consistent with the no-full-HUD MVP exclusion); claim-3 denominator defined ("presented the tool" = the tester reached the pick-up; testers who never find it are excluded, not counted against the bar); (4b) the playtest plan must additionally specify: claim-2 comprehension rubric, scenario ordering so the claim-1 reveal cannot contaminate claim-2, operational attempt boundaries, colloquial→trace mapping for the claim-1 rubric, and a **no-`Debug.Log` trace flush**.
5. **Edit 5 — Scope & priority reconciliation** [level] [producer] — (5a) **Target planning datum = 7 segments (8-9 stretch)** + producer-revalidation before sprint planning + per-segment geometry budgets committed in the Level GDD (7-9 segments + all Target systems exceed a first-project 10-12 week window at MVP-level velocity); (5b) static cameras carry a **bounded minority of pressure events** (≤1 camera beat per segment, no consecutive camera-only segments) — guard-driven beats stay dominant (Pillar 1).

### User Design Decisions (2026-08-14, applied)
- **Immediate Chase entry**: the guard who personally sees the player enter a hide spot enters Chase immediately (LKP = the spot, live fix on the spot front) — honors "never capture immunity" without breaking the chase state machine.
- **Accumulator resets at segment transition**: carried-in exposure is penalized where it accrued, then cleared — the new segment starts clean.
- **Target datum**: 7 segments as the planning datum (8-9 stretch); producer revalidates before sprint planning.
- **Confirmation panel**: the confirmation re-review after this changeset runs the **full 6-agent panel** (not review #8's focused shortcut) — the source of this round's 13 blockers.

### Recommended Revisions (deferred to GDD tiers — review #9 handoffs)
- Level GDD: major-area enumeration (3b), encounter-type enumeration + Vertical-Slice demonstration (3c), per-segment geometry budgets (5a).
- Producer: revalidate the Target timeline before sprint planning (5a).
- QA: the playtest-plan additions from 4b (claim-2 rubric, ordering, attempt boundaries, colloquial→trace mapping, no-`Debug.Log` flush) are committed requirements for `design/qa/prototype-playtest-plan.md`.

### Status
Revisions applied (5-edit / ~19-clause changeset, user-approved, 2026-08-14). Review #9 = full 6-agent panel confirmation — 13 blockers resolved, review #8's "focused confirmation shortcut" corrected by the panel. **Pending confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (user decision). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 5 | Recommended: 3
Summary: Full 6-agent panel confirmation re-review after review #9's 5-edit / ~19-clause changeset. All 13 review-#9 blockers + 4 user decisions confirmed genuinely resolved (content-band arithmetic finally verifies by hand; all 6 lenses agree). But the panel surfaced **5 NEW blocking contract collisions introduced/sharppened by the review-#9 changeset**: B1 (the primary accumulator-crossing Chase path never sets a live player fix → a deadlocked, uncatchable chase), B2 (the direct noise/alert spot-investigation catch contradicted the Chase-only capture rule → 3-way FSM contradiction), B3 (a hide-spot Chase terminable by the give-up window before spot verification → distant-spot capture immunity), B4 (the direct-path hide-entry Chase produced no grade input → falsifies the S-claim), B5 (claim 2 unfalsifiable at concept tier). Creative-director adjudicated three disagreements (unity no-BLOCKING vs ai/systems/game/qa BLOCKING on B1-B4 → real, test = "can a programmer implement without guessing"; confirm-window aliasing → event-driven gizmo, no concept edit; direct-investigation catch → explicit carve-out, no Chase promotion). User chose **"Revise now"** + all 4 design forks with the recommended options (B2 = carve-out-no-Chase spot-front capture; D2 = mechanical residual reset at any segment transition; B5 = ≥60% both sub-bars, N≥5, INCONCLUSIVE below; D4 = threshold-sink marker MVP-visible). All resolved via a user-approved **11-edit surgical changeset** (blocking fixes only, zero new content) plus CD rulings D2/D3/D4, the R4 guard-chase-speed invariant, and the R1 content-band honesty fix. Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition).
Prior verdict resolved: Yes — review #9's 13 blockers + 4 user decisions confirmed resolved; this round's 5 blockers (introduced by the #9 changeset) resolved in-session.

### Blocking Items (all 5 resolved via approved 11-edit changeset this session)
1. **B1 — Primary Chase path never set a live fix** [ai] — the accumulator-crossing Chase path (guard pursuing a visible player via sustained LOS) set no live player fix, so the catch check never ran → a guard could chase forever without ever capturing. **Fix (Edit 1)**: the fix-set now includes "Chase entry with sustained LOS on the player" — safe because the catch metric is the player's live transform, never the sampled fix.
2. **B2 — Direct spot-investigation catch contradicted the Chase-only capture rule** [ai] [game] [systems] — the spot-front catch during a noise/alert-directed spot investigation was a 3-way FSM contradiction (capture runs within Chase vs only-the-detector-chases vs the catch fires at a spot front). **Fix (Edit 3, Resolve A + user decision)**: explicit carve-out — a guard who confirms the player at a hide spot front captures them there **without promoting to Chase** (a physical verification, not a pursuit), preserving the single direct Chase path and the alert-derived-never-chases rule.
3. **B3 — Hide-spot Chase terminable by give-up window before spot verification** [ai] [systems] — the chase give-up window could expire mid-approach to a distant hide-spot LKP, clearing the fix before the spot-front catch check ran → distant-spot capture immunity. **Fix (Edit 2)**: for a hide-spot LKP, the give-up window and chase caps are **suspended through the approach and spot-front verification**; those terminations apply only to non-hide-spot LKPs.
4. **B4 — Direct-path hide-entry Chase produced no grade input** [systems] — a direct-path hide-entry Chase is not "fresh LOS exposure crossing a committed BASE threshold," so the harshest in-world outcome could grade clean → falsifies the S-claim. **Fix (Edit 4)**: the direct-path hide-entry Chase counts as **one Chase escalation event** (unless already counted via a threshold-crossing Chase in the same episode).
5. **B5 — Claim 2 unfalsifiable at concept tier** [qa] — no aggregate pass rule for the claim-2 comprehension + execution sub-bars. **Fix (Edits 5 + 6)**: claim 2 passes if **≥60% of testers pass BOTH sub-bars** (comprehension per the plan's rubric, execution per the scoring rule above); minimum viable sample = 5; INCONCLUSIVE below; pass rule added to the playtest-plan requirement list.

### User Design Decisions (2026-08-14, applied)
- **B2 carve-out-no-Chase**: a noise/alert-directed guard who confirms the player at a spot front captures there without promoting to Chase — the spot-front catch is a physical verification, not a pursuit.
- **D2 mechanical reset**: the residual resets to zero at **any segment transition** (successful clear *and* respawn), mirroring the accumulator's segment-transition reset.
- **B5 ≥60% both sub-bars**: claim 2 passes if ≥60% of testers pass BOTH sub-bars; N≥5; INCONCLUSIVE below.
- **D4 threshold-sink MVP-visible**: the residual threshold-sink marker ships as a meter element in the MVP tester build (not the Target-tier attribution HUD).

### Recommended Revisions (deferred to GDD tiers — review #10 handoffs)
- **R1 [systems] [game]** — content-band honesty (folded into the changeset, Edit 11): "closes to" → "approximates", exact closure S ∈ [32.1, 41.4] noted, quicker-player figure scoped to the band's lower end; tighter band + escape-fraction calibration pinned in the Level GDD.
- **R2 [level]** — AC(d) "certified-route vicinity" radius `[X] m` placeholder + hide-spot depth constraint → Level GDD.
- **R3 [level]** — Vertical Slice's "demonstrates camera-disable choke" tier collision (VS ships no cameras) → Level GDD / scope wording.
- **R4 [unity]** — guard-chase-speed > player-run-speed (folded into the changeset, Edit 10): hard invariant of the catch contract; the speed ratio is a Perception-GDD tuning knob.
- **R5 [perception]** — confirm-window ≥ 2× AI-tick pin → Perception GDD.
- **R6 [perception] [game]** — sight-of-entry operational timing + telegraph → Perception GDD (Pillar 4).

### Status
Revisions applied (11-edit surgical changeset, user-approved, 2026-08-14 — blocking fixes only, zero new content). Review #10 = full 6-agent panel confirmation of review #9's changeset — all 13 #9 blockers + 4 user decisions confirmed resolved; 5 new blockers (introduced by the #9 changeset) resolved in-session. **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 4 | Recommended: 3
Summary: Full 6-agent panel confirmation re-review after review #10's 11-edit surgical changeset. All 5 review-#10 blockers + 4 user decisions confirmed genuinely resolved by all 6 lenses (formulas, content band, and claim testability all verified; the APPROVED lenses verified everything they tested — the strongest signal the structure has converged). The panel surfaced **4 NEW blockers introduced by the review-#10 changeset**, all one-clause contract pins violating the document's own committed claims, ruled BLOCKING by the creative-director after a 3-3 lens split (unity/game/ai NEEDS REVISION vs systems/level/qa APPROVED): B1 (catch-closure lag budget — the 2-5 Hz-sampled chase target trails a full-speed runner by `V_run × T_sample ≈ 1.0-3.6 m`, so guard-chase-speed > player-run-speed alone cannot guarantee catchability), B2 (the carve-out spot-front capture produces zero grade input → a camera-tripped hiding player can grade S), B3 (the carve-out capture reads as random — no telegraph), B4 (an unreachable hide-spot LKP hangs the chase forever since give-up + caps suspend through spot-front verification → AC(d) bound vacuous). User chose **"Revise now"** and the **closure inequality pin** for B1. All resolved via a user-approved **8-edit / 9-operation surgical changeset** (blocking fixes only, zero new content). Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition).
Prior verdict resolved: Yes — review #10's 5 blockers + 4 user decisions confirmed resolved; this round's 4 blockers (introduced by the #10 changeset) resolved in-session.

### Blocking Items (all 4 resolved via approved round-11 changeset this session)
1. **B1 — Catch-closure lag budget** [unity] (ai RECOMMENDED → CD ruled BLOCKING) — the chase target is the 2-5 Hz-sampled NavMesh feet position while the catch metric is the live transform, so a strictly-faster guard trails a runner by `V_run × T_sample ≈ 1.0-3.6 m`; `guard-chase-speed > player-run-speed` alone cannot guarantee catchability → a full-speed runner is permanently uncatchable (the exact kiting the speed invariant exists to prevent). **Fix (user chose closure-inequality pin)**: `catch range > V_run × T_sample + hysteresis + guard/player capsule radii + stopping distance` committed as a **hard invariant of the catch contract**; `V_run` / `T_sample` named Perception-GDD tuning knobs.
2. **B2 — Carve-out grades clean** [game] → CD ruled REAL — the B2 carve-out capture (noise/alert-directed guard confirms the player at a hide spot front, no Chase promotion) produces zero grade input: no BASE-threshold LOS crossing, no Chase escalation, zero exposure integral while hidden → a camera-tripped hiding player can grade S. **Fix (Edit 5)**: the carve-out spot-front capture **counts as one Chase escalation event**, attributed to the segment where the spot-front confirmation completes — the harshest in-world outcome never grades clean through either route.
3. **B3 — Carve-out legibility** [game] → CD ruled REAL — no telegraph that a guard is committed to verifying a specific spot → the carve-out capture reads as random (Pillar 2/4 breach). **Fix (Edit 2)**: the capture is **telegraphed before it resolves** (commit-to-verify-this-spot = approach-intent/alert telegraph under Pillar 4, mechanism pinned in the Perception GDD) and **listed on the grade card** as an escalation event (grade-legibility commitment).
4. **B4 — Hide-spot reachability / AC(d) vacuity** [ai] + qa handoff-note → CD ruled BLOCKING (concept tier; Target-tier impact) — B3 suspends give-up + caps for hide-spot LKPs, so an unreachable hide-spot LKP hangs the chase forever, and AC(d)'s bound (the suspended window) is vacuous. **Fix (Edits 3 + 6)**: **guard-reachability authoring pin** (Level-GDD) — every hide spot in the certified-route vicinity has a NavMesh path from guard to spot front, so a hide-spot-LKP chase always resolves at the spot (catch or confirm-empty); the AC(d) sweep now asserts it.

### Recommended Revisions (folded into the round-11 changeset)
- **R1 [game] [level]** — session-length honesty: quicker-player figure ~35-40 min, not sub-baseline ~30 (the first-run-traversal definition provides no sub-baseline); Session Length row + pacing line corrected.
- **R2 [level]** — camera-disable choke demonstrated at the Target tier (the Vertical Slice ships no cameras); the VS demonstrates the camera-free types.
- **R3 [qa]** — "no live attribution (dev gizmos only)" clarified: dev-gizmo **verification only** — the tester run itself shows none.

### Deferred to GDD tiers (review #11 handoffs, not concept edits)
- **Perception GDD**: catch-closure values (`V_run`, `T_sample`, catch-range invariant), NavMesh.SamplePosition failure fallback, bounded confirm-empty duration, |ΔY| ledge gate, confirm-window ≥ 2× AI tick, sight-of-entry + carve-out (approach-intent) telegraphs, guard-chase-speed ratio.
- **Systems GDD**: hide-entry Chase event-count precedence, `d ≥ ε` IEEE clamp, residual decay snap, grade merge/max algorithm.
- **Level GDD**: safe-vantage blind wedge, AC(d) radius value + hide-spot guard-reachability, per-segment budgets, major-area enumeration.
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`): K=3 attempt semantics, claim-3 "reached" vs "found" denominator, claim-1 first-capture recruitment, debugger/dump telemetry mitigation.

### Status
Revisions applied (round-11 changeset, user-approved, 2026-08-14 — blocking fixes only, zero new content). Review #11 = full 6-agent panel confirmation of review #10's changeset — all 5 #10 blockers + 4 user decisions confirmed resolved; 4 new blockers (introduced by the #10 changeset) resolved in-session. **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-14 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 1 | Recommended: 15+
Summary: Final confirmation re-review of the round-11 changeset with the full 6-agent panel. All 4 review-#11 blockers + the round-11 changeset confirmed **genuinely resolved by all 6 lenses** — the strongest convergence signal in twelve rounds (5 of 6 lenses independently verified the content-band arithmetic, the catch-closure invariant, and claim testability; game/systems/level/unity APPROVE-ready, qa-lead CONDITIONAL APPROVE). The panel surfaced **1 NEW BLOCKING contract defect at the ai lens**: the catch metric (live-transform XZ + |ΔY| ≤ 1.0-1.5 m, no reachability component — the Euclidean backstop is the same test) permits a catch to fire **through thin geometry** while the guard is mid-approach to a stale LKP, violating the doc's own committed claim and Pillar 2. Creative-director adjudicated the ai-programmer [BLOCKING] vs unity-specialist [NICE-TO-HAVE] dispute → **BLOCKING (ai)**, resolved with a one-clause reachability gate. User chose **"Revise now"**. All resolved via a user-approved **6-edit surgical changeset** (blocking fixes + precision hardenings only, zero new content). Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist).
Prior verdict resolved: Yes — review #11's 4 blockers confirmed genuinely resolved; 1 new blocker (through-wall catch — a latent contract defect surfaced by the round-12 panel, not a changeset regression) resolved in-session.

### Blocking Items (all resolved via approved round-12 changeset this session)
1. **B1 — Catch fires through thin geometry mid-approach to a stale LKP** [ai-programmer, CD-adjudicated BLOCKING] — the catch metric is pure live-transform XZ + |ΔY| ≤ 1.0-1.5 m with no reachability/occlusion component (the Euclidean backstop is the same XZ+ΔY test); the live fix persists through the LKP-approach leg (cleared only on arrival-with-no-LOS), so a guard whose path runs alongside a thin wall with the player on the other side fires a catch **through the wall** — a false positive violating the doc's committed claim "a catch can never fire when the guard is traveling to a stale last-known-position" and Pillar 2 (a catch that reads as random is a bug). **Fix (Edits 1-3)**: the catch check now requires the **reachability gate** — navmesh path distance from guard to a point near the player's *current reachable* position (never the sampled fix/destination/stale LKP) ≤ catch range + margin; the committed claim is qualified ("…unless the player is physically within reach along the navmesh path"); the **Euclidean backstop inherits the gate**. Proximity-only capture is preserved (no LOS component), and the hide-spot approach catch still fires because the guard is on a path to near the spot front (guard-reachability pin). This resolves both the ai false-positive concern and unity's literal-reading concern (no blanket no-catch-while-traveling rule).

### Precision hardenings folded into the round-12 changeset
1. **`T_sample` mislabel** [systems F1, CD: fold in] — redefined as the maximum sample **period** (`1/cadence`, 0.2-0.5 s at the 2-5 Hz cadence) so `V_run × T_sample` is dimensionally meters; a programmer reading "cadence" undersizes the catch range ~2.5× and silently resurrects the round-11 kiting bug (Edit 4).
2. **Residual reset-vs-inert clause collision** [systems F5, CD: fold in] — "resets at any segment transition" flatly collided with "inert during Chase" when a Chase crosses a boundary; a Chase in progress is now exempt from the transition reset, deferred to the chase's resolution (Edit 5).
3. **AC(a) "(one full period)" vacuous reading** [level F2, CD: harden] — the window is now a **strict non-empty subset of one full patrol period** (the period, including scan sweeps, bounds it); a start phase outside the window always exists, so the AC stays timing-gated (Edit 6).

### Recommended Revisions (deferred to GDD tiers — review #12 handoffs)
- **Perception GDD**: `T_sample` period pin (0.2-0.5 s) with the worked-range check; catch-closure defaults at the tighter end of the invariant (~3 m at 5 Hz, not ~5 m at 2 Hz — the 2 Hz end reads as a generous grab); slope-aware |ΔY| tolerance (a 30° slope at 5 m rises ~2.9 m — 3× the tolerance; the reach collapses on steep terrain); chase-vs-noise (a chasing guard ignores noise events; the chase terminations are exclusive); "hide-spot LKP" operational definition (positional vs informational + proximity clause); catch-timer accounting (active-time with a cumulative pathPending cap); confirm-empty bounded duration; residual decay during Chase; reachability-gate values.
- **Systems GDD**: cross-guard incident event-count merge — two converging guards at one spot-verification (guard A's Chase event + guard B's carve-out event) must emit **one** Chase escalation event per incident (game R2 / systems F4); reachability-gate + |ΔY| ledge-gate values.
- **Level GDD**: AC(d) guard-reachability sweep **re-scheduled to the first hide-spot build (Vertical Slice)** — the MVP bakes no hide spots (line 243), so the milestone-0 sweep is vacuous (level F3); AC(d) grid-resolution + geometric-vs-behavioral semantics (qa R-1); "hide-from-investigation" demonstration moved to **Target tier** (VS ships no hide-spot zones — the sibling gap to round-11 R2) (level F4); per-segment pressure-event counts (a 3.6-min segment can hold exactly 1 event — cadence denominator unpinned) + guard-presence budgets for guard-poor major areas (level F1/F5); difficulty-curve reframe ("authored escalation; the player's mastery is what makes it feel fair") (game R6); camera-tripping cost rule (no zero-cost free Lure — game R4).
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`): claim-2 valid-attempt **residual ≈ 0 at start** (qa R-2); claim-1 contributing-vs-triggering cause definition (qa N-2); plus the already-committed requirements (claim-2 comprehension rubric, scenario ordering, operational attempt boundaries, colloquial→trace mapping, no-`Debug.Log` flush).

### Nice-to-Have (carried to GDD tiers)
- Confirm-empty duration: state instant-on-arrival or pin a bounded verify duration (systems F3).
- Ring-buffer allocation discipline: preallocated struct array, zero per-frame allocation (WebGL non-moving GC hitch) (unity).
- WebGL clipboard flush needs a user-gesture context + small bounded ring + jslib `navigator.clipboard.writeText` fallback; "no runtime file system" is not literally true (IndexedDB exists) (unity / qa N-3).
- Chaser `ObstacleAvoidanceType.NoObstacleAvoidance` consideration when 2 guards converge on a spot front (unity).
- Residual decay vs segment pacing cool-down trade ("peek, wait out the wariness, peek again") (game N1).
- AC(d) bound should reference the catch+backstop envelope (not the suspended give-up window) for sustained-LOS stand-offs (systems F6).
- Runtime hang-guard for a guard whose hide-spot path fails at runtime (permanent pathPending) (systems F7).
- Off-mesh players caught at larger distance (backstop margin) — pin the margin ≤ hysteresis (systems F8).
- Capture op-def doesn't cover the carve-out's terminality ("Capture = guard reaches Chase…"); residual decay during Chase unspecified; Chase guard receiving a peer alert unaddressed (ai N1/N2/N3).
- WebGL draw-call/vertex budget pin for the portfolio demo (unity).

### Status
Revisions applied (round-12 changeset, user-approved, 2026-08-14 — blocking fixes + precision hardenings only, zero new content). Review #12 = full 6-agent panel confirmation of the round-11 changeset — all 4 #11 blockers confirmed resolved; 1 new blocker (through-wall catch) + 3 precision hardenings resolved in-session. **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

## Review — 2026-08-14 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 4 | Recommended: 15+ (GDD-tier handoffs)
Summary: Final confirmation re-review of the round-12 changeset with the full 6-agent panel. All 4 review-#12 blockers + the round-12 changeset confirmed genuinely resolved by 5 of 6 lenses (game/systems/level/unity APPROVE-ready, ai focused). The panel surfaced **4 new concept-tier blockers** adjudicated by creative-director as all resolvable via a focused changeset: **CT-1** [game/level/ai] — the round-12 reachability gate tested only the player's *reachable position*, not the guard's *navigation goal* → the through-wall catch re-opens on a passing-by/passing-away approach to a stale LKP; **CT-2** [systems/qa] — AC(a) vacuous as verified (no negative sample → an always-clear route passes); **CT-3** [qa] — AC(d) contradiction (hide-spot evasion bound vs. suspended give-up window); **CT-4** [qa] — backstop off-mesh self-contradiction ("catch fires if within reach" vs. "give-up window, never off-mesh immunity"). Plus the **camera-choke dispute** [level vs. game guarantee] adjudicated as a sanctioned exception. User chose **"Revise now"**; the single design fork (nav-goal gate vs. retained "physically within reach" qualifier) was grouped into one question → **nav-goal gate**. All resolved via a user-approved **6-edit surgical changeset** (blocking fixes only, zero new content). Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-13 changeset for that re-review.
Prior verdict resolved: Yes — review #12's 4 blockers confirmed genuinely resolved; 4 new blockers + 1 dispute (latent contract defects surfaced by the round-13 panel, not changeset regressions) resolved in-session.

### Blocking Items (all resolved via approved round-13 changeset this session)
1. **CT-1 — Through-wall catch re-opens: reachability gate doesn't test the guard's navigation goal** [game-designer, level-designer, ai-programmer; CD adjudicated] — the round-12 gate verified the player's *current reachable* position was within path distance, but not that the guard was *traveling toward* the player; a guard mid-approach to a stale LKP whose route passes near thin cover could still fire the catch through a wall (passing-by/passing-away), re-opening the review-#12 B1 false positive. **Fix (Edits 1-2)**: the catch check now requires the **nav-goal gate** — the guard's *current navigation goal* is the player's vicinity (live pursuit — Chase with sustained LOS, or a valid re-sight) or the spot front (a hide-spot fix the player is believed to occupy); never active during travel to a stale LKP with no player-ward goal — **AND** the **path-arrival metric** — navmesh path distance from guard to the player's *current reachable* position ≤ catch range + margin, so the catch **cannot resolve before the path arrives**; the live-transform Euclidean XZ + |ΔY| is demoted to the off-mesh/backstop fallback. The **margin is committed ≤ hysteresis** (closed the unpinned-margin knob). Proximity-only capture preserved (no LOS component); hide-spot approach catch still fires (guard's nav goal *is* the spot front). This was the only genuine design fork — the user chose the nav-goal gate over the retained "physically within reach" qualifier.
2. **CT-2 — AC(a) vacuous as verified: no negative sample** [systems-designer, qa-lead] — the window/outside-window contrast was asserted but never verified; an always-clear route passes the AC as verified and the "correct crossing plan" comprehension becomes vacuous. **Fix (Edit 4)**: AC(a) verification must include **≥1 sampled start phase outside the window that accrues committed escalation at base residual** (the negative sample).
3. **CT-3 — AC(d) contradiction: hide-spot evasion bound vs. suspended give-up window** [qa-lead] — AC(d) asserts no sampled position evades longer than the give-up window, but hide-spot LKPs suspend the give-up window through spot-front verification by design — the bound is unsatisfiable for hide spots. **Fix (Edit 5)**: the sweep assertion is **scoped to non-hide-spot positions**; hide spots are governed by the guard-reachability assertion + spot-front verification completing.
4. **CT-4 — Backstop off-mesh self-contradiction** [qa-lead] — "capture still fires if the guard is genuinely within reach" (fallback) vs. "the chase give-up window is reserved for the legitimate eluded case, never off-mesh immunity" (same paragraph, off-mesh context) — no defined behavior for off-mesh. **Fix (Edit 3)**: **"genuinely within reach" reconciled across both checks** — for on-mesh/shallow off-mesh (a reachable sample exists within the bounded `maxDistance`) the backstop's Euclidean test is the same reach the path-arrival metric gates; for **deep off-mesh** (no reachable sample) the catch contract does not apply, governed by the committed deep-off-mesh definition + AC(d)'s authoring rule (no player-reachable deep off-mesh stand-off within the certified-route vicinity) — no off-mesh catch immunity.
5. **Camera-choke dispute (sanctioned exception)** [level-designer vs. game guarantee] — static-camera "never clear by timing" chokes contradicted "pure timing remains a possible crossing at base residual". **Fix (Edit 6)**: the guarantee now carries **one sanctioned exception** — static cameras enforce "never clear by timing" chokes in guard-poor segments, **bounded at ≤1 camera beat per segment and no consecutive camera-only segments** (per the committed camera budget) — acknowledged, never silent.

### Recommended Revisions (deferred to GDD tiers — round-13 handoffs)
- **Perception GDD**: nav-goal gate operationalization (live-pursuit nav-goal definition for Chase-with-sustained-LOS vs. re-sight vs. stale-LKP); path-arrival metric values (catch range + margin, margin ≤ hysteresis committed — set the absolute margin); reachability-gate + |ΔY| ledge-gate values; the round-12 handoffs remain (T_sample period pin, catch-closure defaults, slope-aware tolerance, chase-vs-noise, hide-spot-LKP op-def, catch-timer accounting, confirm-empty bounded duration, residual decay during Chase).
- **Level GDD**: the camera-choke exception's ≤1-beat / no-consecutive bound feeds the camera budget (already committed — now explicitly the pure-timing guarantee's boundary); AC(d) guard-reachability sweep re-scheduled to the first hide-spot build (Vertical Slice) — the MVP bakes no hide spots, so the milestone-0 sweep is vacuous (round-12 level F3 carried).
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`): AC(a) negative-sample protocol (≥1 out-of-window start phase at base residual) joins the committed requirements.

### Nice-to-Have (carried to GDD tiers)
- Carried from review #12 (unchanged): confirm-empty duration pin; ring-buffer allocation discipline; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance on convergent spot-front checks; residual-decay vs. segment-pacing cool-down trade; AC(d) bound referencing the catch+backstop envelope; runtime hang-guard for failed hide-spot paths; off-mesh backstop margin ≤ hysteresis (now committed at concept tier); carve-out terminality op-def; Chase guard receiving a peer alert; WebGL draw-call/vertex budget pin.

### Status
Revisions applied (round-13 changeset, user-approved, 2026-08-14 — blocking fixes only, zero new content). Review #13 = full 6-agent panel confirmation of the round-12 changeset — all 4 #12 blockers confirmed resolved; 4 new concept-tier blockers + 1 dispute (nav-goal gate, AC(a) negative sample, AC(d) non-hide-spot scope, backstop deep off-mesh consistency, camera-choke exception) resolved in-session via a 6-edit surgical changeset. **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

## Review — 2026-08-15 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 13 | Recommended: 15+ (GDD-tier handoffs, carried)
Summary: Final confirmation re-review of the round-13 changeset with the full 6-agent panel. game-designer issued a CONDITIONAL APPROVE ("converged"); the other 5 lenses surfaced 13 blockers — creative-director ruled the majority wins: "converged" ≠ "contract-complete", and the milestone-0 build + qa plan cannot be authored against an undefined "spot front" or an unfalsifiable AC(d). All 13 adjudicated as resolvable in-session and resolved via a user-approved **13-clause surgical changeset** (blocking fixes only, zero new content): catch-contract cluster (spot-front definition, backstop-depth pin, FSM-owned `GuardGoalMode` nav-goal gate, PathPartial scoring, hysteresis-as-meters), AC cluster (AC(d) geometric restatement + committed sweep inputs, hide-spot reschedule to the first hide-spot build, AC(c-2) outcome-based, AC(a) window ≤ ½ patrol period, VS/tier scope carry), grade cross-guard merge, camera cluster (guard-poor definition + ≤2 camera-only of 7–9, residual-warity consequence for fruitless camera trips), Chase-ignores-peer-alerts, claim-2 residual-floor scoring. Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-14 changeset for that re-review.
Prior verdict resolved: Yes — review #13's 4 blockers + dispute confirmed genuinely resolved by 5 of 6 lenses; the 13 blockers are latent contract defects surfaced by the round-14 panel, not changeset regressions.

### Blocking Items (all resolved via approved round-14 changeset this session)
1. **"Spot front" undefined** [ai B1] — the catch contract's authoring target had no definition (where does the guard halt? what geometry verifies reach?). **Fix (Edit 1)**: spot front = the navmesh-sampled point nearest the hide spot's entry/cover boundary where the guard halts to verify — geometry enumerated in the Level GDD.
2. **Backstop false-confirm-empty** [ai B2; unity B1] — a guard at the spot front could resolve the reach check against a player inside an over-deep spot as "empty". **Fix (Edit 2)**: backstop-depth pin — each hide spot's interior depth ≤ catch range + margin (Level-GDD authoring pin), so false "confirm empty" cannot occur.
3. **Engine-keyed nav-goal gate re-opens the through-wall catch** [ai B2, unity B1] — a `NavMeshAgent` engine query (`destination`/`pathEndPosition`) does not change when a fix goes stale, so an engine-keyed gate reproduces the review-#12/#13 false positive. **Fix (Edit 3)**: the gate reads a named FSM-owned goal-mode variable (`GuardGoalMode { LivePursuit, HideSpotFront, StaleLKP, None }`), never an engine query.
4. **PathPartial + backstop-margin underspecified** [qa sweep, unity B2] — the shallow-off-mesh "capture still fires" claim needs a scoring rule when the sampled target is unreachable; hysteresis's unit was open. **Fix (Edits 4a–4c)**: a partial path scores by cumulative corner distance to the closest reachable corner (≤ catch range + margin, backstop ANDed on top); CT-4 "same reach" → "same bound"; hysteresis declared a distance in meters (the closure invariant adds it to a pure distance sum).
5. **AC(d) unfalsifiable** [qa B1] — the evasion bound referenced the suspended give-up window, which does not bound sustained-LOS stand-offs by design. **Fix (Edit 5)**: restated **geometrically** — per sampled player-reachable position, navmesh path distance from a reachable guard is within the catch+backstop envelope (the reference bound); committed sweep inputs (radius value, grid spacing ≤ ½ catch range, bounded `NavMesh.SamplePosition maxDistance`) asserted by the milestone-0 gate; shallow off-mesh added to the sweep domain.
6. **AC(d) hide-spot vacuity at milestone-0** [qa B2] — the bake ships no hide spots, so the guard-reachability assertion is vacuous there. **Fix (Edit 5b)**: hide-spot assertions rescheduled to the **first hide-spot build** (Target tier), explicitly vacuous at milestone-0, re-verified at that tier.
7. **AC(c-2) spatial proxy** [qa B3] — "must not re-cover the Burst-only route's sightline" is a spatial proxy, not the outcome. **Fix (Edit 6)**: outcome-based — any full Burst transit **with a noise event** completes with no committed LOS escalation, trace-verified; failure consequence kept.
8. **AC(a) near-vacuous window** [qa B4] — a near-full-period window is nearly always-clear. **Fix (Edit 7)**: window = strict non-empty subset **≤ half of one full patrol period**.
9. **VS/tier scope contradiction** [level B2/B3] — the VS claim listed camera-free types but hide-from-investigation needs hide-spot zones and the camera-disable choke needs cameras; hide-spot scope missing from the Target row. **Fix (Edit 8)**: VS demonstrates only VS-shipping types (guard-timing + burst-distract); hide-from-investigation + camera-disable demonstrated at Target; **hide-spot zones added to the Target row** (first hide-spot build).
10. **Cross-guard double-count** [systems B1] — two guards converging on one spot-verification (hide-entry Chase + carve-out) would grade as 2 Chase escalation events. **Fix (Edit 9)**: one spot-verification emits **one Chase escalation event total**, attributed to the confirmation segment.
11. **Camera-choke scope** [level B1; game R-4 elevated] — "guard-poor segment" undefined; no facility-wide cap on camera-only segments. **Fix (Edit 10)**: guard-poor = **≤1 guard-driven pressure beat per patrol cycle** (Level-GDD enumerated); facility-wide cap **≤2 camera-only segments of 7–9** (strict minority — Pillar 1).
12. **Consequence-free camera trips** [game R-4, elevated to blocking] — a camera trip was a free, unlimited, consequence-free distraction (anti-puppeting contradiction). **Fix (Edit 11)**: a camera alert resolving without LOS exposure accrues a **bounded share of residual wariness** (mirroring fruitless noise investigations) — never grade-penalizing a clean crossing.
13. **Chase receives peer alerts** [ai B3] — an alert could escalate or redirect an active pursuit. **Fix (Edit 12)**: a guard in Chase **ignores peer alerts entirely** — alert redirection applies to Patrol/Investigating guards only.
14. **Claim-2 residual carryover** [game R-5] — an execution attempt could inherit carried wariness and mis-measure geometry + plan. **Fix (Edit 13)**: a scored execution attempt must start at **residual ≈ 0** (committed floor); above-floor attempts are unscored (secondary evidence) and do not consume the K=3 bound.

### Recommended Revisions (deferred to GDD tiers — round-14 handoffs)
- **Level GDD**: spot-front geometry enumeration (entry/cover boundary → navmesh-sampled point); backstop-depth pin authoring (interior depth ≤ catch range + margin); guard-poor segment enumeration (≤1 guard-driven pressure beat per patrol cycle); certified-route vicinity radius value; camera budget + ≤2-camera-only facility cap geometry.
- **Perception GDD**: `GuardGoalMode` variable spec (ownership, set/clear sites, transitions); PathPartial scoring values (corner-distance bound); hysteresis absolute meter value; residual-floor value for the claim-2 "≈ 0" bound; camera-trip residual-wariness share; round-13 handoffs carried (nav-goal gate operationalization, path-arrival metric values, reachability-gate + |ΔY| ledge-gate values, T_sample period pin, catch-closure defaults, etc.).
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`): AC(d) committed sweep inputs (radius, grid ≤ ½ catch range, bounded maxDistance) asserted by the milestone-0 gate; AC(c-2) outcome verification protocol (Burst transit with noise event, trace-verified); claim-2 residual-floor scoring rule (attempts starting above the floor = secondary evidence, K=3 preserved).

### Nice-to-Have (carried to GDD tiers)
- Carried from review #13 (unchanged): confirm-empty duration pin; ring-buffer allocation discipline; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance on convergent spot-front checks; residual-decay vs. segment-pacing cool-down trade; runtime hang-guard for failed hide-spot paths; carve-out terminality op-def; WebGL draw-call/vertex budget pin. New: |ΔY| ledge-gate false positive (agreed as RECOMMENDED — a shallow slope/ledge crossing can trip the vertical-delta gate; tune |ΔY| + sample the navmesh foot at the gate rather than the transform).

### Status
Revisions applied (round-14 changeset, user-approved, 2026-08-15 — blocking fixes only, zero new content). Review #14 = full 6-agent panel confirmation of the round-13 changeset — 5 of 6 lenses APPROVE-ready; 13 blockers (latent contract defects, not regressions) resolved in-session via a 13-clause surgical changeset. **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-15 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 7 | Recommended: ~20 (GDD-tier handoffs, carried)
Summary: Final full 6-agent panel re-review of the round-14 changeset. **First unanimous confirmation in 15 rounds** — all six lenses confirmed the round-14 13-clause changeset is textually present and internally consistent; unity independently validated engine-implementability. 5 of 6 lenses surfaced 7 new latent contract blockers, all ruled BLOCKING at concept tier by creative-director (a wrong number, a missing quantifier, an uncapped measure, claim-mechanism gaps — each a one-clause pin, no design change). All 7 resolved via a user-approved **7-clause surgical changeset** (blocking fixes only, zero new content), with two design forks user-adjudicated (ai B1 → constrain; level B1 → both mechanisms). Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-15 changeset for that re-review.
Prior verdict resolved: Yes — review #14's 13 blockers unanimously confirmed textually applied; the 7 blockers are latent contract defects surfaced by the round-15 panel, not changeset regressions.

### Blocking Items (all resolved via approved round-15 changeset this session)
1. **Cross-guard event-count merge** [game B1] — the round-14 clause covered only hide-entry Chase + carve-out; two guards who both personally see the entry grade 1 or 2 Chase events depending on which rule a programmer applies. **Fix (Edit 1)**: general incident rule — one spot-verification incident emits exactly one Chase escalation event total, regardless of which guard-events triggered it (a hide-entry Chase + a carve-out, or multiple hide-entry Chases at the same spot), attributed to the confirmation segment.
2. **S-claim has no enforcement knob** [game B2] — "never grade S while a guard committed an escalation" is guaranteed only by a non-zero integral; the S-band thresholds are deferred placeholders. **Fix (Edit 2)**: S is the zero-input band — S requires zero above-floor exposure (null integral above the forgiveness floor) AND zero escalation events; the forgiveness floor is the S-band's lower edge.
3. **Content-band "exact closure" arithmetically false** [systems B1] — S ∈ [32.1, 41.4] yields 44.94 min (<45 floor) and 60.03 min (>60 ceiling). **Fix (Edit 3)**: closure tightened to **S ∈ [32.14, 41.38]** (true arithmetic closure under the ~1-min extreme tolerance; escape-fraction calibration stays a Level-GDD pin).
4. **PathPartial through-wall** [ai B1; CD adjudicated BLOCKING over unity's RECOMMENDED] — a thin wall between two navmesh regions makes corner distance ≈ 0, the backstop passes, and the guard captures through geometry — falsifying the doc's own "never through intervening geometry" claim. **Fix (Edit 4, user chose "constrain")**: corner-scoring restricted to genuine stand-offs — the corner-to-live-transform segment must be geometry-clear (guard's corner and player's live position mutually reachable within the bounded `NavMesh.SamplePosition maxDistance`); a partial path whose obstruction is intervening solid geometry does NOT score and cannot pass the gate.
5. **Point-blank catch-immunity window** [ai B2] — the catch fix is set only on a *valid* re-sight (sustained LOS ≥ min duration), so a player peeking at ≤ catch range for less than that is visible, within reach, and uncatchable. **Fix (Edit 5)**: decoupled — the goal flips to `LivePursuit` on FIRST LOS gain (catch check live for any player within reach, including a sub-min-duration point-blank peek); only the give-up-window reset is gated on a valid re-sight ≥ min-duration.
6. **Camera-beat budget doesn't enforce guard-dominance** [level B1; CD adjudicated over game's clause-11 verification] — an all-guard-poor-with-cameras facility satisfies every committed constraint yet yields camera = 50% of pressure events. **Fix (Edit 6, user chose "both mechanisms")**: facility-wide camera-beat cap **≤4 total** + per-segment guard-beat minimum (any camera-bearing segment carries **≥2 guard-driven beats**) + **≤2 guard-poor segments facility-wide**; mirrored in the Content Volume row.
7. **AC(d) vacuously satisfiable** [qa B1] — "from a guard reachable to it" is existential with no existence requirement; guard-unreachable islands pass the very AC they exist to exclude. **Fix (Edit 7)**: existential + non-vacuous — per sampled player-reachable position, **THERE EXISTS at least one guard** (a sweep-sampled guard position within the certified-route vicinity) whose navmesh path distance to it is ≤ catch range + margin; otherwise the position is a stand-off and the AC fails; the guard's reference position(s) pinned.

### Recommended Revisions (deferred to GDD tiers — round-15 handoffs)
- **Perception GDD**: residual decay functional form (must reach exactly 0, or define base as R ≤ ε — else only the first exposure of a segment produces an Investigate event and "repeated exposure is penalized" collapses); confirm-window scaling with residual ("repeated peeks self-escalate" is otherwise unsupported — sub-window peeks always cancel); chase duration-cap accounting (accumulates through sustained LOS or suspended?); `rate = min(k/d, rate_max)` d-floor + constraint `k ≥ rate_max × catch_range`; |ΔY| tuning (stair-baiting / thin-mezzanine) + ledge-gate as committed sweep inputs; catch-check cadence (~120–180 `CalculatePath`/s at 60 Hz × 2–3 guards is a WebGL hitch risk); guard-chase-speed invariant vs tuning knob; camera-trip residual per-guard allocation.
- **Unity / engine note**: use `agent.CalculatePath` (agent-parameterized), not static `NavMesh.CalculatePath` (static ignores agent radius → through-geometry catch through a narrow gap).
- **Level GDD**: escape-fraction calibration pin; guard-poor segment cycle-vs-time cadence bridge; "interior depth" metric; no-stand-off sweep for Target segments outside the certified-route vicinity; milestone-0 gate input list (catch range + margin); camera budget geometry (≤4 facility-wide, ≥2 guard beats per camera-bearing segment, ≤2 guard-poor segments).
- **Systems GDD**: cross-segment escalation attribution precedence; forgiveness floor visible on the grade card; anti-puppeting saturation at R_max.
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`): AC(d) grid spacing by capsule diameter (not ½ catch range); AC(a) lower bound + ≤½P verification; claim-2 residual-floor decay-wait (clean completion on a post-miss attempt); claim-3 control discoverability + denominator shrinkage.
- Round-14 handoffs carried (unchanged).

### Nice-to-Have (carried to GDD tiers)
- Carried from review #14 (unchanged): confirm-empty duration pin; ring-buffer allocation discipline; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance on convergent spot-front checks; residual-decay vs. segment-pacing cool-down trade; runtime hang-guard for failed hide-spot paths; carve-out terminality op-def; WebGL draw-call/vertex budget pin. New: "pressure beat" vs "pressure event" terminology alignment; "committed floor" ambiguity (claim-2 floor vs grade floor).

### Status
Revisions applied (round-15 changeset, user-approved, 2026-08-15 — blocking fixes only, zero new content). Review #15 = final full 6-agent panel re-review of the round-14 changeset — first unanimous confirmation of a changeset's textual application; 7 latent contract blockers resolved in-session via a 7-clause surgical changeset. **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-15 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 2 | Recommended: ~15 (GDD-tier handoffs, carried)
Summary: Final full 6-agent panel re-review of the round-15 changeset. All 7 round-15 clauses confirmed textually present and internally consistent (clauses 1–5, 7); the arithmetic closure S ∈ [32.14, 41.38] verified under every lens (32.14×1.3+0.10×32.14≈44.996; 41.38×1.3+0.15×41.38≈60.001); the FSM chase+capture contract engine-implementable with no deadlocks/livelocks. All 6 lenses flagged **BLOCKER A — camera-budget self-contradiction**: round-15 clause 6's "≥2 guard-driven beats per camera-bearing segment" collides with the round-13 sanctioned camera-choke exception (guard-poor = ≤1 guard beat/cycle) — G(S_k) ∈ [2,∞)∩(−∞,1] = ∅, the exception silently deleted (violating round-13's "never silent"), the camera-disable choke encounter type unauthorable, and the camera-only-segment cap vacuous. qa-lead additionally flagged **BLOCKER C — S-band razor-edge**: `forgiveness_floor ≤ 0.2 × T_base` permits equality, letting a residual-lowered Investigate at exactly the floor contribute zero to both grade inputs. game-designer's clause-5 BLOCKING (give-up-while-staring during sub-min-duration point-blank peeks) was adjudicated by CD as NOT BLOCKING — letter-consistent (a sub-min peek is give-up-while-hunting by definition; "never while staring" is scoped to sustained LOS) — with the closure **elevated to a committed Perception-GDD requirement**: `catch-timer duration < re-sight min-duration < give-up window`. All resolved via a user-approved **3-edit surgical changeset** (blocking fixes + one hardening, zero new content): camera-budget exemption (guard-poor camera-choke segments exempt from the ≥2 minimum, bounded by the ≤2 guard-poor cap — applied in both mirrored locations, lines 114/199), strict `<` on forgiveness_floor (line 120), and claim-2 residual-floor terminology R ≤ ε_residual (line 229). Approval remains **conditional** on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-16 changeset for that re-review.
Prior verdict resolved: Yes — review #15's 7 blockers unanimously confirmed textually applied by all 6 lenses; 2 new blockers (latent contract collisions surfaced by the round-16 panel, not changeset regressions) resolved in-session.

### Blocking Items (all resolved via approved round-16 changeset this session)
1. **Camera-budget self-contradiction** [all 6 lenses; CD adjudicated BLOCKING] — round-15 clause 6's per-segment guard-beat minimum ("any segment carrying a camera beat also carries ≥2 guard-driven beats") collides with the round-13 sanctioned camera-choke exception and the round-14 guard-poor definition ("≤1 guard-driven pressure beat per patrol cycle"): a camera-choke guard-poor segment must satisfy both ≥2 and ≤1 (systems proof: G(S_k) ∈ [2,∞)∩(−∞,1] = ∅). The exception is unauthorable — silently deleted. **Fix (Edits 1–2)**: guard-poor segments exercising the sanctioned camera-choke exception are exempt from the ≥2 minimum (each ≤1 camera beat and ≤1 guard beat per patrol cycle, bounded by the ≤2 guard-poor facility-wide cap), applied in both mirrored locations (lines 114, 199) — explicit, never silent; guard dominance preserved via the ≤4 camera cap + ≤2 guard-poor cap; every non-guard-poor camera-bearing segment still requires ≥2 guard beats.
2. **S-band razor-edge** [qa-lead F2; systems c5; CD BLOCKING] — `forgiveness_floor ≤ 0.2 × T_base` permits equality: a residual-wary guard's Investigate entry at exactly the floor yields zero above-floor integral and no escalation event → a real detection contributes zero to both grade inputs, re-opening the round-15 S-claim enforcement knob. **Fix (Edit 3)**: strict inequality `forgiveness_floor < 0.2 × T_base` — every residual-lowered Investigate entry (T_entry ≥ 0.2 × T_base > forgiveness_floor) contributes a positive above-floor integral; the S-band lower edge closes.

### Recommended Revisions (deferred to GDD tiers — round-16 handoffs)
- **Perception GDD**: `catch-timer duration < re-sight min-duration < give-up window` (committed requirement, elevated from the clause-5 dispute — CD); catch-timer retention/pause across goal flips and LOS breaks (reset-on-break would contradict the committed hysteresis); LKP update on sub-min-duration contact; carve-out `GuardGoalMode = HideSpotFront` lifecycle in Investigate (set/clear + mid-approach alert interruption); slope-aware |ΔY| backstop rejection (30° slope over 5 m rises ≈2.9 m); chase-vs-noise (a chasing guard ignores Burst noise events); claim-2 residual-floor value ε_residual.
- **Systems GDD**: spot-verification incident boundary (temporal/spatial window defining "one incident" for the cross-guard merge); S-achievability gap — AC(a) verifies zero *committed* escalation, weaker than S's zero *above-floor exposure* (flagged, S validated at Vertical Slice only).
- **Level GDD**: camera-bearing segments must sit at the large end of the 7-segment band (a 3.6-min segment cannot hold ≥3 beats at the 2–4 min cadence floor); guard-poor segment enumeration re-scoped with the round-16 exemption; escape-route coherence; "exact closure" S ∈ [32.14, 41.38] carries false precision given the ~1.3 overhead is measured.
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate): PathPartial "geometry-clear" operationalization; AC(d) grid spacing — reconcile the round-15 capsule-diameter handoff with "finer than ½ catch range" so stand-off pockets smaller than the grid cannot slip the sweep; WebGL `NavMeshPath` allocation discipline (reuse one mutable path per agent); claim-2 residual-floor scoring must reference ε_residual.

### Nice-to-Have (carried to GDD tiers)
- Carried from review #15 (unchanged): confirm-empty duration pin; ring-buffer allocation discipline; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance on convergent spot-front checks; residual-decay vs. segment-pacing cool-down trade; runtime hang-guard for failed hide-spot paths; carve-out terminality op-def; WebGL draw-call/vertex budget pin; "pressure beat" vs "pressure event" terminology alignment; "committed floor" ambiguity (now resolved at concept tier — claim-2 floor vs grade floor). New: AC(d) deep off-mesh exclusion from the existential test + guard-position sampling domain (game R3); per-segment row arithmetic overshoots the S-invariant by ~0.6 min (level F3); forgiveness-floor equality edge (now resolved at concept tier — strict <).

### Status
Revisions applied (round-16 changeset, user-approved, 2026-08-15 — blocking fixes + one hardening only, zero new content). Review #16 = final full 6-agent panel re-review of the round-15 changeset — all 7 clauses unanimously confirmed textually applied; 2 new latent contract collisions resolved in-session via a 3-edit surgical changeset (camera-budget exemption ×2, strict `<`, floor terminology). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-15 — Verdict: NEEDS REVISION (focused)
Scope signal: M
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 6 | Recommended: ~30 (GDD-tier handoffs, carried)
Summary: Final full 6-agent panel confirmation of the round-16 changeset. Round-16 clauses 1 (camera-budget exemption) and 3 (claim-2 residual floor R ≤ ε_residual) confirmed textually present + internally consistent in both mirrored locations; clause 2's strict-`<` formula present but its own surrounding prose regressed — "at or below" permits the equality the changeset was written to forbid (B-1, 3-lens consensus). Panel surfaced 5 further one-clause contract pins (B-2 PathPartial geometry-clear defective — `SamplePosition` is a nearest-point query, not an occlusion test; B-3 elevated hide spots = capture immunity; B-4 chained hide-spot hops — catch-timer retention uncommitted; B-5 give-up-window suspension scope self-contradictory; B-6 S-achievability gap — AC(a) tolerance above the forgiveness floor). Both specialist disputes adjudicated BLOCKING by CD (B-1: systems/game/qa over unity NTH; B-2: qa over unity defer). All resolved via a user-approved 7-edit surgical changeset (blocking fixes only, zero new content): strictly-below prose; named `Physics.Linecast` occlusion test; backstop vertical pin + AC(d) vertical-delta sweep (authoring-pin choice); catch-timer retained across goal flips/LOS breaks while a live fix is held; give-up window suspends during any sustained continuous LOS, first ticking at the first LOS break; AC(a) tolerance bounded to forgiveness floor. Creative layer converged and untouched by every blocker. CD standing rule: the round-17 changeset must go through **one more full 6-agent confirmation before APPROVED**. Approval remains conditional on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-17 changeset for that re-review.
Prior verdict resolved: Yes — review #16's 2 blockers resolved; 6 new latent contract pins (1 changeset regression + 5 panel-surfaced) resolved in-session.

### Blocking Items (all resolved via approved round-17 changeset this session)
1. **Prose regression in the round-16 clause itself** [systems B1, game B1, qa B1 — 3-lens consensus; unity NTH; CD adjudicated BLOCKING] — line 120's formula reads `forgiveness_floor < 0.2 × T_base` but the surrounding prose "pinned at or below the residual-lowered entry-threshold floor" is `≤`. Since the residual-lowered entry-threshold floor IS `0.2 × T_base`, the prose permits the exact equality round-16 forbade → a committed residual-lowered Investigate at the floor contributes zero above-floor integral AND zero escalation event → the S-razor re-opens for any prose-citing reader; also voids the `T_entry > forgiveness_floor` guarantee at the equality point. An incomplete-change regression: the changeset updated the formula and left the prose in pre-fix form. **Fix (Edit 1)**: "at or below" → "strictly below" (one word).
2. **PathPartial "geometry-clear" defective in-text** [qa B2; unity RECOMMENDED defer; CD adjudicated BLOCKING in-text] — line 74 defines geometry-clear as "mutually reachable within the bounded `NavMesh.SamplePosition maxDistance`". `SamplePosition` is a nearest-point query, not an occlusion test. Reading (a): trivially satisfied for any on-mesh player → thin wall between two navmesh regions passes → through-wall catch (round-15's exact false positive). Reading (b): no path exists between the corner and the position for every PathPartial → corner-scoring never scores → the shallow-off-mesh "capture still fires" claim unreachable. **Fix (Edit 2)**: name the operative occlusion test — physics `Physics.Linecast` from the guard's corner to the player's live transform (guard + player colliders excluded), clear of intervening solid geometry; explicit note that `SamplePosition` is nearest-point, not occlusion.
3. **Elevated hide spots = capture immunity** [ai B1] — the backstop-depth pin bounds interior depth by catch range + margin but is silent on vertical offset; a shelf/mezzanine spot 2-3 m above the guard's navmesh passes the |ΔY| ≤ 1.0-1.5 m backstop AND AC(d)'s planar sweep, yet the reach check resolves "empty" → false confirm-empty, violating line 77's committed "never capture immunity". **Fix (Edits 3-4, user chose "authoring pin")**: backstop vertical pin — hide-spot interior vertical offset above the guard's navmesh floor ≤ the |ΔY| tolerance (≈1.0-1.5 m), so elevated spots are not authorable; AC(d) sweep additionally checks the vertical delta (a sampled position whose |ΔY| from the guard's reference exceeds the tolerance fails the existence requirement).
4. **Chained hide-spot hops defeat "caps bound the hunt"** [ai B2] — dive→burst-out-seen→dive→repeat suspends or never-terminates every cap (hide-entry suspension + sustained-LOS carve-out); the only terminator is the catch check, whose timer retention-vs-reset across goal flips/LOS breaks is uncommitted (existed only as a Perception-GDD handoff). Reset-on-break → livelock. **Fix (Edit 5)**: catch timer retained — paused, never reset — across goal flips and LOS breaks while the guard holds a live fix; chained hops accumulate catch time rather than restarting it.
5. **Give-up-window suspension scope self-contradictory** [ai B3] — "suspended while a valid re-sight is maintained" excludes the accumulator-entry Chase's initial sustained LOS (not a re-sight) → the window ticks through the initial stare → single-tick corner occlusion + re-peek ≥ min-duration reproduces the exact instant re-Chase loop the sentence claims does not exist. **Fix (Edit 6)**: window suspended during any sustained continuous LOS on the player (initial acquisition or re-acquired), first ticking at the first LOS break — reading B, consistent with the adjacent caps clause.
6. **S-achievability gap** [game R3; CD adjudicated BLOCKING] — AC(a) certifies zero *committed* escalation while explicitly tolerating "transient sub-confirm-window samples" that reach ≥ T_entry ≥ 0.2 × T_base — far above the forgiveness floor (`< 0.2 × T_base`) — so tolerated transients accrue above-floor integral and a certified-clean route followed perfectly can grade below S. **Fix (Edit 7)**: AC(a)'s tolerance bounded to the grade's forgiveness floor — only transients strictly below `forgiveness_floor` tolerated → certified-clean transit implies a null above-floor exposure integral and therefore S-eligibility.

### Recommended Revisions (deferred to GDD tiers — round-17 handoffs)
- **Perception GDD** (owns catch-contract mechanics + tuning): catch-check cadence pin (60 Hz catch check × guards = 120-180 `agent.CalculatePath`/`SamplePosition` per second → WebGL hitch risk — the chase-target sample is throttled but not the catch check) [unity R1]; exact raycast parameters for the corrected geometry-clear test (linecast corner→live transform, guard + player excluded) [unity R2]; PathInvalid handling — scores false (backstop AND-gate prevents fire, state it) [unity R3]; full 4-term catch-closure chain in-text (`catch-timer duration < re-sight min-duration < give-up window < chase-duration cap` — currently only the 3-term sub-chain is in the doc) [systems R2 + ai R1]; `rate = min(k/d, rate_max)` d-floor (0/0 = NaN → meter cascade) [systems R3]; Chase-threshold reachability under the per-tick cap (`rate_max × max-continuous-LOS` must exceed the Chase threshold) [systems R4]; k vs rate_max × catch_range [systems R5]; give-up-window/cap state at the hide-spot → live-pursuit transition [ai R2]; qualify "a single cancelled peek is genuinely free" (a seen dive into a hide spot is a hide-entry Chase, not free) [ai R3]; "sub-threshold LOS contact" → "sub-minimum-duration" in Chase context [ai R4]; sub-window peek exploit — residual accrual-on-cancelled-peek vs ~45-60 s decay balance + confirm-window-scaling-with-residual handoff; scope the "repeated peeks self-escalate" sentence to sustained peeks [game R2, ruled GDD-tier by CD].
- **Systems GDD**: Burst quantity/replenishment/cap — Pillar 3's "tool use is a limited resource" is currently a bare claim [game R4]; camera-trip residual allocation (all segment guards vs investigating guard) [game R5]; forgiveness-floor value, compounding law, band thresholds [deferred per line 120]; float-precision razor on strict-`<` (margin/epsilon), forgiveness_floor ≥ 0 lower bound, V_run ∈ [5, 7.2] pin [systems NTH].
- **Level GDD**: camera-bearing non-guard-poor segments arithmetically forced to ≥4 min — state as explicit authoring rule, not derivation [level R1]; escape-tail (3-6 min) beat content + camera-budget draw uncommitted [level R2]; "camera-only segment" undefined vs the guard-poor exemption (possible redundancy with ≤2 guard-poor) [level R3]; verb-route rule may be unsatisfiable in a camera-only major area (camera-disable is not one of the two committed verbs) [level R4]; ≤1 camera-beat-per-segment cap scope ambiguity in line 114 (make facility-wide explicit) [game R6]; facility geometry volume (~3.9-5.7 km path) = dominant schedule risk — keep producer revalidation as the gate [level R5].
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate — the carried approval condition): all 13 qa recommendations — claim-3 minimum viable denominator (INCONCLUSIVE below 5 presented testers; no vacuous 1-of-1 pass); claim-2 "first post-observation attempt" vs "execution miss does not consume the attempt, K=3" collision; deferred-reveal protocol (a capture during claim-2's crossing is the tester's first capture → the claim-1 trace reveal contaminates claim-2); AC(a) crouch/walk window semantics (one window vs per-speed); AC(a) "no noise-driven Investigate that re-covers the route" operational definition; AC(b) sample coverage; AC(c-2) Burst placement/count protocol; AC(d) guard-position sampling domain + sweep-tool commitment + grid-spacing reconciliation (capsule diameter vs ½ catch range); "camera-only" vs "guard-poor" undefined overlap; claim-3 "reached/found/picked-up" denominator ambiguity; claim-1 recruitment for never-captured testers; enforcement claims (no mid-run access, no `Debug.Log`); ring-buffer capacity/eviction; capture op-def for the carve-out; safe-vantage zero-residual idle-posture assumption; AC(a) residual-reset dev mechanism.

### Nice-to-Have (carried to GDD tiers)
- Carried from review #16 (unchanged): confirm-empty duration pin; ring-buffer allocation discipline; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance on convergent spot-front checks; residual-decay vs. segment-pacing cool-down trade; runtime hang-guard for failed hide-spot paths; carve-out terminality op-def; WebGL draw-call/vertex budget pin; "pressure beat" vs "pressure event" terminology alignment; AC(d) deep off-mesh exclusion + guard-position sampling domain; per-segment row arithmetic overshoot; forgiveness-floor equality (now resolved at concept tier — strict <). New: 2 Hz stop-and-go pursuit feel + WebGL IndexedDB phrasing [unity NTH]; content-band closure is a rounded superset [systems NTH]; 9-segment row overshoots exact closure + spot-verification "incident" boundary + clean-tool-use-visibly-wary tension (communicate via grade card) [game NTH].

### Status
Revisions applied (round-17 changeset, user-approved, 2026-08-15 — blocking fixes only, zero new content). Review #17 = final full 6-agent panel confirmation of the round-16 changeset — clauses 1 + 3 unanimously confirmed present + consistent; clause 2's formula confirmed but its prose regressed (B-1); 5 further one-clause contract pins resolved in-session via a 7-edit surgical changeset (strictly-below prose, named occlusion test, backstop vertical pin + AC(d) vertical sweep, catch-timer retention, give-up suspension scope, AC(a) tolerance bound). CD standing rule: the round-17 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; a 6-clause changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-16 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 10 | Recommended: ~11 (GDD-tier handoffs, carried)
Summary: Final full 6-agent panel confirmation of the round-17 changeset. All six round-17 pins (strictly-below prose, named `Physics.Linecast` occlusion test, backstop vertical pin + AC(d) vertical-delta sweep, catch-timer retention, give-up suspension scope, AC(a) tolerance bounded to forgiveness floor) confirmed textually present + formula-consistent — systems-designer APPROVE-ready at the formula layer (zero blockers). Panel surfaced 10 latent one-clause contract/verifiability blockers — led by a 3-lens consensus (game N-1, ai NB-1, qa N-5) on a catch-timer livelock: round-17's "retained, never reset … while a live fix is held" collides with the pre-existing fix-clearing clause "or the guard redirects to a *new* LKP", so a chained hide-spot hop (dive→dart→dive) resets the retained timer while give-up + caps are suspended → Chase has no terminator. Remaining: AC(c-2) fail-bar ambiguity (B-2); AC(d) guard-position sampling domain (B-3); milestone-0 gate completeness — AC(a) unverifiable without provisional values (B-4); sustained-LOS-out-of-reach livelock — "never while staring" lacks a reachability qualifier (B-5); FSM trace omits LOS breaks — three verifications unexecutable (B-6); AC(d) sweep missing the PathPartial scoring rule (B-7); |ΔY| band not a single committed value (B-8); |ΔY| inclusive-`≤` razor on the hide-spot pin — strict-`<` mirror of round-16 B-1 (B-9); Linecast exclusion mechanism unpinned — `IgnoreCollision` doesn't affect raycasts (B-10). All creative-layer verdicts converged and untouched by every blocker; all 10 resolve as surgical one-clause pins with zero design change. Two design-policy forks adjudicated by user: **Fork A = provisional grade values asserted at the milestone-0 gate** (preserves round-17 B-6 S-eligibility closure); **Fork B = give-up window resumes ticking on a geometrically-unreachable staring player** (reuses the established give-up terminator). All resolved via a user-approved 11-edit surgical changeset (blocking fixes only, zero new content): catch-timer retention scoped to continuously-held live pursuit + reset pinned to full fix relinquishment; fix-redirect clearing restricted to stale LKPs with no live player-ward goal; AC(c-2) fail bar pinned to the claim-2 operational definition; AC(d) guard sampling pinned to on-navmesh points; milestone-0 gate asserts a single committed |ΔY| value + committed provisional grade values; "geometrically reachable" qualifier + give-up resumes on unreachable; LOS breaks + timestamps added to both trace enumerations; AC(d) PathPartial scoring threaded from the runtime contract; |ΔY| band tagged as authoring guidance; hide-spot vertical pin made strictly-below; Linecast exclusion pinned to dedicated collider layers + layer-mask. CD standing rule: the round-18 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; an 11-clause changeset is not safe to self-certify). Approval remains conditional on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-18 changeset for that re-review.
Prior verdict resolved: Yes — review #17's 6 pins confirmed applied; 10 new latent contract/verifiability blockers (1 changeset-clause collision + 9 panel-surfaced) resolved in-session.

### Blocking Items (all resolved via the approved round-18 changeset this session)
1. **Catch-timer reset / fix-redirect collision → chained hide-spot livelock** [game N-1, ai NB-1, qa N-5 — 3-lens consensus, strongest signal in the round] — round-17's retention clause ("retained, never reset … while the guard holds a live fix") collides with the pre-existing fix-clearing clause ("or the guard redirects to a *new* LKP"). Under a strict reading, a chained hide-spot hop redirects to a new LKP each hop → the clearing clause fires → the "while a live fix is held" qualifier is no longer satisfied → the timer resets → and with give-up + caps suspended for hide-spot LKPs, Chase has no terminator → livelock (ai NB-1 supplies a concrete FSM trace). qa N-5 adds: MVP ships no hide spots, so no playtest scenario exercises it — must be pinned at concept tier + routed to a Perception-GDD FSM-contract unit test. **Fixes (Edits 1-2)**: retention scoped to continuously-held live pursuit (a chained hop re-sets the fix target without clearing the timer); reset pinned to full fix relinquishment (stale-LKP arrival with no LOS, confirm-empty, or Chase-episode termination via give-up/cap); the "redirect to a *new* LKP" clearing clause restricted to stale-LKP redirects with no live player-ward goal (`LivePursuit`/`HideSpotFront` redirects re-set the fix target without clearing the catch timer).
2. **AC(c-2) fail-bar ambiguity** [game N-2] — "without a committed LOS escalation" is undefined between the grade's base-threshold-only escalation-event definition and the claim-2 decoupled operational definition (counts residual-lowered entries). The strengthened look-around specifically lowers entry thresholds via the residual, so the two bars pass/fail differently on the exact scenario the AC tests — a Burst crossing triggering a residual-lowered Investigate would PASS under the base-only bar while the player was visually detected. **Fix (Edit 3)**: "committed LOS escalation" here pinned to the claim-2 operational definition (any actual LOS-suspicion escalation to Investigate or Chase, including residual-lowered entries, excluding confirm-window-cancelled peeks and noise/alert-only Investigates), not the grade's base-threshold-only definition.
3. **AC(d) guard-position sampling domain undefined** [level NB-1] — "a sweep-sampled guard position within the certified-route vicinity" is undefined: an off-navmesh grid point below a ledge produces a |ΔY| that wrongly fails a legitimately-catchable elevated position; an on-navmesh-only sample misses the elevated-player stand-off the vertical sub-check exists to catch. The vertical sub-check makes this decisive; the sweep tool cannot be built. **Fix (Edit 4)**: guard positions sampled only at on-navmesh (walkable) points at the same grid resolution as the player-position sample, |ΔY| measured from each guard position's navmesh-sampled reference floor, never from an off-navmesh grid point.
4. **Milestone-0 gate completeness / AC(a) unverifiable** [level NB-2, qa N-4] — the gate asserts only AC(d)'s sweep inputs (radius, grid spacing, maxDistance), but AC(a)/(b)/(c) are also MVP requirements and AC(a) cannot run without committed `forgiveness_floor` + `T_base` + confirm-window + start-phase-window values, deferred to the Systems/Perception GDDs. A milestone-0 build can pass its gate while AC(a) is unverifiable at the subsequent playtest. **Fix (Edit 5, user chose Fork A — provisional values at gate)**: the gate additionally asserts a single committed |ΔY| tolerance value (the ≈1.0-1.5 m band is authoring guidance, not an executable bound — the sweep uses the committed single value) and committed *provisional* grade values (`forgiveness_floor`, `0.2 × T_base`, confirm-window, AC(a) start-phase-window) before AC(a) can be reported passing — re-validated at the Vertical Slice playtest.
5. **Sustained-LOS-out-of-reach livelock** [ai NB-2] — "never while staring at the player" + give-up suspended during sustained LOS + caps suspended during sustained LOS + Chase ignores peer alerts has no geometric-reachability escape hatch. LOS-visible but geometrically unreachable (high ledge beyond the |ΔY| backstop, see-through-but-collider-bearing barrier, uncrossable gap) → catch check red, window suspended, caps suspended, alerts ignored → Chase has no outgoing transition. AC(d)'s mitigation is authoring-time and scoped to the certified-route vicinity. **Fix (Edit 6, user chose Fork B — resume give-up on unreachable)**: the stare-invariant qualified to a *geometrically reachable* player (navmesh path distance ≤ catch range + margin OR the |ΔY|-gated Euclidean backstop); if unreachable, the give-up window resumes ticking and the chase terminates on its expiry (guard returns to Investigate at the LKP, no instant re-Chase), re-suspending if the player becomes reachable before expiry.
6. **FSM trace omits LOS breaks → three verifications unexecutable** [qa N-1] — the trace logs "every LOS gain" but never LOS breaks. Claim-1's rubric example ("I lingered in the light past the confirm window") needs gain/break timestamps; claim-2's confirm-window-cancelled exclusion needs the break's position relative to the confirm window; B-5's "first starts ticking at the first LOS break" needs the break timestamp. The trace is a concept-tier artifact, so the playtest plan (its downstream consumer) is not the place to fix it. **Fix (Edit 7)**: "every LOS gain and every LOS break (source, distance, guard, timestamp)" in both trace enumerations.
7. **AC(d) sweep never references the PathPartial scoring rule** [qa N-2] — the sweep asserts "navmesh path distance ≤ catch range + margin" but never defines how path-partial positions score; the runtime contract scores them via corner-distance + Linecast-clear. A sweep treating PathPartial as unreachable wrongly fails shallow catchable off-mesh positions; summed-full-corner-distance can pass a through-wall stand-off. **Fix (Edit 8)**: the sweep scores path-partial positions by the runtime rule — cumulative corner distance to the closest reachable corner, restricted to genuine stand-offs where the corner-to-live-transform segment is geometry-clear via `Physics.Linecast`; a partial path obstructed by intervening solid geometry does not score and cannot pass.
8. **|ΔY| tolerance is a band, not a committed single value** [qa N-3] — "≈1.0-1.5 m" is a tuning band with no closure rule, absent from the milestone-0 gate's committed-input list; a sweep cannot execute an "≈" bound. **Fixes (Edits 5, 9, 10)**: single committed |ΔY| value asserted at the gate; the band tagged as authoring guidance at both the catch metric and the hide-spot pin.
9. **|ΔY| inclusive-`≤` razor on the hide-spot authoring pin** [unity N1] — the pin's "interior vertical offset above the guard's navmesh floor ≤ the |ΔY| tolerance" lets a spot authored at exactly the tolerance resolve "empty" at runtime (backstop fails under feet-offset/transform-vs-navmesh variance) → false confirm-empty → capture immunity. A strict-`<` mirror of round-16 B-1 — same defect class (inclusive boundary where equality is authorable). **Fix (Edit 10)**: "strictly below the committed |ΔY| tolerance"; float-precision margin handed to the Systems GDD.
10. **`Physics.Linecast` exclusion mechanism unpinned** [unity N2] — "guard and player colliders excluded" never states the mechanism. `Physics.IgnoreCollision` does not affect raycasts (only rigidbody/collider pairs); PhysX auto-skips only the collider containing the ray origin, so a bystander guard's collider can block a legitimate catch. The through-wall-catch commitment (recurring blocker in rounds 12/13/15) is not reliably closed. **Fix (Edit 11)**: dedicated collider layers + a layer-mask filter on the Linecast (never `IgnoreCollision`); a bystander guard's collider must also be layer-excluded.

### Recommended Revisions (deferred to GDD tiers — round-18 handoffs)
- **Perception GDD**: catch-timer FSM-contract unit test (B-1 reset semantics); exact Linecast ray parameters (origin, layer mask); `PathInvalid` handling (scores false, backstop AND-gate prevents fire) [unity]; catch-check cadence pin (60 Hz catch check × 2-3 guards = 120-180 `CalculatePath`/s → WebGL hitch) [unity R1]; full 4-term catch-closure chain in-text (`catch-timer duration < re-sight min-duration < give-up window < chase-duration cap`) [systems + ai]; give-up-window/cap state at the hide-spot → live-pursuit transition; the B-5 reachability-qualifier re-suspend-on-reachable detail; LKP update on sub-min-duration contact; slope-aware |ΔY| backstop rejection; chase-vs-noise (a chasing guard ignores Burst); claim-2 residual-floor value `ε_residual`.
- **Systems GDD**: spot-verification "incident" boundary (temporal/spatial window for the cross-guard merge) [game]; S-achievability note (AC(a) verifies zero *committed* escalation, weaker than S's zero *above-floor* exposure — S validated at Vertical Slice only); Burst quantity/replenishment/cap (Pillar 3's "limited resource" is a bare claim) [game]; camera-trip residual allocation [game]; forgiveness-floor value, compounding law, band thresholds; float-precision razor on the new strict-`<` pins (`forgiveness_floor ≥ 0` lower bound, epsilon/margin, `V_run` pin).
- **Level GDD**: per-segment row arithmetic (9-segment row still overshoots exact closure S ∈ [32.14, 41.38] by ~0.6 min — restate or pin the authoring rule); AC(d) grid-spacing reconciliation (round-15 capsule-diameter handoff vs committed "≤ ½ catch range"); "pressure beat" vs "pressure event" terminology alignment; camera-bearing non-guard-poor segments arithmetically forced to ≥4 min as an explicit authoring rule.
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate — the carried approval condition): LOS-break timestamps in the trace protocol (B-6); AC(d) guard-position sampling domain (B-3); committed |ΔY| single value + provisional grade values (B-4/B-8); AC(d) PathPartial scoring reference (B-7); AC(a) parenthetical example of a tolerated vs disqualifying transient; AC(a) sampling adequacy (≥10 Hz × ≥3 cycles vs confirm-window duration); AC(d) deep off-mesh exclusion; all carried round-17 qa items (claim-3 denominator, claim-2 first-attempt collision, deferred-reveal contamination, AC(a) window semantics, claim-1 never-captured recruitment, enforcement claims, ring-buffer capacity).

### Nice-to-Have (carried to GDD tiers)
- Carried from review #17 (unchanged): confirm-empty duration pin; ring-buffer allocation discipline; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance on convergent spot-front checks; residual-decay vs segment-pacing cool-down trade; runtime hang-guard for failed hide-spot paths; carve-out terminality op-def; WebGL draw-call/vertex budget pin; "pressure beat" vs "pressure event" terminology alignment; AC(d) deep off-mesh exclusion + guard-position sampling domain; per-segment row arithmetic overshoot. New: clean-tool-use-visibly-wary tension (communicate via the grade card, not the in-world state) [game NTH]; 9-segment row / content-band "exact closure" rounded-superset honesty note [systems NTH]; float-precision margin on the strict-`<` pins [systems NTH].

### Status
Revisions applied (round-18 changeset, user-approved, 2026-08-16 — blocking fixes only, zero new content). Review #18 = final full 6-agent panel confirmation of the round-17 changeset — all six round-17 pins confirmed textually present + formula-consistent (systems-designer APPROVE-ready at the formula layer); 10 latent one-clause contract/verifiability blockers resolved in-session via an 11-edit surgical changeset (catch-timer retention scoped + reset pinned; fix-redirect restricted to stale LKPs; AC(c-2) fail bar pinned; AC(d) guard sampling pinned; milestone-0 gate asserts single |ΔY| + provisional grade values [Fork A]; "geometrically reachable" qualifier + give-up resumes on unreachable [Fork B]; LOS breaks + timestamps in the trace; AC(d) PathPartial scoring threaded; |ΔY| band tagged as guidance; hide-spot pin strictly-below; Linecast exclusion via layers + layer-mask). CD standing rule: the round-18 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; an 11-clause changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-16 — Verdict: NEEDS REVISION (focused)
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director
Blocking items: 8 | Recommended: ~30 (GDD-tier handoffs, carried)
Summary: Final full 6-agent panel confirmation of the round-18 changeset (11 surgical edits, user-approved 2026-08-16). All six lens-owned round-18 pins confirmed textually present — systems-designer verified the entire formula layer CLEAN (rate, `T_entry` floor, strict-`<` forgiveness, catch-closure invariant, |ΔY| family, S-band event accounting; round-17's prose regression definitively gone); game-designer confirmed the escalation-event accounting + AC(c-2)/claim-2 bar decoupling airtight and reported NO BLOCKING in its domain; the creative layer converged and untouched by every finding. Panel surfaced **8 latent contract/verifiability blockers** — zero changeset-introduced regressions (the first round in the cycle where the round-18 edits produced no fresh collision class): B-1 reachability-qualifier-vs-catch-gate divergence → sustained-LOS stare livelock + runtime/certification disagreement (ai-programmer); B-2 backstop arithmetic stated two mutually exclusive ways — AND-form vs L1-sum-form (ai-programmer + unity-specialist, 2-lens corroboration); B-3 per-segment rows breach the exact-closure S-band [32.14, 41.38] at 3 of 6 endpoints (systems-designer + level-designer, 2-lens); B-4 AC(d) guard-sampling lattice not coupled to the margin → sweep falsely fails legitimate catchable positions (level-designer); B-5 a 3.6-min camera-bearing non-guard-poor segment is jointly un-authorable (level-designer); B-6 round-18 B-6 half-applied — line-238 trace schema drops source/distance/guard (qa-lead); B-7 AC(d) hide-spot assertion set omits the interior-|ΔY| pin line 77 claims is "swept in AC(d)" (qa-lead + unity-specialist, 2-lens); B-8 the PathPartial `Linecast` is floor-defeated + trigger-polluted (unity-specialist). All 8 resolve as surgical one-clause pins requiring **zero design decisions** (CD: every fix determined by the doc's own committed invariants). Two authoring forks adjudicated by user: **B-5 = camera-bearing non-guard-poor segments require ≥4 min** (explicit concept authoring rule); **B-3 = 2-dp per-segment rows (7 → 4.60-5.91, 8 → 4.02-5.17, 9 → 3.58-4.59) nesting strictly inside the exact closure, declared binding**. All resolved via a user-approved 8-clause surgical changeset (blocking fixes only, zero new content): reachability qualifier pinned to the catch's own gated path-arrival metric; AND-form backstop pinned authoritative, L1-sum deleted; rows reconciled to the exact closure; guard sampling at grid spacing ≤ committed margin + catch range/margin added to the milestone-0 gate inputs; camera-bearing non-guard-poor segments ≥4 min (both budget sites); line-238 trace enumeration made identical to line 231; AC(d) hide-spot assertions gain the backstop vertical pin; Linecast endpoints pinned to body height (+(0, 1.0, 0)) + `QueryTriggerInteraction.Ignore` + World-only mask. **Converging — strongest signal yet** (blocker count 13 → 10 → 8; formula layer down to one committed-number contradiction; first round with zero changeset-introduced collisions), but not yet converged — the AC(d) sweep instrument is the densest remaining contract surface (B-4, B-7, + carried sub-grid pockets). CD standing rule: the round-19 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; an 8-clause changeset is not safe to self-certify). Approval remains conditional on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-19 changeset for that re-review.
Prior verdict resolved: Yes — review #18's 11-edit changeset confirmed applied with zero changeset-introduced regressions; 8 new latent contract/verifiability blockers (panel-surfaced) resolved in-session.

### Blocking Items (all resolved via the approved round-19 changeset this session)
1. **Reachability qualifier and catch gate disagree on shallow off-mesh geometry → sustained-LOS stare livelock + runtime/certification divergence** [ai-programmer F1] — line 74's give-up-resume clause (round-18 Fork B) tests reachability as raw corner distance (ungated); the catch path-arrival gate scores PathPartial as corner distance ≤ bound **AND** corner-to-live `Linecast`-clear (gated). On a shallow off-mesh island whose corner-to-live segment is blocked by the island lip: corner distance ≤ bound → reachability says "reachable" → give-up stays suspended, caps suspended → but the gated catch never passes → Chase has no terminating transition while the player holds LOS. The AC(d) sweep uses the gated rule; the runtime qualifier used the ungated rule — runtime and certification disagreed on identical geometry. **Fix (Edit 1)**: "geometrically reachable" pinned to the catch contract's own path-arrival metric (corner distance ≤ bound **AND**, for partial paths, Linecast-clear genuine stand-off) — reachability ⟺ catchability, one rule; route a Perception-GDD FSM-contract unit test.
2. **Backstop arithmetic stated two mutually exclusive ways** [ai-programmer F2 + unity-specialist F1 — 2-lens corroboration] — line 74 stated the backstop as AND-form ("XZ distance with `|ΔY| ≤` tolerance") AND as L1-sum-form ("XZ + vertical delta ≤ catch range + margin"). Different pass sets (e.g. XZ=2.4, |ΔY|=1.0, bound=3.0: AND passes, sum fails → permanent stare). The sum-form's vertical bound also contradicts B-9's strictly-below hide-spot pin. **Fix (Edit 2)**: AND-form pinned authoritative (XZ ≤ catch range + margin **AND** |ΔY| ≤ committed tolerance), matching the round-12/13 catch metric + catch-closure invariant; the XZ + |ΔY| L1 sum explicitly deleted as a different bound with a different pass set.
3. **Per-segment rows breach the exact-closure S-band** [systems-designer B-1 + level-designer N1 — 2-lens] — line 199 committed both "S ∈ [32, 42] (binding)" and "exact closure S ∈ [32.14, 41.38]"; the rows breached the exact closure at 3 of 6 endpoints (7 → 42.0 > 41.38; 8 → 32.0 < 32.14 AND 42.0 > 41.38; 9 → 41.4 > 41.38), worst first-run 60.9 min. Unfixable by 1-dp re-rounding. **Fix (Edits 3-4, user chose 2-dp rows keeping the closure)**: rows → 7: 4.60-5.91, 8: 4.02-5.17, 9: 3.58-4.59 (nest strictly inside [32.14, 41.38]: 4.60×7=32.2 ≥ 32.14, 5.91×7=41.37 ≤ 41.38, 4.02×8=32.16, 5.17×8=41.36, 3.58×9=32.22, 4.59×9=41.31); exact closure declared the binding authoring constraint.
4. **AC(d) guard-sampling lattice not coupled to the margin → sweep unsound** [level-designer B1] — line 234 committed guard positions "at the same grid resolution as the player-position sample" (spacing ≤ ½ catch range) while the envelope is catch range + margin with margin ≤ hysteresis (small). Player coverage needs ≤ ½ catch range; **guard coverage needs ≤ margin**. Concrete: grid 1.5 m (½ of 3 m), margin 0.3 m, guard at true path distance 3.0 m with nearest walkable lattice neighbor 1.0 m away scores 4.0 m > 3.3 m → AC(d) fails a position the runtime legitimately catches. **Fix (Edits 5-6)**: guard positions sampled at grid spacing ≤ the committed margin (finer than the player grid); **catch range + margin added to the milestone-0 gate's committed-inputs list** (the sweep cannot execute without them).
5. **A 3.6-min camera-bearing non-guard-poor segment is jointly un-authorable** [level-designer B2] — four concept-tier commitments collide: 9-segment floor = 3.6 min/segment, ≥2 guard beats per camera-bearing non-guard-poor segment, ≤1 camera beat/segment, ≥2 min between pressure events. Such a segment needs ≥3 pressure events → ≥4-min span → a 3.6-min segment holds ≤2. **Fix (Edits 7-8, user chose the ≥4-min authoring rule)**: camera-bearing non-guard-poor segments additionally require ≥4 min (explicit concept authoring rule, both budget sites — a 3.6-min 9-segment-floor segment is never a camera-bearing non-guard-poor segment; camera beats there are guard-poor, bounded by the ≤2 guard-poor cap, or absent).
6. **Round-18 B-6 half-applied — trace schema divergence** [qa-lead BLOCKING 1] — line 231 logs every LOS gain/break with (source, distance, guard, timestamp); line 238 (MVP item 5) logged gains/breaks "with timestamps" only. Claim-1 rubric statements of the "Guard A's LOS from the east window" class are unscorable from item-5 telemetry. **Fix (Edit 9)**: line-238 enumeration made identical to line 231 (source, distance, guard, timestamp).
7. **AC(d) hide-spot assertion set omits the interior-|ΔY| pin** [qa-lead BLOCKING 2 + unity-specialist F4 — 2-lens] — line 77 commits the backstop vertical pin and claims it is "swept in AC(d)", but line 234's hide-spot assertions were only guard-reachability + backstop-depth. The general |ΔY| gate runs at milestone-0 where hide spots are vacuous. An elevated spot (interior 1.8 m above floor) passes all stated assertions yet resolves "confirm empty" at runtime → capture immunity, violating line 77's own commitment. **Fix (Edit 10)**: AC(d)'s hide-spot assertion set gains the backstop vertical pin (interior vertical offset strictly below the committed |ΔY| tolerance, mirroring line 77).
8. **The PathPartial `Linecast` is floor-defeated and trigger-polluted** [unity-specialist F2] — the geometry-clear ray from "the guard's corner" (floor level) to "the player's live transform" (unpinned): a floor-level ray returns a PhysX hit at ≈0 distance (raycast from inside the ground) → the segment always reads blocked by the FLOOR → PathPartial corner-scoring never passes (runtime AND sweep) → stand-offs evade capture and the AC(d) sweep certifies the vacuous check it exists to prevent. `Physics.queriesHitTriggers` defaults true → triggers also block. **Fix (Edit 11)**: both endpoints offset to a representative body height (corner + (0, 1.0, 0) → live transform + (0, 1.0, 0)), cast with `QueryTriggerInteraction.Ignore`, World/solid-only mask; exact ray parameters → Perception GDD.

### Recommended Revisions (deferred to GDD tiers — round-19 handoffs)
- **Perception GDD**: accumulator-mediated Chase↔Investigate oscillation while a player camps an unreachable island holding LOS (ai R3 + game N3, cross-lens — "no instant re-Chase" is technically met but the state cycles forever); Chase-vs-direct-heard-noise undefined — line 76 bars only *peer alerts*, not direct noise, so a Chase-guard noise-redirect to a stale LKP makes the redirect clause + its "re-set without clear" parenthetical ambiguous (ai R4); post-Chase Investigate guard confirming a player at a hide-spot front — carve-out scoped to noise/alert-directed guards, a Chase-derived look-around outcome undefined (ai R5); catch-closure invariant silently forces catch range ≥ ~2.5-5.1 m — a natural 3 m fails at 2 Hz + 7.2 m/s (systems R-1); `forgiveness_floor` has no committed >0 lower bound + "strictly below" is unquantified — commit a float-precision margin/epsilon (systems R-3 + unity F3); anti-puppeting wariness lands on the tool user — the residual-share is the load-bearing knob (game R2); "bounded time under base" overstated — spaced ~60-s peeks outlast the 45-60 s decay (game R3); confirm-window cancel is a Pillar-2 boundary — must stay a judgment window, not a reflex check (game R5); `SamplePosition` nearest-point thin-wall artifact + pin `agent.CalculatePath` vs static `NavMesh.CalculatePath` + `PathInvalid` handling (unity F8/F9).
- **Systems GDD**: merged multi-guard episode grade integral undefined (sum vs max) — benign for the S-band, load-bearing for A/B band thresholds (systems R-2); Burst quantity/replenishment/cap — Pillar 3's "tool use is a limited resource" is a bare claim (game R6); forgiveness-floor value, compounding law, band thresholds.
- **Level GDD**: "~35-40 min for quicker players" unsupported — model floor is 45 min (32.14×1.3 + 0.10×32.14), 35-40 requires a multiplier below the committed ×1.3 (level R1); AC(a)'s sub-floor-only tolerance is stricter than grade/claim-2 (cancelled peeks excluded there) — pure-timing routes must be clearance-clean, not peek-clean (level R2); hide-spot guard-reachability/backstop pins scoped to the certified-route vicinity but the chase-hang hazard applies to any hide spot that becomes an off-route LKP (level R3); "manipulation verb" inconsistency — Pillar 3 commits exactly 2 verbs but the camera-disable choke is treated as a verb-gated crossing (level R4); AC(d)'s enumeration of reachable off-mesh/path-partial positions unspecified (level R5); escape-tail pressure-event/budget status unspecified (level R6); per-segment row arithmetic now reconciled to the exact closure (round-18 Level-GDD handoff closed).
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate — the carried approval condition): claim-2 residual-floor value `ε_residual` committed nowhere (qa 3); claim-1 "contributing event" rater rubric (qa 4); milestone-0 gate omits catch range + margin (qa 5 — now partially closed by B-4's gate edit, but the *values* are still uncommitted); claim-2 "first post-observation attempt" vs "execution miss does not consume the attempt, K=3" collision (qa 6, carried); unscored-tester denominator — a tester stuck at zero scored attempts silently counted as failure (qa 7); claim-3 "presented the tool" has no trace/observation anchor (qa 8); trace flush gated on a cause statement, which a clean claim-2 completion never produces — the "recorded rather than misattributed" guarantee is false as written (qa 9); AC(b) transit-attempt sample count unspecified (qa 10); AC(c-2) crossing movement state / footstep-noise scope unspecified (qa 11); AC(d) sub-grid stand-off pockets not bounded (qa 12, carried from round-16); claim-1 N≥5 depends on claim-2 failure rates → INCONCLUSIVE risk (qa 13).

### Nice-to-Have (carried to GDD tiers)
- Carried from review #18 (unchanged): catch-timer FSM-contract unit test; Linecast exact ray parameters; `PathInvalid` handling; catch-check cadence pin; full 4-term catch-closure chain in-text; give-up-window/cap state at hide-spot → live-pursuit transition; slope-aware |ΔY| rejection; chase-vs-noise; spot-verification "incident" boundary; S-achievability note; camera-trip residual allocation; confirm-empty duration pin; ring-buffer allocation; WebGL clipboard flush + user-gesture context; chaser obstacle-avoidance; residual-decay vs segment-pacing; runtime hang-guard; carve-out terminality op-def; WebGL draw-call/vertex budget; "pressure beat" vs "pressure event" terminology. New: carve-out timer banking + telegraph ordering (telegraph window ≥ catch-timer duration not guaranteed) [ai N6]; deep off-mesh "no immunity" enforced only by AC(d), not the FSM [ai N7]; `V_run` range (1.0-3.6 m forcing [5.0, 7.2] m/s) unstated [systems N1]; residual-decay vs segment-length tension [systems N2]; camera-only ⊂ guard-poor subset implicit [systems N3 + level N4]; escalation-event "BASE" qualifier missing [systems N4]; spot-verification "incident" boundary undefined [systems N5]; |ΔY| boundary inclusive-vs-strict at equality [systems N6]; `rate_max`/`k` units (per-tick vs per-second) uncommitted — the one commitment that makes the grade input well-posed [systems N7]; S-band verb-agnostic — pure-timing achieves S without engaging the Pillar-3 fantasy act (consequence of the committed "consequences, not verbs" decision) [game R1, acknowledgment not edit]; S-permanent-taint motivation cliff after first escalation [game R4, playtest observation]; AC(a) parenthetical overstates S-strictness [game N1]; claim-2 "first attempt" surface wording [game N2]; off-route stand-off cheese [game N3]; grade card should label the carve-out distinctly [game N4]; AC(d) inline "≈1.0-1.5 m" bound re-introduces the authoring band [unity F5 + qa 14]; WebGL IndexedDB/clipboard precision — "no runtime file system" is imprecise + `navigator.clipboard` needs a user gesture + secure context [unity F6]; no committed WebGL draw-call/vertex budget [unity F7].

### Status
Revisions applied (round-19 changeset, user-approved, 2026-08-16 — blocking fixes only, zero new content). Review #19 = final full 6-agent panel confirmation of the round-18 changeset — all six lens-owned round-18 pins confirmed textually present + formula-consistent (systems-designer CLEAN at the formula layer; game-designer no BLOCKING; creative layer converged and untouched); **zero changeset-introduced regressions** (first round in the cycle with no fresh collision class); 8 latent contract/verifiability blockers resolved in-session via an 8-clause surgical changeset (reachability qualifier pinned to the gated catch rule; AND-form backstop pinned, L1-sum deleted; rows reconciled to the exact closure [user: 2-dp]; guard sampling ≤ margin + catch range/margin at the gate; camera-bearing non-guard-poor segments ≥4 min [user: authoring rule]; line-238 trace schema completed; AC(d) hide-spot set gains the interior-|ΔY| pin; Linecast endpoints + `QueryTriggerInteraction.Ignore`). **Converging — strongest signal yet** (blocker count 13 → 10 → 8; formula layer down to one committed-number contradiction, now closed; first round with zero changeset-introduced collisions), not yet converged — the AC(d) sweep instrument remains the densest contract surface. CD standing rule: the round-19 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; an 8-clause changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-16 — Verdict: NEEDS REVISION (focused)
Scope signal: XL
Specialists: ai-programmer, unity-specialist, level-designer, qa-lead, game-designer, systems-designer, creative-director
Blocking items: 10 | Recommended: 7 (plus ~30 carried GDD-tier handoffs)
Summary: Final full 6-agent panel confirmation of the round-19 changeset (8 surgical clauses, user-approved 2026-08-16). All eight round-19 clauses confirmed textually applied — systems-designer verified the formula layer CLEAN (accumulator rate, `T_entry` floor, strict-`<` forgiveness, catch-closure invariant, |ΔY| family, content-band exact closure S ∈ [32.14, 41.38], S-band event accounting); game-designer no BLOCKING; creative layer converged and untouched; **zero changeset-introduced regressions — the first clean round in the cycle**. Panel surfaced **10 latent one-clause contract/verifiability blockers**, all ruled BLOCKING at concept tier by the creative-director: the catch-contract cluster (B-1 give-up-resume `AND`→`OR` — catch = path-arrival AND backstop, so "cannot catch" is OR; the path-blocked-but-backstop-pass and navmesh-reachable-but-|ΔY|-fail stuck-states both livelock Chase; also restores the ONE-rule "reachability ⟺ catchability" [ai]; B-2 accumulator-driven Chase entry un-gated on reachability — give-up→Investigate→Chase infinite loop on a staring-but-unreachable player, "no instant re-Chase" had no mechanism [ai]; B-3 fixed +1.0 m Linecast offset stance-blind — false catch through 0.6–0.9 m cover a crouched player genuinely occupies [unity]; B-4 catch-check cadence unpinned — 60 Hz synchronous path queries hitch single-threaded WebGL during chase [unity]; B-5 path-arrival API unpinned — static `NavMesh.CalculatePath` ignores radius/areaMask [unity]) + B-6 AC(d) lattice misses sub-grid stand-off pockets → exhaustive per-walkable-cell centroid sampling [level]; B-7 verb-route guarantee un-satisfiable in a permitted camera-only major area → guard-presence floor (every major area retains ≥1 guard, never zero; camera-disable a route-opener, never a manipulation verb) + "camera-only"→"guard-poor" rename [level]; B-8 trace never flushed on clean claim-2/3 completion → flush at every attempt boundary [qa]; B-9 claim-2 ε_residual committed nowhere + no reset → residual-decay wait + [2] consecutive above-floor starts cap the sub-bar, K=3 terminates [qa]; B-10 claim-3 lacks the N≥5 INCONCLUSIVE floor → inherits claims 1-2's [qa]. Both specialist disputes adjudicated by CD: B-7 vs game-designer R5 (level-designer wins on substance — a pure deferral hands the Level-GDD author an unsolvable spec; game-designer's resolution instinct adopted via the one-clause guard-presence floor, Level GDD enumerates topologies); qa B-8/B-9/B-10 + level B-6 concept-tier vs deferrable (all four are contradictions/false-certification gaps in the concept's own committed text — BLOCKING inline; protocol detail re-homes: sweep cell enumeration → Level GDD, flush/decay protocol + ε_residual value → playtest plan). All resolved via a user-approved 10-clause surgical changeset (blocking fixes only, zero new content). **Strongest convergence signal yet — CD projection: ~1 more full-panel round under the deferral path, ~3 under full inline pinning; the next round is the final one if the round-21 confirmation finds zero new concept-tier blockers (a second consecutive clean round)**. CD standing rule: the round-21 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; a 10-clause changeset is not safe to self-certify). Approval remains conditional on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-21 changeset for that re-review.
Prior verdict resolved: Yes — review #19's 8-clause changeset confirmed applied with zero changeset-introduced regressions; 10 new latent contract/verifiability blockers (panel-surfaced) resolved in-session.

### Blocking Items (all resolved via the approved round-21 changeset this session)
1. **Give-up-resume `AND`→`OR`** [ai-programmer A] — L74 give-up resumes only when path-arrival fails AND backstop fails, but catch = path-arrival AND backstop, so its negation is OR. Stuck-state 1: path-blocked-but-backstop-passing (thin partition — partial-path corner Linecast blocked) → catch impossible, give-up never resumes, caps suspended under sustained LOS → Chase livelocks. Stuck-state 2: navmesh-reachable-but-|ΔY|-failing (ramp/landing) → same livelock. Contradicts the committed ONE-rule (runtime qualifier and give-up mechanism certify different pass sets). Fix: resume conjunct AND→OR ("resumes whenever the catch gate cannot fire").
2. **Re-Chase loop on an unreachable staring player** [ai-programmer B] — L74 "no instant re-Chase because the player is unreachable" has no mechanism: give-up expires → Investigate → sustained continuous LOS re-charges the accumulator → Chase entry (accumulator path, no reachability gate) → give-up suspends/resumes (unreachable) → Investigate → infinite; duration/re-sight caps never fire under sustained LOS. Fix: accumulator cannot reach the Chase threshold while the player is unreachable by the catch gate's metric (only Investigate-level accumulation proceeds).
3. **Stance-blind fixed +1.0 m Linecast offset** [unity-specialist B1] — L74 round-19 clause B-8: the ray at +1.0 m from floor passes over 0.6–0.9 m parapet cover a crouched player (primary stealth stance) genuinely occupies → reads geometry-clear → partial-path gate passes → catch through intervening geometry, violating the doc's own "never through intervening geometry" commitment + Pillar 2. Fix: offset each endpoint by that actor's current capsule height (guard standing; player's live crouch/stand), never a fixed +1.0 m.
4. **Catch-check cadence silent hole** [unity-specialist B2] — L74 chase-target refresh is throttled 2–5 Hz but the catch metric (`CalculatePath` + `SamplePosition`) is unpinned → literal implementation runs 60 Hz × 2–3 guards synchronous path queries (120–180/sec) = hitch source on WebGL. Fix: pin the catch-reference sample + path-arrival metric to the same shared 2–5 Hz tick; the raw-transform Euclidean backstop is the only per-frame pre-check.
5. **Path-arrival API unpinned** [unity-specialist B3] — L74 static `NavMesh.CalculatePath` ignores agent radius/areaMask → can understate reachability and fire a catch the guard cannot physically execute. Fix: pin `NavMeshAgent.CalculatePath` (instance, agent-parameterized) as the sole path source.
6. **AC(d) sub-grid coverage gap** [level-designer B1] — L234 the committed lattice (spacing ≤ ½ catch range) can miss a player-reachable stand-off pocket narrower than the lattice → false "no stand-offs" certification. Fix: sample the centroid of every walkable navmesh cell — exhaustive per-cell coverage supersedes the grid (guard lattice ≤ margin unchanged).
7. **Verb-route un-satisfiability in a camera-only major area** [level-designer B2 vs game-designer R5] — L114/199 the ≥1-verb-only-route-per-major-area guarantee is un-fulfillable in a permitted camera-only major area: camera-disable is not one of the 2 committed manipulation verbs (Burst/Lure). Fix: guard-presence floor — every major area retains ≥1 guard (guard-poor = ≤1 guard beat, never zero guards); camera-disable is a route-opener, never a manipulation verb; retire legacy "camera-only segment" → "guard-poor segment" (both budget sites).
8. **Clean-completion trace eviction** [qa-lead B1] — L231/238 flush gated on the post-capture cause statement; a clean claim-2/3 completion produces no capture → ring buffer evicts the clean run's trace → "recorded rather than misattributed" false as written. Fix: flush at every attempt boundary (start/end) and additionally after the cause statement (both sites).
9. **Claim-2 ε_residual reset + non-termination** [qa-lead B2] — L229 ε_residual committed nowhere; above-floor attempts don't consume K=3 and no reset rule exists → stall. Fix: a non-scored above-floor start triggers a residual-decay wait (or protocol reset) below ε_residual before the next scored attempt; [2] consecutive above-floor starts cap the sub-bar (K=3 then decides); ε_residual's value commits in the playtest plan.
10. **Claim-3 lacks the minimum-denominator floor** [qa-lead B3a] — L231 claim 3 passes on ≥60% of presented-testers but has no N≥5 / INCONCLUSIVE-below floor that claims 1-2 carry → a 2-of-2 vacuous pass possible. Fix: claim 3 inherits the floor — N≥5 presented-testers, verdict INCONCLUSIVE below.

### Recommended Revisions (deferred to GDD tiers — round-20 handoffs)
- **Systems/Perception GDDs**: AC(a) parenthetical → "reaching the Investigate threshold **and committing** (surviving the confirm window)" — wording fix, no mechanical change [systems A]; state the exact pre-image S ∈ [32.1429, 41.3793] — the rounded band is ~0.06–0.24 s wide at the edges [systems B]; fold AC(c-2) residual-reset into B-9's family + reword "bounded time under base" → "bounded within the residual decay window" [game R1/R4]; spot-front vs exposure-source attribution edge case (hide spot on a segment boundary) [game R2 → Systems GDD].
- **Level GDD**: camera-disable route-openers enumerated; per-segment topologies under the guard-presence floor; exhaustive cell-enumeration mechanics for the AC(d) sweep.
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate — the carried approval condition): ε_residual numeric value (B-9); flush-at-attempt-boundary protocol (B-8); S-strictness motivation cliff probe [game R6]; WebGL clipboard `.jslib` + HTTPS + user gesture; preallocated value-type ring buffer, zero per-frame allocation [unity RECs].
- **Perception GDD**: `PathInvalid` → gate fails, no catch; order `catch-timer duration < re-sight min-duration` [ai REC]; reword "WebGL has no runtime file system" → IndexedDB-backed virtual FS [unity R3].

### Nice-to-Have (carried to GDD tiers)
- All ~30 carried deferrals from reviews #7–#19 (unchanged): AI spec → AI GDD, movement milestone-0 → /prototype, spatial budget → Level GDD, alert-state modifier + LOS charge falloff tuning → Perception GDD, noise-maker physicality → Noise GDD, etc. New: `NavMeshAgent.CalculatePath` vs static already closed inline (B-5); catch-cadence pin closed inline (B-4).

### Status
Revisions applied (round-21 changeset, user-approved, 2026-08-16 — blocking fixes only, zero new content; 10 clauses / 14 edits). Review #20 = final full 6-agent panel confirmation of the round-19 changeset — all eight round-19 clauses confirmed textually applied + formula-consistent (systems-designer CLEAN at the formula layer; game-designer no BLOCKING; creative layer converged); **zero changeset-introduced regressions — the first clean round in the cycle**; 10 latent one-clause contract/verifiability blockers resolved in-session via a 10-clause surgical changeset (give-up-resume AND→OR; reachability gate on accumulator-driven Chase entry; per-actor capsule-height Linecast offset; catch-cadence pin; `NavMeshAgent.CalculatePath` sole path source; AC(d) exhaustive per-walkable-cell sampling; guard-presence floor + camera-only→guard-poor rename; trace flush at attempt boundaries; claim-2 ε_residual reset rule; claim-3 N≥5 INCONCLUSIVE floor). **Strongest convergence signal yet — CD projection: ~1 more full-panel round under the deferral path, ~3 under full inline pinning; the round-21 confirmation is final if it finds zero new concept-tier blockers (a second consecutive clean round)**. CD standing rule: the round-21 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; a 10-clause changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: re-review APPROVES → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-16 — Verdict: NEEDS REVISION (focused)
Scope signal: XL
Specialists: ai-programmer, unity-specialist, level-designer, qa-lead, game-designer, systems-designer, creative-director
Blocking items: 7 | Recommended: 6 (plus ~30 carried GDD-tier handoffs)
Summary: Final full 6-agent panel confirmation of the round-20 changeset (10 clauses / 14 edits, user-approved 2026-08-16). All ten round-20 clauses confirmed textually applied + internally consistent — systems-designer verified the formula layer CLEAN, game-designer no BLOCKING, creative layer converged and untouched, zero changeset-introduced regressions (second consecutive clean changeset). **However the confirmation itself was NOT clean**: the panel surfaced 7 new latent concept-tier blockers, all ruled BLOCKING at concept tier by the creative-director — breaking the round-20 projection that the round-21 confirmation would be "final if it finds zero new concept-tier blockers"; a second consecutive clean round remains pending. The 7 blockers: (F1 game B1) S-band grade semantics inversion — the round-15 confirm-window exemption left sub-threshold-but-entry-reaching exposure grading, inverting the round-17 zero-input S-band; (F2 systems C1) session-length contradiction — "~35-40 for quicker players" (L25) vs the committed ×1.3 first-run model, no sub-45 path existed; (F3 ai B1) reachability predicate dual definitions — give-up resume pinned to path-arrival only while the catch gate is path-arrival AND |ΔY|-backstop; (F4 ai B2) give-up-resume vs hide-spot suspension scope — the round-20 OR-resume would resume the give-up mid hide-spot approach, terminating the chase before spot-front verification; (F5 unity B1) AC(d) sweep Linecast stance divergence — the round-19 per-actor capsule-height offsets re-pinned the runtime clause but not the sweep, so sweep certification and runtime catch could disagree; (F6 qa B1) claims-1-2 cohort unsatisfiability — claim 2's ≥60% clean-pass bar structurally caps the claim-1 5-first-captures floor; (F7 qa B2) claim-3 denominator unanchored — "presented the tool" / "reached" un-operationalized with no telemetry anchor. Three design-policy forks adjudicated by user: F6 = continued-session phase (a clean objective grab does not end the session — later attempts stay playable and scoreable as that tester's first capture); F1 = grade only committed exposure (sub-threshold AND window-cancelled episodes alike excluded from the integral, S ⟺ zero committed exposure AND zero escalation events); F7 = pick-up-reached trace event in both FSM trace schema sites ('reached' = entering the pick-up's reach radius); F2 = re-scope as sub-model claim (quicker players ≈ S×1.0 + escape ≈ 35.4-37.0, explicitly a minimal-observation sub-×1.3 case outside the committed ×1.3 model). All resolved via a user-approved 13-edit surgical changeset (blocking fixes only, zero new content). Blocker count 10 → 7 (net decline resumed after round 20's 10; the 'final if zero' projection did not hold). CD standing rule: the round-21 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; a 13-edit changeset is not safe to self-certify). Approval remains conditional on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). User chose **"Re-review in a new session"** — this entry records the round-21 changeset for that re-review.
Prior verdict resolved: Yes — review #20's 10-clause changeset confirmed applied with zero changeset-introduced regressions (second consecutive clean changeset); 7 new latent concept-tier blockers (panel-surfaced) resolved in-session.

### Blocking Items (all resolved via the approved round-21 changeset this session)
1. **S-band grade semantics inversion** [game-designer B1] — L120 the round-15 "confirm-window-cancelled episodes are excluded from the integral" exempted only window-cancelled peeks; sub-threshold exposure that reached the Investigate entry threshold but stayed below the forgiveness floor still graded (the round-18 AC(a) wording "any transient reaching the Investigate entry threshold would accrue above-floor integral" made the S-band's zero-above-floor condition inverted — a segment with only sub-threshold-sustained exposure was ineligible for S). Fix: grade only **committed exposure** — only exposure that crossed the Investigate entry threshold (base or residual-lowered) feeds the integral; sub-threshold exposure below the entry threshold never grades at all; S ⟺ zero committed exposure AND zero escalation events (both AC(a) wording sites re-pinned).
2. **Session-length contradiction** [systems-designer C1] — L25 "~35-40 for quicker players" vs the committed ×1.3 first-run model (S×1.3 + escape → 45-60 min): a quicker player at the band's lower end would need sub-baseline traversal (≈ S×1.0), which the committed first-run-traversal definition does not provide — a sub-45 figure contradicted the committed model. Fix (user: re-scope as sub-model claim): quicker players ≈ S×1.0 + escape ≈ 35.4-37.0 (32.14×1.0 + 0.10×32.14 = 35.4 to +0.15×32.14 = 37.0), explicitly a stated minimal-observation sub-×1.3 case outside the committed ×1.3 observation model; "minimal observation" added to the F2 table row so the table and prose agree.
3. **Reachability predicate dual definitions** [ai-programmer B1] — L74 the give-up resume was pinned to "geometrically reachable = path-arrival metric" while the catch gate is path-arrival AND |ΔY|-backstop: a |ΔY|-failing ramp/landing player is catchable-false but resume-true → the give-up resumes on a player the catch gate can never reach → Chase terminates while the player is genuinely catchable by walkup. Fix: reachable ⟺ the full catch gate can fire (path-arrival AND |ΔY|-gated backstop, one rule, never path-arrival alone); the |ΔY|-failing player is unreachable for both the give-up resume and the accumulator cap; path-arrival retained as the primary check.
4. **Give-up-resume vs hide-spot suspension scope** [ai-programmer B2] — L74 the round-20 OR-resume ("resumes whenever the catch gate cannot fire") collided with the round-18 hide-spot suspension: a hide-spot LKP whose approach fails the catch gate would resume the give-up mid-approach, terminating the chase before spot-front verification. Fix: the resume applies to **non-hide-spot** LKPs only; hide-spot LKPs remain under the suspension (suspended through approach + spot-front verification).
5. **AC(d) sweep Linecast stance divergence** [unity-specialist B1] — L234 the round-19 per-actor capsule-height clause re-pinned the *runtime* clause's ray but not the AC(d) sweep's ray: a sweep using a different (e.g. fixed +1.0 m) offset could certify stand-offs the runtime would catch through → sweep certification and runtime catch disagree. Fix: the sweep uses the **same parameterized ray** as the runtime — both endpoints offset by each actor's committed capsule height, testing the conservative lowest ray (the player's crouch offset), `QueryTriggerInteraction.Ignore`, World/solid layer-mask; the committed stance/offset set added to the milestone-0 committed inputs.
6. **Claims-1-2 cohort unsatisfiability** [qa-lead B1] — L231 claim 1 requires 5 first-captures (N≥5, ≥80% true-cause) but claim 2's ≥60% clean-pass bar (first post-observation attempt, K=3) means a tester who passes clean on the first attempt produces no first-capture: if ≥60% of testers pass clean, at most ~40% contribute first-captures → the 5-first-captures floor is structurally unsatisfiable with a 5-tester cohort. Fix (user: continued-session phase): the tester session **continues past a clean objective grab** — later attempts remain playable, and a capture on any later attempt is that tester's first capture, scoreable against claim 1 → joint satisfiability restored.
7. **Claim-3 denominator unanchored** [qa-lead B2] — L231 "presented the tool" = "reached the placed pick-up" with 'reached' un-operationalized and no telemetry anchor; a tester who merely walks near the pick-up is counted as presented. Fix (user: pick-up-reached event): 'reached' = the tester entering the pick-up's reach radius, telemetry-anchored via a new **pick-up-reached trace event** in both FSM trace schema sites (L231 + L238); the exact radius is committed in the playtest plan.

### Recommended Revisions (deferred to GDD tiers — round-21 handoffs)
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate — the carried approval condition): ε_residual numeric value (round-20 B-9); the claim-3 pick-up reach radius (this round F7); flush-at-attempt-boundary protocol (round-20 B-8); continued-session attempt-boundary protocol (this round F6); WebGL clipboard `.jslib` + HTTPS + user gesture; preallocated value-type ring buffer.
- **Systems/Perception GDDs**: the S-band zero-committed-exposure wording final state; residual-decay vs segment pacing; `rate_max`/`k` per-tick vs per-second units.
- **Level GDD**: camera-disable route-openers; per-segment topologies under the guard-presence floor; exhaustive cell-enumeration mechanics for the AC(d) sweep (round-20 B-6).
- **Perception GDD**: `PathInvalid` → gate fails, no catch; catch-timer < re-sight min-duration ordering.

### Nice-to-Have (carried to GDD tiers)
- All ~30 carried deferrals from reviews #7–#20 (unchanged): AI spec → AI GDD, movement milestone-0 → /prototype, spatial budget → Level GDD, alert-state modifier + LOS charge falloff → Perception GDD, noise-maker physicality → Noise GDD, catch-timer FSM-contract unit test, Linecast exact ray parameters, WebGL draw-call/vertex budget, etc.

### Status
Revisions applied (round-21 changeset, user-approved, 2026-08-16 — blocking fixes only, zero new content; 7 blockers / 13 edits). Review #21 = final full 6-agent panel confirmation of the round-20 changeset — all ten round-20 clauses confirmed textually applied + internally consistent (systems-designer CLEAN at the formula layer; game-designer no BLOCKING; creative layer converged); **zero changeset-introduced regressions — the second consecutive clean changeset**; but the confirmation was **NOT clean** — 7 new latent concept-tier blockers resolved in-session via a 13-edit surgical changeset (grade only committed exposure; quicker-players re-scoped as a sub-×1.3 case; reachable ⟺ full catch gate; give-up resume scoped to non-hide-spot LKPs; sweep reuses the runtime's parameterized ray + offsets into milestone-0 inputs; continued-session phase; pick-up-reached trace event ×2 sites). **Convergence signal: blocker count 10 → 7** (net decline resumed) — but the round-20 projection that the round-21 confirmation would be "final if it finds zero new concept-tier blockers" **did NOT materialize**; a second consecutive clean round remains pending. CD standing rule: the round-21 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; a 13-edit changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. Next: round-22 re-review clean → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-17 — Verdict: NEEDS REVISION (focused)
Scope signal: XL
Specialists: game-designer, systems-designer, ai-programmer, unity-specialist, qa-lead, creative-director (6 agents)
Blocking items: 8 | Recommended: 13 (incl. the carried approval condition)
Summary: Final full 6-agent panel confirmation of the round-21 changeset (7 blockers F1-F7 / 13 edits, user-approved 2026-08-16). **All thirteen round-21 edits confirmed textually applied + internally consistent** — systems-designer CLEAN at the formula layer, game-designer no BLOCKING regressions, unity F5 engine-consistent (sweep reuses the runtime's parameterized ray), qa F6/F7 consistent (continued-session + pick-up-reached trace at both sites), ai FSM clean under the intended reading (Chase-wins precedence, catch-cadence pin). **However the confirmation itself was NOT clean**: the panel surfaced 8 new latent concept-tier blockers, all ruled BLOCKING at concept tier by the creative-director — a second consecutive clean round remains pending. The 8 blockers: (gB1 game B) AC(a) tolerance self-contradiction — the round-17 'forgiveness floor as tolerance' clause re-admitted raw transient samples above the committed-exposure-only rule, making the AC(a)→S-eligibility link logically broken; (gB2 game B) escalation-event precedence split — the never-count rule was un-scoped vs the round-16 'highest-reached-threshold' rule: a residual-lowered Chase-crossing could grade clean through the never-count rule and the grade could not represent the harshest in-world outcome; (gB3 game B) clean-tool promise over-broad — 'never grade-penalizes clean tool use' conflicted with the grade card listing spot-verification Chase events, so a clean toolbar crossing could appear on the card as a Chase penalty (grade-legibility commitment broken); (aB1 ai B) keep hidden caps vs AC(d) hide-spot-resolves guarantee contradiction — chase-duration and re-sight-count caps were suspended only inside the give-up window, but the AC(d) spot-front catch requires the timer to run, so a duration/re-sight cap could terminate the chase while the spot-verification hold is live and the catch timer is ticking → the catch may never fire and the LKP 'resolves at the spot' only in prose; (uB1 unity B) sweep path-query method un-pinned — the AC(d) sweep never committed a path-distance API, so it could use the static `NavMesh.CalculatePath` and understate reachability, disagreeing with the runtime's agent-parameterized instance API; (uB2 unity B) shallow off-mesh not authored — the deep off-mesh no-stand-off rule has no shallow-off-mesh mirror, so a reachable shallow pocket beyond the |ΔY|-tolerance is not swept and could be capture immunity; (qB1 qa B) claim-3 numerator undefined — the numerator ('testers to whom the tool was presented AND used it unprompted') has no counting rule, so the ≥60% bar had no denominator/numerator linkage and could not be tallied cleanly; (qB2 qa B) post-clean attempt structure missing — the round-21 continued-session clause re-armed capture production but gave no structure at all (no re-arm of the objective, route order, attempt boundaries, or end condition). Three design-policy forks adjudicated by user (all recommended options): gB3 = narrow clean-tool promise to "never **directly** grade-penalizes" (the spot-verification Chase event that appears on the card is an escalation event, not a direct grade penalty — the residual it seeds is the in-world consequence); gB2 = Chase-wins precedence — a residual-lowered re-entry that reaches Chase emits one Chase event (the never-count rule scoped to Investigate-level re-entries; highest-reached-threshold governs); qB2 = re-arm + route — after a clean grab the objective re-arms and the tester is directed through remaining testable routes (incl. the Burst-only route, guaranteeing capture production via AC(b)) until first capture or a committed cap of N later attempts, all committed to the playtest plan. Other fixes (no user fork): gB1 = AC(a) tolerance bounded to committed exposure only — a window-cancelled crossing is committed-escalation zero and does not fail the AC, so the AC(a) → S-eligibility link is restored without re-admitting raw transients; aB1 = the hide-spot catch-timer exemption — while the guard holds a live `HideSpotFront` fix the LOS-break pause does not apply (the hold is a catch-timer-live state, a suspension of the pause rather than an LOS-break pause), so the spot-front catch always resolves via the short timer and the caps cannot terminate a live spot-verification hold; uB1 = the `NavMesh.CalculatePath` reason corrected (takes only an areaMask, ignores radius/height) + the sweep's path-query pinned to the same agent-parameterized instance API via a warped hidden agent at each sampled guard position; uB2 = the shallow off-mesh authoring rule added mirroring deep off-mesh (no reachable shallow pocket beyond the committed |ΔY| tolerance within the certified-route vicinity, verified by the same geometric off-mesh probe sweep); qB1 = trace schema pins the noise-source (Burst / movement) at both sites + the claim-3 numerator rule committed to the playtest plan. All resolved via a user-approved 8-edit surgical changeset (blocking fixes only, zero new content). Blocker count 7 → 8 (flat-adjacent; the generation of new latent blockers continued even as prior ones closed — the AC(d) sweep instrument + the grade-legibility surface remain the densest contract surfaces and are now both fully pinned). CD standing rule: the round-22 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; an 8-edit changeset is not safe to self-certify). Approval remains conditional on the qa-lead-approved `design/qa/prototype-playtest-plan.md` (carried condition — file does not exist). Per the standing user decision, the pending confirmation re-review is in a **fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the full 6-agent panel — the round-22 changeset is the review target. **Unless the user chooses otherwise at the closing widget, the concept contract surface is now frozen at the round-22 final state — after the pending clean confirmation, the only remaining gates are APPROVED → `/gate-check` → `/map-systems`; further changes to the concept's contract surface would enter via a new review round.**
Prior verdict resolved: Yes — review #21's 13-edit changeset confirmed textually applied + internally consistent across all 5 specialist lenses (formula layer CLEAN); the confirmation itself surfaced 8 new latent concept-tier blockers (this entry's set), resolved in-session.

### Blocking Items (all resolved via the approved round-22 changeset this session)
1. **AC(a) tolerance self-contradiction** [game-designer B1 / gB1] — L234 the round-17 forgiveness-floor clause re-admitted raw transient accumulator samples within tolerance, contradicting the round-21 committed-exposure-only rule (the only tolerated transients are those that never commit): a certified-clean transit could not imply a null graded integral on the old wording, breaking the AC(a) → S-eligibility link. Fix: tolerance bounded to **committed exposure only** — the only tolerated transient samples are those that never commit (a transient that crosses the Investigate entry threshold and survives the confirm window commits and disqualifies S; a window-cancelled crossing is committed-escalation zero and does not fail the AC); the raw-sample `forgiveness_floor` clause deleted.
2. **Escalation-event precedence split** [game-designer B2 / gB2] — L120 the never-count rule (noise/alert/residual-lowered re-entries never count) was un-scoped against the round-16 highest-reached-threshold rule: a residual-lowered re-entry that reaches Chase would produce zero Chase events and grade clean through the never-count rule. Fix: the never-count rule is scoped to **Investigate-level re-entries only**; a residual-lowered re-entry that reaches **Chase emits one Chase event** for that episode (highest-reached-threshold governs — the harshest in-world outcome never grades clean through the never-count rule).
3. **Clean-tool promise over-broad** [game-designer B3 / gB3] — L75/L78/L120/L145 'never grade-penalizes clean tool use' conflicted with the grade card listing spot-verification Chase events: a clean toolbar crossing (a noise event the player consciously placed) could appear on the grade card as a Chase penalty, breaking the grade-legibility commitment (the player could not predict their own grade). Fix (user: narrow to 'directly'): the promise narrows to "never **directly** grade-penalizes" at all four sites — the spot-verification Chase event that appears on the card is an escalation event, not a direct grade penalty; the residual wariness clean tool use seeds is the in-world consequence, never a grade input.
4. **Hide-spot catch-timer exemption** [ai-programmer B1 / aB1] — L74 the catch-timer pause across LOS breaks while holding a live fix, combined with the round-18 keep-hidden caps (duration + re-sight-count, suspended only inside the give-up window), contradicted AC(d)'s hide-spot-resolves guarantee: a chase-duration or re-sight-count cap could terminate the chase while the spot-verification hold is live and the catch timer is ticking → the spot-front catch may never fire, and the 'hide-spot LKP always resolves at the spot' guarantee holds only in prose. Fix: while the guard holds a live `HideSpotFront` fix (approach + verification), the **LOS-break pause does not apply** — the hold is a catch-timer-live state, a suspension of the pause rather than an LOS-break pause, so the spot-front catch always resolves via the short timer and the caps cannot terminate a live spot-verification hold in mid-resolution (a hide spot is never capture immunity).
5. **Sweep path-query method un-pinned** [unity-specialist B1 / uB1] — L74/L234 the runtime catch contract is pinned to the agent-parameterized `NavMeshAgent.CalculatePath` instance API, but the AC(d) sweep never committed a path-distance method: an implementer could use the static `NavMesh.CalculatePath` (which takes only an areaMask and ignores the agent's radius/height) in the sweep, understating reachability and disagreeing with the runtime. Fix: the sweep's path queries use the **same agent-parameterized instance API** as the runtime — a warped hidden agent at each sampled guard position carries the guard's radius and areaMask — never the static API; the static-API reason corrected (areaMask-only, ignores radius/height → unparameterized centerline path can understate reachability).
6. **Shallow off-mesh not authored** [unity-specialist B2 / uB2] — L234 the deep off-mesh no-stand-off rule has no **shallow off-mesh** mirror: a reachable shallow off-mesh pocket beyond the committed |ΔY| tolerance (a low ledge/step the navmesh samples but the live transform does not foot) is neither authored out nor swept, and could be capture immunity through the |ΔY|-gated backstop (the sole backstop the runtime has for shallow off-mesh players). Fix: the shallow off-mesh authoring rule added — no player-reachable shallow off-mesh pocket within the certified-route vicinity exceeds the committed |ΔY| tolerance relative to its navmesh-sampled reference floor, verified by the same geometric off-mesh probe sweep.
7. **Claim-3 numerator undefined** [qa-lead B1 / qB1] — L231/L238 claim 3 ('≥60% of testers to whom the build presented the tool use it unprompted') defines the denominator ('presented' = pick-up-reached) but never the numerator ('use it unprompted'): no counting rule, no linkage timing, unpumpable. Fix: the claim-3 numerator rule committed to the playtest plan (a tester counts iff their pick-up-reached event is followed by a Burst use on a later attempt — the competency is measured against attempts after the pick-up was presented, never the attempt that reached it) + the trace schema pin at both sites (`noise events` → `noise events with their source (Burst / movement)`), so Burst-mediated noise is trace-identifiable at the source.
8. **Post-clean attempt structure missing** [qa-lead B2 / qB2] — L231 the round-21 continued-session clause (capture on a later attempt is scoreable) re-armed claim-1 capture production but gave no structure: no objective re-arm, no route order, no attempt boundaries, no end condition — the post-clean phase was open-ended and uncommitted. Fix (user: re-arm + route): after a clean grab, the **objective re-arms** and the tester is directed through the **remaining testable routes — including the Burst-only route, guaranteeing capture production via AC(b)'s committed-escalation outcome** — until the protocol's end condition (first capture, or a committed cap of N later attempts); attempt boundaries, route order, and end condition committed to the playtest plan.

### Recommended Revisions (deferred to GDD tiers — round-22 handoffs)
- **Playtest plan** (`design/qa/prototype-playtest-plan.md`, milestone-0 gate — the carried approval condition): ε_residual numeric value (round-20 B-9); the claim-3 pick-up reach radius (round-21 F7); the claim-3 numerator numeric end-condition value (this round qB1, the cap-N and burst-timing); flush-at-attempt-boundary protocol (round-20 B-8); post-clean attempt-structure protocol (this round qB2); WebGL clipboard `.jslib` + HTTPS + user gesture; preallocated value-type ring buffer.
- **Systems/Perception GDDs**: the S-band zero-committed-exposure wording final state; residual-decay vs segment pacing; `rate_max`/`k` per-tick vs per-second units.
- **Level GDD**: the AC(d) sweep's shallow off-mesh probe sweep mechanics (this round uB2); camera-disable route-openers; per-segment topologies under the guard-presence floor; exhaustive cell-enumeration mechanics for the AC(d) sweep (round-20 B-6); the sweep's warped-hidden-agent path-query fixture (this round uB1).
- **Perception GDD**: `PathInvalid` → gate fails, no catch; catch-timer < re-sight min-duration ordering.

### Nice-to-Have (carried to GDD tiers)
- All ~30 carried deferrals from reviews #7–#21 (unchanged): AI spec → AI GDD, movement milestone-0 → /prototype, spatial budget → Level GDD, alert-state modifier + LOS charge falloff → Perception GDD, noise-maker physicality → Noise GDD, catch-timer FSM-contract unit test, Linecast exact ray parameters, WebGL draw-call/vertex budget, etc.

### Status
Revisions applied (round-22 changeset, user-approved, 2026-08-17 — blocking fixes only, zero new content; 8 blockers / 8 edits). Review #22 = final full 6-agent panel confirmation of the round-21 changeset (7 blockers / 13 edits) — **all thirteen round-21 edits confirmed textually applied + internally consistent** (systems-designer CLEAN at the formula layer; game-designer no BLOCKING regressions; unity F5 engine-consistent; qa F6/F7 consistent; ai FSM clean under the intended reading); **zero changeset-introduced regressions — the third consecutive clean changeset**; but the confirmation was **NOT clean** — 8 new latent concept-tier blockers (gB1–gB3, aB1, uB1/uB2, qB1/qB2) resolved in-session via the approved 8-edit surgical changeset (AC(a) tolerance → committed-exposure-only; escalation-event precedence → Chase-wins; clean-tool promise narrows to 'directly'; hide-spot catch-timer exemption; sweep path-query pinned to the agent-parameterized instance API; shallow off-mesh authoring rule; claim-3 numerator rule + noise-source trace pin; post-clean re-arm + route structure). **Convergence signal: blocker count 7 → 8** (flat; the AC(d) sweep instrument + the grade-legibility surface — the two densest contract surfaces — are now both fully pinned, but the panel has generated at least one new concept-tier blocker every single round of the cycle). CD standing rule: the round-22 changeset must go through **one more full 6-agent confirmation before APPROVED** (the panel has caught defects every single round; an 8-edit changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision — never skip the panel). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. The concept contract surface is now at its **round-22 final state** — after a clean pending confirmation, the remaining gates are APPROVED → `/gate-check` → `/map-systems` → `/design-system [perception]`.

---

## Review — 2026-08-17 — Verdict: NEEDS REVISION (focused)
Scope signal: XL
Specialists: game-designer (×2), systems-designer, ai-programmer, unity-specialist, level-designer, qa-lead, creative-director (7 reports / 6 lenses)
Blocking items: 3 | Recommended: 15+ (incl. the carried approval condition)
Summary: Final full 6-agent panel confirmation of the round-22 changeset (8 blockers / 8 edits, user-approved 2026-08-17). **All eight round-22 edits confirmed textually applied + internally consistent** — 4th consecutive clean changeset, systems-designer CLEAN at the formula layer, creative layer holds, zero changeset-introduced regressions. **However the confirmation itself was NOT clean**: CD ruled 3 concept-tier blockers, all one-clause surgical pins, all closures of holes in the round-22 fixes (two of the three are incomplete round-22 fixes, not new collision classes). The 3 blockers: (D1) never-count scoping — the round-22 gB2 fix scoped only the residual-lowered item; the noise/alert clauses were left un-scoped against the line-76 'personal sustained LOS promotes to chaser' rule, so an alert-derived Chase could grade clean through the never-count exception — the exact loophole gB2 was built to close (systems-designer B-2 + game-designer run B B2 independently confirm); (D4) Investigate self-resolve exit — the aB1 fix exempted the LOS-break pause but not the ~4 s Investigate self-resolve, whose 'unreachable' predicate is unpinned against the catch-gate reachability predicate → a second path to hide-spot immunity via the carve-out (ai-programmer Finding 2); (D5) lossy AC(a) sampler — AC verification via ≥10 Hz overlay sampling can miss a committed escalation whose confirm-window is shorter than the 100 ms sample period → AC(a) false-passes, unsound-ing the AC(a)→S-eligibility link gB1 just repaired (qa-lead R-7). The remaining specialist disputes were ruled RECOMMENDED by CD: D2 attribution precedence (game-designer run B B3 — ground already deferred to the Systems GDD in round 20); D3 Patrol↔Investigate livelock (ai-programmer Finding 1 — Perception-GDD pin, Chase-level guarantee honored by the mechanism); D6 'guarantees capture production' over-strong (qa-lead R-1). All resolved via a user-approved 3-edit surgical changeset (blocking fixes only, zero new content): D1 = level-scope all three never-count clauses — noise/alert/residual-lowered entries never count at the Investigate level, but any of these that reaches Chase emits one Chase event (highest-reached-threshold governs), parenthetical corrected (noise/alert entries are consequences of noise/alert, not prior exposure); D4 = self-resolve suspended during a live `HideSpotFront` hold (catch-timer-live state, mirroring the aB1 exemption), with `catch-timer < Investigate-timeout` committed as a Perception-GDD ordering backstop; D5 = AC verification pinned to the event-complete FSM decision trace (the trace line 238 already mandates), the ≥10 Hz overlay sampling demoted to display/spot-check, never the verification source. **Convergence signal: blocker count 8 → 3** (net decline resumed after the round-22 flat). **CD saturation commitment: round-24 is the final confirmation** — if it surfaces a cluster that is not a hole in these 3 pins, that is saturation, and CD will APPROVE with precision delegated to the Perception/Systems/Level GDDs and the playtest plan. CD standing rule: the round-23 changeset must go through **one more full 6-agent confirmation before APPROVED** (standing rule: never skip the panel; a 3-edit changeset is not safe to self-certify). **Pending final confirmation re-review in a fresh session** (`/clear` → `/design-review design/gdd/game-concept.md`) with the **full 6-agent panel** (standing user decision). **BLOCKING prerequisite before milestone-0 unchanged**: write + qa-lead-approve `design/qa/prototype-playtest-plan.md`. The concept contract surface is now at its **round-23 final state** — after a clean pending confirmation, the remaining gates are APPROVED → `/gate-check` → `/map-systems` → `/design-system [perception]`.
Prior verdict resolved: Yes — review #22's 8-edit changeset confirmed textually applied + internally consistent across all 6 specialist lenses (4th clean changeset); the confirmation itself surfaced 3 new latent concept-tier blockers (this entry's set), resolved in-session.

---

## Review — 2026-08-17 — Verdict: APPROVED (conditional)
Scope signal: XL
Specialists: game-designer, systems-designer, ai-programmer, level-designer, qa-lead, unity-specialist, creative-director (7 reports / 6 lenses)
Blocking items: 0 (+ 1 carried approval condition) | Recommended: 15+
Summary: **SATURATION CONFIRMED — final full 6-agent panel confirmation of the round-23 changeset (D1/D4/D5), the round the creative-director designated as final.** All three round-23 edits confirmed textually applied + internally consistent across all 6 lenses (5th consecutive clean changeset; systems-designer formula layer CLEAN; game-designer creative/pillar sweep CLEAN; unity-specialist 'no reason to withhold approval'; qa-lead confirms all claims/ACs testable + D5's lossy-sampler hole closed; ai-programmer confirms the FSM terminates in every state within the AC(d) envelope). The confirmation surfaced **zero new blocker classes** — every finding is a one-clause closure of a round-23 pin or a handoff with a committed downstream home. Blocker count 3 → **effectively 0**; the veto-worthy risk surface is exhausted. **CD ruling: APPROVED (conditional)** with six enumerated delegations binding the downstream gates — C-1 [Level GDD, before first hide-spot build] zero-hang confinement: every authored hide spot lies inside the certified-route vicinity OR the guard-reachability/backstop/vertical pins apply to all hide spots unconditionally (closes ai-programmer's B1 — the closest call, ai right on substance / level-designer right on fixability, splitted into a binding delegation, saturation-class not a new blocker class); C-2 [Perception GDD] carve-out spot-front hold is noise/alert-immune, fix persists until catch/confirm-empty (closes game-designer R1, a hole in D4's closure whose dominant reading already covers it); C-3 [Level GDD] '2-4 min between pressure events' scoped per-segment; C-4 [Perception GDD + playtest plan, before milestone-0] trace protocol records the Investigate-threshold-crossing (confirm-window-start) event + per-escalation trigger cause/position, 231/238 reconciled (closes ai-programmer R2 — a hole in D5's trace protocol, folded into the C-5 gate with no new gate); C-5 [before milestone-0, CARRIED — NOT WAIVED] `design/qa/prototype-playtest-plan.md` must exist + be qa-lead-approved before the engine milestone-0 build, plan must also reconcile claim-2 'first attempt' prose vs the K=3 rule and AC(b) 'guarantees capture production' → 'guarantees committed-escalation production'; C-6 [delegated precision] forgiveness_floor strict-< with real margin ≤ 0.15×T_base + spot-verification cap for converging Chases → Systems GDD; thin-geometry nearest-snap same-surface rule + confirm-window vs time-to-cross-at-floor + catch-range re-validation when V_run lands → Perception GDD; exhaustive-sampling effective spacing + lattice ≤ one-margin band → Level GDD. **This approval is explicitly NOT a milestone-0 unblock.** Remaining gates: APPROVED → `/gate-check` → `/map-systems` → `/design-system [perception]` → Perception/Systems/Level GDD authorship → prototype-playtest-plan → qa-lead gate → milestone-0. The concept contract surface reaches its final state at the **round-23 version**; further changes enter only via `/gate-check` exceptions or a new review round.
Prior verdict resolved: Yes — review #23's 3-edit changeset (D1/D4/D5) confirmed textually applied + internally consistent across all six specialist lenses (5th clean changeset); saturation confirmed so the CD's standing re-review requirement is discharged — no further confirmation rounds for the concept contract surface.
