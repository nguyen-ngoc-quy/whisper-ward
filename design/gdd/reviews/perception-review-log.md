# Perception — Review Log

## Review — 2026-08-18 — Verdict: APPROVED
Scope signal: L
Specialists: None (lean review mode)
Blocking items: 0 | Recommended: 2
Summary: Confirmation-panel re-review of revision 3.4 in lean mode. The sole blocking item from the prior review (J1 suppression-rule wording) verified closed at every statement site: R12, R13, and AC-R12(v) all now read "exactly one escalation decision per escalation tier — no same-tier duplicates", with the escalate-to-Chase path carrying both `investigate-commit` then `Chase-entry` in order (AC-R8 positive leg / AC-F9); R13's closing "or" clause rewritten to the per-tier one-each form; the Overview's "escalation-threshold-crossed" drift killed (grep: zero matches). Registry lock-step confirmed — `trace_event_schema` v3.3 schema unchanged. **CORRECTION (same session):** the "no stale per-episode suppression language" claim was wrong — `/consistency-check` found the registry's `direction_note` + investigate-commit note still carried the pre-rev-3.4 per-episode rule ("EXACTLY ONE escalation decision per episode — its first"), contradicting the approved per-tier GDD. Both lines re-worded to the per-tier rule in `entities.yaml` (2026-08-18). GDD verdict stands; registry re-locked. All 8 gates J1–J8 verified landed on disk. Recommended (non-blocking): AC-R13's "one escalation per episode" could carry an E10-preempt cross-reference to prevent re-review churn; header status note could point to the review log instead of carrying the full J1 narrative. Dependency graph: all 11 declared systems remain forward-contracts to Not-Started GDDs (expected for design order 1, Open Q1). Verdict: **APPROVED** — release to Guard AI FSM (design order 2).
Prior verdict resolved: Yes

## Review — 2026-08-18 — Verdict: MAJOR REVISION NEEDED
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, qa-lead, unity-specialist, performance-analyst, creative-director
Blocking items: 12 | Recommended: 12
Summary: First review. The design spine is sound (two-component meter, confirm-window commit/grace, residual bounded decay, event-not-command contract) but failed as a specification: (1) an AC battery written against starter values its own knob ranges contradict (decay bands excluded legal τ, AC-R7 commit timing impossible, AC-F2 corner ΔA); (2) an event contract too thin for its consumers (no Chase-end, no sight-of-entry, no numeric R, no entry_id); (3) the self-escalation claim violated its own F7 commit semantics and was arithmetically impossible (AC-R11). The creative-director ruled MAJOR REVISION NEEDED — precision work plus one mechanism decision, not a redesign. User chose the cancel-cap mechanism for the peek-exploit (new knob n_cancel_cap) and flat residual.
Prior verdict resolved: First review

## Revision — 2026-08-18 — 12/12 blockers addressed (revision 2)
Decision: **Cancel-cap forced Investigate** (Option A) for the peek-exploit — new knob `n_cancel_cap` (starter 3): window-open-then-cancel increments a per-guard counter; at cap, a forced Investigate commits at the last perceived position. Residual stays **flat** (per game-designer/creative-director adjudication).
Applied: B1 cancel-cap + AC-R11 v2 with R-freeze control; B2 Chase-end event (R12 + states table); B3 hide-entry escalation; B4 schema v2 (entry_id, numeric R, break_type, hide_entry, Chase-end — GDD + registry); B5 decay bands re-derived ([0.009, 0.046] at +48 s); B6 fixture-base note + AC-F2/F7/F8 corrections; B7 F10 max-of-band 5.15, floor 5.2; B8 T_chase safe range capped at 1.00; B9 t_catch floor 1.0; B10 E20 mask separation committed; B11 AC-R3 surface-index fixture; B12 LOS continuity + rendered-cone bias (5°) + AC-R2b. Recommended also landed: confirm-window legibility contract, R14 immunity gateway, R15 suspension-at-fix-set, t_resight_min floor, H.0 harness gains (R-freeze, peer-alert, virtual tick clock). Registry updated in lock-step.
Pending: re-review (fresh session recommended), then Approval.

## Review — 2026-08-18 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, qa-lead, unity-specialist, performance-analyst, creative-director
Blocking items: 10 | Recommended: 15
Summary: Re-review of revision 2 verified all 12 prior blockers (B1–B12) correctly applied. The design spine survived adversarial review (two-component meter, confirm window, residual decay, the user-chosen cancel-cap, event-not-command boundary); the ~10 new blocking items are contract-surface precision failures, not structural ones: the R12 event/trace contract was incomplete against its consumers (no investigate-resolution fruitless channel, no Capture/carve-out record, no numeric-R on noise/alert paths), F8's "(starter 2.8–12.8)" band was arithmetically false and contaminated the locked registry (true starter band [4.0, 6.0]), E20's bystander mask contradicted the approved concept, and several ACs were vacuous (R11 residual leg, F8 s_diff leg, R3 surface-index fixture, R2b render leg). Creative-director ruled NEEDS REVISION — targeted hardening, not redesign. User adjudication (2026-08-18): cancel-cap surfaced as **pips with decay** on the meter's residual side, **sight-committed-only reset** (noise/alert Investigates do not reset), **non-grading** cap-forced Investigate. Revision 3 applied across `perception.md` + `entities.yaml` (10/10 blockers resolved; R12 v3 schema: Investigate-resolution, Capture + carve_out, R-on-escalation; T_floor locked as derived relation; catch-gate per-tick cost bindings in R1/AC-R1; E20 bystander exclusion). Pending: re-review in a fresh session, then Approval.
Prior verdict resolved: Yes

## Review — 2026-08-18 — Verdict: NEEDS REVISION
Scope signal: L
Specialists: game-designer, systems-designer, ai-programmer, qa-lead, unity-specialist, performance-analyst, creative-director
Blocking items: 16 | Recommended: 28
Summary: Third review (full depth). Creative-director: the spine is sound and has been since revision 2 (two-component meter, confirm window, residual, cancel-cap, event-not-command — no specialist called for structural change); every blocker sits on the contract surface. Verified all revision-3 claimed fixes were correctly applied; new stratum is contract *coherence*: (a) the d-band (catch_range+2, R_vis] = (7.5, 12] is undefined and internally contradictory (F1 path-distance domain false vs R1 pre-gate gating the query; AC-R9 d=8 rate 0.1875 unsatisfiable; F2 unreachable cap inoperative in-band; stale-d freeze + bootstrapping hole); (b) F2 same-tick ordering unpinned (Chase fire on capped-unreachable player); (c) cancel counter accrues from the machine's own motion — R12 break_type has no causal class for peek-out or guard-turn breaks; hide-dive double-counts; (d) pip/counter divergence — pips fade on the residual clock while the counter persists → cap fires dark at 2026-08-18-ruling's own forbidden outcome, incl. segment-carryover; (e) window-commit during a live Investigate is a three-way collision (F7/state-table vs R13/E11/AC-R13(a) vs concept line 120 episode model) with no committed-but-unresolved state and no Confirm→Chase row; (f) publisher-ownership split for noise-Investigate + hide-entry Chase violates the doc's own event-not-command pin; (g) MVP battery is phantom-green (Target-tier hide/capture fixtures, H.0(6) omit Chase pack); (h) AC-R2(6) crouch geometry unsatisfiable, AC-R4 wall occludes its own noise, AC-R15 variant vacuous; (i) H.0 lacks CalculatePath counter + R_before/R_after; (j) WebGL cost story calibrated to an unmeasured 10–15 ms premise. FIVE USER ADJUDICATIONS PENDING (from Phase-5 widget): (1) semantics of d in (7.5, 12] — Euclidean-with-in-band-reachability (recommended) vs no-charge-beyond-pre-gate vs clearance-tier; (2) cancel-counter causer attribution — sweep-exclusion + hide-dive-supersedes (recommended) vs causer-blind; (3) pip/counter coupling — decay together / waive-out (recommended) vs persist-until-reset; (4) window commit during live Investigate — episode-model emit+grade per concept line 120 (recommended) vs one-episode suppression; (5) Pillar-2 stance on phase-locked flicker — accept+document (recommended) vs hysteresis. Revision 3.1 to be bounded: adjudications + decision-consequences + doc-precision only, no new knobs/cost stories; P-series recommendeds triaged to QA plan, then confirmation panel re-review. Scope-restructure lever noted: R1 cost story → owning GDD, R12 trace schema → Telemetry (L→M lever).
Prior verdict resolved: Yes

## Revision — 2026-08-18 — revision 3.1 applied (16-blocker third-review pass + 5 adjudications resolved)
Decision: All five Phase-5 adjudications resolved to the panel-recommended options (user-confirmed 2026-08-18): (1) **d semantics in (7.5, 12]** = Euclidean-with-in-band-reachability → **dual-mode d (ADJ-1)**; (2) cancel-counter causer attribution = sweep-exclusion + hide-dive-supersedes → **causal classes (ADJ-2)**; (3) pip/counter coupling = decay-together/waive-out → **ADJ-3**; (4) window-commit during live Investigate = episode-model emit+grade per concept line 120 → **ADJ-4**; (5) Pillar-2 stance on phase-locked flicker = accept+document, no hysteresis → **ADJ-5**. Applied across `perception.md` + `entities.yaml` (registry in lock-step).
Blocker resolution within the 3.1 boundary (adjudications + decision-consequences + doc-precision only — no new knobs/cost stories): (a) d-band closure — F1/R1 dual-mode, AC-R9 d=8 joins the Δ-band; (b) F2 same-tick ordering pinned + AC-F2 same-tick leg; (c) cancel-counter causal attribution in R7/R11/R12/state-table/E6; (d) pip/counter waive-out coupling + non-vacuous AC-R11 control/waiver runs; (e) episode absorption R13/F7/state-table/AC-R13(a); (f) publisher-ownership split R12 + AC-R12 + registry schema (publisher class per record); (g) H.0(6) Chase pack added (AC-R8/R13(b)/E17/F9/F2 legs); (h) AC-R2(6) geometry pinned satisfiable, AC-R4 fixture unblocked (Burst in clear line), AC-R15 variant made non-vacuous (frozen-timer assert); (i) H.0 CalculatePath counter + R_before/R_after; (j) R1/AC-R1 unmeasured-latency correction (old 10–15 ms premise struck). UI legibility contract + n_cancel_cap knob updated for ADJ-2/ADJ-3.
Pending: confirmation-panel re-review (fresh session), then Approval. P-series (~28 recommendeds) triaged to QA plan per the 3.1 boundary — NOT into the GDD.
Prior verdict resolved: Revision 3.1

## Revision — 2026-08-18 — revision 3.2 applied (confirmation-panel 7-blocker pass + wall/segment ruling)
Decision: Confirmation-panel re-review (fresh panel — six specialists + creative-director, verdict `[CD-PERCEPTION-CONFIRM-4]: NEEDS REVISION`, bounded "last closure pass if bounded"). Seven blocking roots, every one a ruling/line/config-derived fix — zero structural changes requested. User adjudication (2026-08-18): **wall/segment classifier = BEHIND COVER COUNTS** — `peek-out` +1 (player-caused); `guard-sweep` +0 (machine motion, excluded); `hide-dive` supersedes → hide path; `wall/segment` +1 iff the player moved during the window (duck-behind-arbitrary-cover counts; only HideSpot entry exempt); simultaneous player-motion + sweep → player-sided motion wins; segment transitions never count.
Blocker resolution within the 3.2 boundary: (B1) wall/segment classifier landed as a procedural rule in R7 (+ R11/state-table/E6/E8/AC-R7/AC-R11(vi)/knob); (B2) reachability-consistent escalation — unconditional Δ-band Chase-cap (A←min(A, T_chase−δ_reach) in (7.5, 12.0], no suppression of legitimate Chase), R15 give-up predicate + livelock closure, E23 PathInvalid mapping (off-link ∞, on-link defer/keep-prior), AC-F2/AC-R15/AC-F8 legs; (B3) R12 escalation-commit junction — cap-reached + confirm-window-elapsed sensing facts (never escalations), FSM investigate-commit on EVERY path with cause enum, commit added to Perception's FSM subscription (ADJ-4 live signal), Capture implies Chase-end, redirected|abandoned resolution causes, entry_id allocated by Perception/relayed by FSM, R13 junction clause, AC-R12 legs (i)–(iv); (B4) registry/schema conformance — trace_event_schema v3.2 (publisher on all records, LOS-break R, Confirm-window-elapsed, Cap-reached, per-episode cause), 13 constants added, noise-radii safe_ranges + max≤R_vis, Dependencies note + Open Q #5; (B5) F2 ΔA legal-corner note (0.40 = 0.8×0.5 never false-failed) completing the same-tick ordering fix; (B6) AC battery — AC-R1 lower-bound + path-derived legs, AC-R8 fresh-base leg, AC-R13 same-tick d=2.571 fixture, AC-R9(a) machine-precision equality, AC-F13 config-derived max≤R_vis, H.0(2) exporter fields + H.0(6) gate additions; (B7) F12 E20 hearing mask + height-offset leg, F11 V_run runtime-max overload (≥7.92 at band max 7.2; starter 7.50) + AC-F11 runtime-max measurement. Registry lock-steps Section G exactly (drift in newly-added ranges caught + corrected).
Pending: confirmation-panel re-review in a fresh session (never inline), then Approval → Guard AI FSM (design order 2).
Prior verdict resolved: Yes

## Revision — 2026-08-18 — revision 3.3 applied (confirmation-panel 8-gate bounded closure pass J1–J8)
Decision: Confirmation-panel re-review `[CD-PERCEPTION-CONFIRM-5]` — NEEDS REVISION, bounded "last closure pass if bounded": 8 approval gates J1–J8, ~16 tagged findings, zero new user adjudications required. User decision (2026-08-18): **"Revise the GDD now"** — full approval for the changeset across `perception.md` + `entities.yaml`.
Gate resolution within the 3.3 boundary: (J1) **junction completion** — `Chase-entry` FSM decision record (Euclid-graded Chase escalation; meter Chase-row trigger on every Chase path incl. hide-entry/carve-out), suppression rule (exactly one escalation decision per episode — its first), "MANDATORY on every path" re-scoped to every *episode-opening* path, A/R freeze on all Chase paths, AC-R12 leg (v); (J2) **unified reachability** — one Perception-owned un-gated per-tick `reachability` fact (R3 sample AND path-arrival AND |ΔY|≤delta_y_tolerance 1.0) consumed by the F2 Chase-cap + R15 give-up predicate + catch-gate geometry; Δ-band separation corrected (old "≥1.3 m" struck; rigorous all-legal-config bound ≥2.15 m at F1); d metric pinned center-to-center, player not carved `NavMeshObstacle`; (J3) **noise-path schema** — Noise-heard `entry_id` (allocated first-noise-heard) + broadcast/per-guard single-consumption + R-at-commit via R-bearing sensing fact (no sync query); (J4) **classifier consistency** — displacement-only decision rule (counterfactual demoted to heuristic), cls_displacement_deadzone 0.10 + cumulative-absolute guard-head yaw cls_sweep_yaw 5° measurement basis, `segment` causal class, guard-head yaw as Perception input; (J5) **battery fixture audit** — one-pass re-derivation, T_sample pinned (AC-R8 0.5 + positive leg, AC-F5 0.2, default 0.5), AC-R1(f) equality closure + bend-crossing strict leg, AC-R11(i) from cycle 1 on, AC-R10 non-fruitless negative probe; (J6) **harness capabilities** — per-tick d readout, continuous-motion injector, catch-gate injector rewired to the reachability fact, Update-accumulated virtual clock authority; (J7) **engine pins** — `isOnOffMeshLink`/`currentOffMeshLinkData.valid` mid-link discriminator (R1/E23/AC-R1(g)), SamplePosition maxDistance ≈ 0.4 m locked (R3/AC-R3), F12 origin +0.25 m + stair-riser/30°-slope probes (AC-F12), own-capsule exclusion + capsule-height ADR note (E20); (J8) **registry/schema conformance** — F10 5.15→5.05 everywhere (F10/F14(5)/AC-F10/AC-F14(5)/degeneracy/Tuning/catch_range floor/C-6), c_noise/R_noise_share effective-min reconcile (F6 + AC-F6), V_investigate≤V_run joint leg (F14(9)/AC-F14(9)), r_guard/r_player LOCKED (operand bands struck), trace_event_schema v3.3 (`Reachability`, `Chase-entry`, Noise-heard `entry_id`, `causal_class` `segment` + `guard_yaw_delta`). Registry in lock-step.
Pending: confirmation-panel re-review in a fresh session (never inline), then Approval → Guard AI FSM (design order 2). This session closes at [A] Re-review in a new session.
Prior verdict resolved: Yes

## Review — 2026-08-18 — Verdict: NEEDS REVISION (1 blocking item — J1 suppression-rule wording)
Scope signal: L
Specialists: None (lean review mode)
Blocking items: 1 | Recommended: 2
Summary: Confirmation-panel re-review of revision 3.3 in lean mode. All 8 gates J1–J8 verified landed (registry lock-step confirmed on disk: delta_y_tolerance, cls_* constants, navmesh_sample_maxdistance, f12_noise_origin_offset, locked r_guard/r_player 0.40/0.35, catch_range max-of-band 5.05, V_investigate joint coupling, trace_event_schema v3.3 with Reachability + Chase-entry). One blocking defect found inside J1's own scope: the suppression rule, stated three times (R12, R13, AC-R12(v)) as "exactly one escalation decision per episode — its first" plus the closing "one Investigate-commit or one Chase-entry per episode", contradicts AC-R8 positive leg ("exactly one investigate-commit then one Chase-entry"), AC-F9, the state-table Chase row, and R13's own absorption clause — an episode that commits Investigate then reaches Chase carries both records. Fix (rev 3.4): suppression rule re-scoped to per escalation tier in R12/R13/AC-R12(v) — one decision per tier, no same-tier duplicates, escalate-to-Chase path publishes both records in order, hide-entry vs threshold Chase mutually exclusive openers, episode grades once at its highest threshold; R13 closing "or" clause rewritten; Overview "escalation-threshold-crossed" → "threshold-crossing" (kills drift against "Perception never emits an escalation"). Wording-only pass — registry and schema untouched. Dependency graph: all 10 declared systems are forward-contracts to Not-Started GDDs (documented, Open Q1) — expected for design order 1, not broken references.
Prior verdict resolved: Yes