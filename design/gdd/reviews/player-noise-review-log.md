# Review Log — Player Noise (`design/gdd/player-noise.md`)

## Review — 2026-08-27 — Verdict: MAJOR REVISION NEEDED (re-review)
Scope signal: L
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer + creative-director (senior synthesis)
Blocking items: 11 | Recommended: 0
Summary: Fresh-session `/design-review` (full) re-review. Confirmations: all 8 first-review blockers landed (stateless relay; limited-resource Burst; two-layer audio folded into the stepped footfall; AC1–AC15 harness rework testable; registry-dedup pass). New verdict: **11 contract-hygiene blockers** — a concentrated pass across the noise contract, not a rebuild. Key fixes: raised-origin Burst ballistics (B1 — flat-form 9.9 m struck, starter lands ≈ 11.5 m in-band [8, 12]); two-ids-two-owners `fact_id`/`entry_id` split, prior OQ4 publish-ingress `entry_id` direction **COUNTERMANDED** (B2); hearing cadence decoupled from the vision tick via a dedicated **5 Hz hearing sub-evaluation** `T_hearing` 0.2 s with `t_audible = t_commit + U(0, T_hearing)` (B8); planar `d_noise` metric + new `hearing_y_tolerance` 4.0 m [3, 5] closing the atrium vertical hole (B10); binary acoustic portals — shared E20 linecast mask for vision/hearing/Burst, **no partial attenuation** (B9); controller-owned audio-only `rustle_event` at `rustle_threshold` 0.70 [0.60, 0.80] of the current stride (B5); camera-azimuth instant-launch throw with arc + landing ghost, pickup-reach radius 1.6 [1.0, 2.5] (B7); FSM noise drop-until-resolution + `t_noise_recommit_cooldown` 2.0 s [1.0, 3.0], noise never opens Chase (B11/E18); cadence leg downgraded to advisory ratio-logging (B3); merged-voice footfall with body+accent auto-blend (B4); guard-proxy audio cue as a first-class contract with dedupe (B6).
Prior verdict resolved: Yes

**Applied same session, 2026-08-27 (re-review revision):** full 11-blocker changeset on the target GDD + registry, plus three Approved-file exception amendments. **Cross-doc amendments (completed):** (1) `design/gdd/perception.md` — hearing sub-evaluation `T_hearing` + planar `d_noise`/`hearing_y_tolerance` metric pin, shared E20 mask note, new E24, AC-F12 legs (B8/B10/B9), and the R5/R12 two-ids-two-owners clause; **the prior rev-3.4 cross-doc obligation (`entry_id` allocation → publish ingress, OQ4) is HEREBY COUNTERMANDED — Perception keeps `entry_id` at first-noise-heard (R12/J3), the emitter allocates the shared transport token `fact_id`**; (2) `design/gdd/guard-ai-fsm.md` — E18 authored (noise-during-live-episode drop + `t_noise_recommit_cooldown` [1.0, 3.0], never-Chase negative leg) + AC-FSM-21; (3) `design/gdd/player-third-person-controller.md` — `rustle_event` emission contract (rule (h), F5g sole-audio-exception, M8 leg, `rustle_threshold` knob). Registry (`design/registry/entities.yaml`): `T_hearing`, `hearing_y_tolerance`, `t_noise_recommit_cooldown`, `rustle_threshold`, `throw_release_height`, `pickup_reach_radius` registered; `burst_throw` v0/θ safe-ranges re-derived for raised-origin (band [8, 12] ⇒ v0 ∈ [8.1, 10.2]); cadence leg annotated ADVISORY (ratio-logged, not load-rejected).

## Review — 2026-08-27 — Verdict: MAJOR REVISION NEEDED
Scope signal: L
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer + creative-director (senior synthesis)
Blocking items: 8 | Recommended: 9
Summary: The emitter's dual audible ledger (controller-owned F5 vs noise-own F1) was struck — NoiseEmitter is now a stateless, aperiodic relay: the controller's `step_event` stream is the sole hearing/audio channel, and the emitter publishes exactly one `Noise heard` per consumed event, recomputing nothing. Limited-resource Burst (throw = permanently consumed; re-arm only at ≥1–2 unconsumed pickups per room; carried-unthrown survives death) closes infinite-noise farming while holding game-concept's "tool use is a limited resource" binding. Two-layer audio (L0 player-local Foley at animation contact that is never a hearing event + L1 ledger-commit accent as the only hearing event) plus the AC1–AC15 relay-harness rework (injected tick counter, `CompareTolerance` ≤ 1e-9 / < eps_d, no `~` nondeterminism) make every criterion independently testable.
Prior verdict resolved: First review

**Applied same session, 2026-08-27:** full revision — stateless-relay contract (F1 struck), ledger/first-commit semantics returned to Controller F5 (0.551 s Walk / 0.456 s Run consumed; prior 0.528/0.416 struck), shared-`entry_id` at publish ingress, limited-resource Burst lifecycle (Placed → Carried → InFlight → Landed → Consumed), hearing geometry (origin + `f12_noise_origin_offset` 0.25, `World/solid` mask with Player layer excluded), aperiodic emission with `t_audible = t_commit + U(0, T_sample)`, AC rework (E1–E18 renumbered, AC1–AC15), registry-dedup pass (constants → `design/registry/entities.yaml`; `burst_throw` block registered).

**Cross-doc obligations:** (1) perception.md rev 3.4 — `entry_id` allocation moves from first-noise-heard (Perception) to publish ingress (NoiseEmitter), exception-based amendment (OQ4); (2) guard-ai-fsm.md — E18 noise-during-Investigate drop-until-resolution, exception-based review; (3) registry — cadence-monotonicity joint leg (F3) registers beside the existing stride-ordering leg (H.0.7).

## Review — 2026-08-27 — Verdict: MAJOR REVISION NEEDED (re-review, fresh session)
Scope signal: XL
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer, performance-analyst, ux-designer, ui-programmer + creative-director (senior synthesis)
Blocking items: 10 | Recommended: 5 | Adjudicated disagreements: 5 (all [A] — recommended)
Summary: Re-review of the 2026-08-27 revision. **Verdict: MAJOR REVISION NEEDED** — the prior 11 contract-hygiene blockers (B1–B11) were addressed at the contract level, but the behavior the contract produces was not stress-tested. The 10 new BLOCKING items break the implementation into three classes: (1) **WebGL-impossible** — F4 raised-origin formula is invalid off flat ground (0.20 m stair riser breaks AC6 tier 3); `dt_max` undefined, doubles `δ_sim` on 33 ms WebGL frames; `LayerMask.GetMask` string-allocates per call; `CompareTolerance ≤ 1e-9` unachievable in single-precision `Vector3`; `fact_id` type/lifetime/unsubscribe policy undefined; aggregate worst-case frame cost ≈12.3 ms with zero slack. (2) **Pillar 1/2 undermined** — Burst push-out formula is geometrically wrong (sphere oscillates into wall or player capsule); E18 cooldown + blanket drop makes a thinking guard LESS intelligent with corroborating noise (Pillar 1); cadence inversion (Fast Walk 2.93 Hz vs Slow Run 1.56 Hz) breaks Pillar 2 readability; fresh-session commit windows (0.46 s Run) too punishing; controlled-risk ladder collapses (crouch 1.8 < 2.3 patrol → never catches up; run 50% louder for 73% more speed → always run). (3) **AC gaps hide all of the above** — AC5/AC6/AC12/13/15 delegate to suites that don't exist for the relay; tick counter / busSpy / fsmSpy contracts underspecified.
Prior verdict resolved: **PARTIAL** — 11 contract-hygiene blockers resolved at the contract level; behavior pass revealed 10 new blockers (the prior pass was a contract pass, not a behavior pass).

**Adjudicated disagreements (5/5 = [A] recommended):** (1) Cadence-monotonicity — **HARD-reject at load** (was ADVISORY); inverts to validate load; load report still flags for Pillar-2 audit. (2) Acoustic portals — **keep binary** (B9 unchanged); no partial attenuation; level uses real geometry. (3) Burst landing ghost — **keep**; trade miss-cost for placement clarity. (4) Footstep voice — **keep merged (B4) with LUFS targets**; add body+accent auto-blend table; no second voice-pool. (5) Hearing sub-eval — **keep 5 Hz (B8)**; decoupled from vision tick; clear ACs.

## Review — 2026-08-30 — Verdict: MAJOR REVISION NEEDED (re-review)
Scope signal: XL
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer, performance-analyst + creative-director (senior synthesis)
Blocking items: 13 | Recommended: 0
Summary: Third fresh-session `/design-review` re-review. Confirmations: B1 (raised-origin Burst), B2 (fact_id/entry_id split), B3 (cadence monotonicity HARD-reject), B4 (Wwise Blend Container + bus true-peak limiter), B5 (controller-owned rustle), B6 (guard-proxy contract), B7 (throw-verb spec), B8 (T_hearing 0.2 s), B9 (binary portals), B10 (planar d_noise + soft Y falloff), B11 (E18 + cooldown + MVP single-guard re-anchor), Pass A AI behavior contract, all senior-blocker amendments (canonical lifecycle table, Burst source_timestamp, thrower-velocity composition, R_eff zero-at-cutoff, production hearing path, LivenessSnapshot contract) — all landed. New verdict: **13 contract-clarity blockers** — a focused tightening pass on the contract itself, not a behavior pass. Key fixes: (1) F4 root selection clarified from "earliest strictly positive" to "minimum strictly positive" with uniqueness proof; (2) AC5 implementation binding to `PerceptionHearingService` scheduler phase and exact-boundary drain semantics; (3) `source_event_class_rank` field added to `NoisePublished` schema (movement=0, Burst=1) and to the F1 variable table; (4) Queue admission order pinned as `hearing_queue_admission_order = (stable_guard_snapshot_position, guard_eid, source_timestamp, fact_id)` in `entities.yaml`; (5) `CompareTolerance ≤ 5×10⁻³ m (5 mm)` for gameplay contracts (pure-math quantizer remains 1 ppm but is NOT a gameplay tolerance); (6) AC15b-MVP re-anchor timer formula corrected: `S_DIFF = s_diff(r_actual, r_investigate)` is a registered speed-ratio (not a fixed scalar); (7) B1/B10 surface-vs-atrium clarification: the F4 band `[8, 12] m` is a readability band, applied to the **actual-height oracle** for non-flat volumes, never a flat-ground projection; (8) AC1b/AC2b cross-state latch testable legs expanded: fixture asserts the field-by-field `busSpy` record carries `stride_target_state` AND `state` simultaneously and that the `radius` value traces to `N(stride_target_state)`; (9) Harness policy updated to 10 named fixtures including `NoiseFixtureSpec` master spec; (10) AC20 manifest field contract aligned with `LevelFixture` schema and `CompareTolerance` references; (11) AC21 OQ6 caveat strengthened: `PENDING_OQ6` is not a passing evidence value, harness must include `Physics.autoSyncTransforms` + bus/audio main-thread cost in measurement boundary; (12) AC19 audio onset documentation: DSP onset is bound to virtual gameplay clock, the audio bridge records the requested virtual gameplay timestamp and the DSP sample start reported by the audio integration, and a nonexistent Wwise scheduled-start callback is never treated as a gameplay clock; (13) Re-anchor budget formula documentation: the selected give-up budget is `max(t_giveup_base × S_DIFF × (1 + k_thorough × (R_reanchor / R_max)), t_giveup_base + t_noise_reanchor_extend)`, with `S_DIFF` defined as a registered speed-ratio. Player Noise remains **In Review** pending a post-revision re-review.
Prior verdict resolved: **PARTIAL** — B1–B11 + Pass A + senior-blocker amendments resolved; the 13 new blockers are contract-clarity refinements, not behavior gaps.

**Applied same session, 2026-08-30 (13-blocker revision):** full 13-blocker changeset on the target GDD, the FSM, the registry, and a new master fixture spec. **Cross-doc amendments (completed):** (1) `design/gdd/player-noise.md` — F4 root selection `minimum_strictly_positive` with uniqueness proof; F1 schema gains `source_event_class_rank`; F1 variable table adds `source_event_id` and `source_event_class_rank` rows; AC5 implementation binding to `PerceptionHearingService`; AC15b-MVP `S_DIFF` formula correction; B1/B10 surface-vs-atrium clarification; CompareTolerance pinned at 5×10⁻³ m; harness policy expanded to 10 fixtures including `NoiseFixtureSpec`; AC20 manifest field contract aligned; AC21 OQ6 caveat strengthened; AC19 audio onset documentation; AC1b/AC2b field-by-field testable legs; re-anchor budget formula documentation; (2) `design/gdd/guard-ai-fsm.md` — `LivenessSnapshot contract (cross-doc amendment 2026-08-30)` section naming the `trace_event_schema` and `LivenessFact {op, tier, cause, source_timestamp, ...}` payload; `S_DIFF` formula in MVP re-anchor timer; `AC-FSM-EX` row asserting write-side LivenessFact publication; (3) `design/registry/entities.yaml` — `cadence_monotonicity_constraint` field (F3(3) re-derivation as HARD load rejection, not ADVISORY); `hearing_queue_admission_order` field (deterministic tuple `stable_guard_snapshot_position, guard_eid, source_timestamp, fact_id`); `max_hearing_guards_per_boundary.note` updated to reference `guard-perception-order`; `burst_throw` invariants `root_policy` and `root_policy_proof` updated to `minimum_strictly_positive`; (4) `design/fixtures/noise-fixture-spec.md` — **NEW** master fixture spec, schema `WW-NOISE-FIXTURE-SPEC-1.0`, `validator=NoiseFixtureSpecValidator.v1`, `profile=MVP`, pins the 10-fixture mandatory set, virtual-clock injection policy, trace field contracts, comparison tolerances, queue admission order, identity/retry/rejection contract, schema versions, cross-fixture parity, and acceptance criteria AC-FX-1 through AC-FX-10.

**Next:** run a clean fresh-context full `/design-review design/gdd/player-noise.md` after the 13-amendment revision. If APPROVED, ask the required tracking widget, then update systems-index/review log and follow the standing approval commit/push workflow; otherwise continue revision without fabricating evidence.

## Review — 2026-08-31 — Verdict: NEEDS REVISION (re-review)
Scope signal: XL
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer, performance-analyst + creative-director (senior synthesis)
Blocking items: 12 | Recommended: 12
Summary: Fourth fresh-session `/design-review` (full). Confirmations: the 13 contract-clarity items landed and held (F4 `minimum_strictly_positive` proof, `source_event_class_rank`, queue admission order, `CompareTolerance ≤ 5×10⁻³ m`, 10-fixture harness, AC20/AC21 alignment, `NoiseFixtureSpec` 1.0, `level_burst_route` registry expansion, lifecycle table). New verdict: **NEEDS REVISION — 12 blockers** in three classes: (1) formula/datum physics — the MVP re-anchor budget was anchored on the `S_DIFF` speed ratio (`s_diff(r_actual, r_investigate)`), which is unmeasurable at the required sampling instant, hits an `r_actual=0` cliff, and contains a dead `max()` floor → re-anchored on the shared difficulty scalar `s_diff` (D1) with `reanchor_speed_ratio` STRUCK and a tombstone rule; the ΔY falloff datum was the guard eye (pose-unstable, inflates shrink at raised endpoints) → datum moved to guard feet, eye demoted to occlusion-ray endpoint, all 7 fixture receiver probes recomputed with an explicit `guard_feet_ws` field. (2) semantics/fantasy — AC19 let `estimated`/trace-only states pass → `confirmed` is the only passing state; Wwise-specific onset capture contradicted the middleware-neutral stack (OQ3 pending) → neutral wording; the ephemeral 3 s landing ghost undermined diegetic aim → persistent while-Carried affordance; suppressed receipts read as AI deafness in visible episodes → suppression micro-tell; the 2.0 ms slice masqueraded as a whole-frame budget → 33 ms p95 whole-frame WebGL gate with 2.0/12.0 ms renamed decomposition records. (3) fixture authoring — gate pickup sat inside the patrol corridor (siting clearance violated) → re-sited ≥ 2.0 m; `failed_ceiling_01` never contacted its ceiling solid (arc overflew it) → overhang re-stage with grounded-launch contact math; `failed_void_01` reused the slab variant (slab wins contact before timeout) → dedicated empty void variant; manifest records deviated from the registry field contract (teaching/signal-A/search-patrol/restart schemas, unknown keys, missing gate `trigger_noise_kind`/`landing_fact_id` binding, room AABB inconsistency) → full conformance pass.
Prior verdict resolved: **PARTIAL** — all 13 contract-clarity blockers landed and were confirmed; the 12 new blockers are formula-anchoring, datum-physics, and fixture-authoring defects the prior contract pass did not reach.

**Escalation note:** third consecutive round where prior-round fixes generated the next round's blockers (2026-08-27 behavior pass → 10; 2026-08-30 contract pass → 13; 2026-08-31 formula/datum/fixture pass → 12). Each round has narrowed (contract → behavior → numeric/authoring), but the pattern argues for verifying revised values by recomputation, not re-assertion, before the next review.

**Applied same session, 2026-08-31 (12-blocker revision, all 8 design decisions [A]/recommended):** (1) `design/gdd/player-noise.md` — re-anchor formula rewritten on `s_diff` with struck-tombstone rule; feet-datum falloff wording; AC19 confirmed-only pass mapping; middleware-neutral onset + OQ3-pending note; persistent while-Carried Ghost Landing Sight; suppression micro-tell; AC21 rebound to `webgl_whole_frame_budget_p95_ms = 33.0` p95 @ 30 fps with 2.0/12.0 ms decomposition records and `stress_30_guard_8_fact` demoted to diagnostic-only; corroboration exact-origin sharp edge documented. (2) `design/gdd/guard-ai-fsm.md` — S_DIFF → s_diff cascade (6 sites: re-anchor block, C1.0, extension paragraph, D1 table + tombstone rows, AC-FSM-21 leg g, AC-FSM-21C). (3) `design/gdd/perception.md` — feet-datum cascade (endpoint paragraph, F12 R_eff/ΔY rows with flat-ground R_walk_eff ≈ 3.75 / R_run_eff ≈ 5.63 / R_burst_eff ≈ 10.37, AC-R12B). (4) `design/fixtures/noise-fixture-spec.md` — schema 1.1 rev: AC19 fixed pass/fail mapping, Pickup/Gate trigger-kind binding, level-manifest record additions (siting clearance, room self-consistency, `receiver_phase_window[]`, teaching `noise_heard` = raw NoisePublished + Patrol precondition), AC21 tolerance rows rebound to the 33.0 ms gate. (5) `design/levels/mvp-burst-route-fixture.md` — gate pickup re-sited `(1.5, 0.0, 6.0)` (≈ 3.20 m clearance); certified launches moved to `(1.5, 1.5, 6.0)` with launch-invariant F4 values kept and migration prose; room AABB `max.y = 4.0` self-consistency; gate wall re-aligned `z∈[5.95,6.05]`; ceiling overhang re-staged `y∈[3.0,3.05]` (contact t = 0.330873 s, x ≈ 4.107); new `geometry_throw_void_open_01`; teaching/signal-A/guard-extended-search/restart re-authored to registry field contracts (`patrol_pre_spend_01`/`patrol_settled_after_spawn_01` windows, six atomic restart steps); gate transition binds `trigger_noise_kind: Burst` + `landing_fact_id`; receiver probes feet-datum recomputed (9.84375 / 3.75 / 4.2 / 0.0-exclusive) with diagonal boundary endpoint `(14.96058, 1.6, 14.96058)` inside the room and verticality moved to raised synthetic sources; `source_position_provenance` → registry `source_position_id`; manifest block trimmed to the registry field list; AC checklist extended; Known Open Authoring Items note. (6) `design/registry/entities.yaml` — `reanchor_giveup_timeout` re-anchored expression, `reanchor_speed_ratio` STRUCK, `webgl_whole_frame_budget_p95_ms` 33.0, field_contract `gate_transitions` += `trigger_noise_kind`/`landing_fact_id`, `receiver_probes` += `guard_feet_ws`, conditional rules `gate_transitions.kind_binding` + `receiver_probes.falloff_datum`.

**Next:** clean fresh-context full `/design-review design/gdd/player-noise.md` in a NEW session (user-selected path: /clear, then re-review). Only a genuine APPROVED verdict may change approval records; no commit/push performed for this revision.

## Review — 2026-09-01 — Verdict: NEEDS REVISION (re-review, **PARTIAL PHASE 3b — tool outage**)
Scope signal: XL
Specialists: audio-director (completed). **NOT RUN — Agent-tool classifier outage:** game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, level-designer, performance-analyst, and creative-director senior synthesis (9 retry batches across two sessions, ~1 h cumulative waits). Main-session Phases 1–3 (completeness, dependency graph, consistency/recomputation) completed: 8/8 sections; dependency GDDs on disk; re-anchor formula recomputed [6.0, 8.0] s starter / [4.8, 11.6] s full domain — matches; F4 33/33 numeric PASS confirmed; cadence ratio ≈ 0.79 re-verified. **This is NOT a complete full-mode review and cannot produce an APPROVED verdict.**
Blocking items: 9 | Recommended: 8+ (audio-director R1–R8 preserved in session transcript) | Unadjudicated: 1
Summary: Audio-director alone returned **8 blockers**: B1 — AC17's "visible to the player" gate (zero-proxy/micro-tell) is an undefined, unowned perception query; player-noise.md:797 conflates "no LOS" with "cannot see." B2 — AC19 `confirmed` onset has no defined report mechanism in either middleware (Unity `PlayScheduled` returns no acknowledgment; Wwise `GetSourcePlayPosition` poll unclassified). B3 — AC19's ≤ −1 dBTP bus-limiter has no evidence-state mapping or capture path; Unity ships no true-peak limiter; native audio DSP unavailable on WebGL. B4 — suppressed-receipt micro-tell delegated to FSM but not authored in guard-ai-fsm.md (bidirectionality violation). B5 — `audio_cue_onset_tolerance_ms` value=20 ms < 1024-sample mix quantum at 48 kHz (≈21.3 ms); registry range [10, 40] cannot contain a quantum-honoring WebGL value. B6 — Blend parameter source field unpinned; AC1b/AC2b latch-divergence commits break F5(f) fairness identity. B7 — sound_performance_audit.md:188-189, 207-209 hardcode Wwise Blend Container + RTPC vocabulary, prejudging OQ3. B8 — "registry-backed coarse quantization steps" for quantized_azimuth/distance_band/occlusion_band: no such registry entries exist. **[main review] B9 (unadjudicated)** — AC20 requires `receiver_phase_window[]` route-gate evidence; the route fixture carries only `patrol_phase_window` — AC/fixture coverage gap (level-designer adjudication pending).
Prior verdict resolved: N/A — review could not complete; the 12-blocker 2026-08-31 revision was confirmed by recomputation in Phases 1–3 but not adversarially re-reviewed in 7 specialist domains (game feel, formula boundaries, AC testability, guard-FSM contract, Unity/WebGL implementability, level certification, performance methodology).

**Applied same session, 2026-09-01 (user-selected: revise now):** 9-blocker revision pass on player-noise.md, guard-ai-fsm.md, sound_performance_audit.md, design/registry/entities.yaml, design/levels/mvp-burst-route-fixture.md, and design/fixtures/noise-fixture-spec.md as required. A complete full-mode re-review (all 8 specialists + creative-director) in a FRESH session remains the required gate before any APPROVED verdict; only a genuine APPROVED may change approval records (then commit+push per standing user preference).

## Review — 2026-09-20 — Verdict: NEEDS REVISION (re-review)
Scope signal: XL
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer, performance-analyst + creative-director (senior synthesis)
Blocking items: 9 | Recommended: 8
Summary: Fifth fresh-session `/design-review` (full). All 9 blocking items adjudicated and revised: (1) Crouch Skip pruned in favor of unified upright stationary ballistics ($v_0=10.0\text{ m/s}, \theta=30^\circ, h_{release}=1.5\text{ m}$) with same-tick stand clearance check `BURST_STAND_CLEARANCE_BLOCKED`; (2) Corroboration re-anchor scatter standardized to exact acoustic centroid ($0.0\text{ m}$) across Burst and footsteps; (3) Corroboration budget harmonized on monotonic formula with 2-corroboration saturation cap; (4) Deterministic total ordering promoted $t_{publish}$ to leading sort key across all queues; (5) Pickup reach linecast origin typo corrected to $1.2\text{ m}$ chest height; (6) AC19b/ADR-0003 partitioned between stock Unity non-clipping peak ceiling $\le 0\text{ dBFS}$ / virtual onset tolerance vs OQ3 middleware native true-peak readback; (7) Level fixture repaired $0.5\text{ m}$ ceiling void, reserve pickup sited $\ge 3.20\text{ m}$ from corridors, Signal-A cover repositioned, and closed gate route retracted; (8) Concave interior corner multi-surface depenetration clamping rule added; (9) Float comparison epsilon $1\times 10^{-6}$ added for cadence ratio.
Prior verdict resolved: PARTIAL — prior 2026-09-01 blockers resolved; full 8-specialist adversarial review identified 9 contract/geometry/audio blockers, all revised and synchronized cross-file across 7 files and C# test suites.

**Applied same session, 2026-09-20 (9-blocker revision, all 3 user design decisions [A]/recommended):** (1) `design/gdd/player-noise.md` — pruned Crouch Skip, unified upright stationary throw, stand clearance check, exact centroid corroboration, monotonic budget, $t_{publish}$ leading sort, chest-height pickup reach, stock vs middleware limiter partition, concave corner clamping, cadence ratio epsilon; (2) `design/levels/mvp-burst-route-fixture.md` — reserve pickup re-sited, ceiling and wall bounds aligned to $4.5\text{ m}$, Signal-A cover repositioned, closed gate route retracted, 5 `F12_OFFSET` typos repaired, Crouch Skip pruned; (3) `design/registry/entities.yaml` — pruned crouch starter configs, updated throw stationary constraint, standardized scatter to $0.0\text{ m}$, registered `limiter_stock_desktop`, registered `BURST_STAND_CLEARANCE_BLOCKED` and `burst-stand-clearance-blocked`; (4) `design/fixtures/noise-fixture-spec.md` & `docs/architecture/adr-0003-audio-virtual-timestamp-boundary.md` — added `limiter_stock_desktop` passing limiter evidence state; (5) `design/gdd/guard-ai-fsm.md` — standardized `r_corroborate_scatter_movement` to $0.0\text{ m}$; (6) `src/AI/Core/NoiseRejectionCodes.cs`, `src/AI/Perception/BurstSimulationService.cs`, `src/AI/Testing/BurstAdmissionVerificationSuite.cs` — runtime and unit test updates for `BURST_STAND_CLEARANCE_BLOCKED`.

**Next:** clean fresh-context full `/design-review design/gdd/player-noise.md` in a NEW session after `/clear`. Status intentionally remains **In Review** pending fresh re-review panel verdict.

## Review — 2026-09-20 — Verdict: APPROVED (re-review)
Scope signal: L
Specialists: game-designer, systems-designer, qa-lead, ai-programmer, unity-specialist, audio-director, level-designer, performance-analyst + creative-director (senior synthesis)
Blocking items: 0 | Recommended: 0
Summary: Sixth fresh re-review confirmation pass. All 9 prior contract and geometry blockers were confirmed landed and mathematically verified. The 4 cross-file synchronization blockers (monotonic re-anchor budget formula parity across entities.yaml and guard-ai-fsm.md, registration of input_buffer_window_s and throw_velocity_snap_threshold, registration of t_investigate_max, and throw-while-moving-rejected diagnostic alignment) were resolved and verified across the canonical registry and downstream consumers. Zero blocking items remain.
Prior verdict resolved: Yes