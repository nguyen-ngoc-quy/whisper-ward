# Unity — Version Reference

| Field | Value |
|-------|-------|
| **Engine Version** | Unity 6 LTS (6000.3.17f1) |
| **Project Pinned** | 2026-08-12 |
| **LLM Knowledge Cutoff** | January 2026 |
| **Risk Level** | LOW — version is within LLM training data |

## Note

This engine version is within the LLM's training data. Unity 6 core APIs
(URP, new Input System, Cinemachine, NavMesh, ProBuilder) are known. Engine
reference docs are optional but can be added later if agents suggest
incorrect APIs.

Run `/setup-engine refresh` to populate full reference docs at any time.

## Project-Specific Packages (planned)

- URP (Universal Render Pipeline) — rendering
- Input System (new) — keyboard/mouse + partial gamepad
- Cinemachine — third-person camera
- NavMesh (AI Navigation) — guard pathfinding
- ProBuilder — level blockout
- Unity Test Framework (NUnit) — unit tests
